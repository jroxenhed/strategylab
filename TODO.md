# StrategyLab TODO

This file holds open work only. Finished work lives in TODO-archive.md, half formed ideas live in IDEAS.md, and decisions that wait on John live in ~/.local/state/strategylab/john-questions.md. The shape of this file is checked by bin/check-todo-plain.py.

## Features

- [ ] <a id="f435"></a> **F435** Plan richer nodes for the node-based strategy builder. John chose on 2026-09-12 to grow it past small nodes, so a Code node with a multi-line value becomes the first customer of a side panel that edits the selected node's parameters, the old F272. Start with a short design note on what a Code node computes and how it is sandboxed. [medium] [features]
## Architecture

- [ ] <a id="f347"></a> **F347** Test whether filings lead price for stocks that few people watch: drift after 8-K and 10-Q filings, split by market cap, dollar volume and analyst coverage. John approved the charter on 2026-09-12 with a symmetric design, so it must be able to show the opposite too. Next: lock the charter with its sampling clock and design MDE, then run it on a worker. [medium] [arch]
- [ ] <a id="f393"></a> **F393** Run the one fresh-data test of the insider-sell idea p-495676dd and write its permanent result entry. John picked the window 2021 to 2026H1 on 2026-09-12, so first rebuild the insider-filing cache past late 2023, then run the test once on a worker. Also merge the constants the two premise_run files both define. Brief: docs/plans/2026-06-10-f393-confirm-window-decision-brief.md. [hard] [arch]
- [ ] <a id="f404"></a> **F404** Search the price, ratings, short-interest and news data for numbers that move before a stock does, and turn the survivors into short testable ideas on the research page. John said go on 2026-09-12. First step is a spec and plan, then the join layer over the four data sets and the return table. Design: docs/plans/2026-06-09-phase1-discovery-scan-companion-note.md. [hard] [arch] [next]
- [ ] <a id="a8"></a> **A8** The chart loads every bar into its series, so a 100K bar 5 minute history is slow. Pass only the visible bar range to the price and indicator series, and load more as the user pans. Auto downsampling at wide zoom already covers the zoomed out case, so this is the last piece. It touches Chart.tsx, SubPane.tsx, useOHLCV.ts, chartUtils.ts and the indicators route. [hard] [arch]
- [ ] <a id="f7"></a> **F7** The folder frontend/src/features/trading now holds 14 components plus 5 test files, up from the 10 that made this a wait item. Split it into subfolders the way nodebuilder already is: bot management, account view, and shared parts, with PaperTrading.tsx as the entry point. Nothing blocks this now. [arch]
- [ ] <a id="f25"></a> **F25** Bots poll for new bars every 10 to 60 seconds, and each poll costs 200 to 500 ms of network time, which caps how fast a bot can react. Replace that polling with Alpaca's live bar stream, which pushes each new bar as it closes. All saved bots already use the Alpaca IEX feed, and the alpaca-py library ships the stream client. [hard] [arch]
- [ ] <a id="f170"></a> **F170** `eval_rule` reads indicator values bar by bar from pandas, which is slow on intraday runs. Change it to read numpy arrays built once per run. Tests for the new fast path are already written and marked expected-to-fail until it lands. Callers sit in backtest, quick backtest, trading, regime, bot runner and exits, so it needs a solo session. [hard] [arch]
- [ ] <a id="f188"></a> **F188** Bots only run on intraday bars, 1 minute to 1 hour, so daily strategies cannot be deployed, and daily is the timeframe where most known edges work. A bot gets new bars by asking the server on a timer, every 10 to 60 seconds, which does not fit a daily bar. The save flow already warns about this. What is left is to make the bot wake once a day after the close. [hard] [arch]
- [ ] <a id="f423"></a> **F423** When a backtest runs too long the server gives up on the request but the work keeps running, so repeated timeouts can pile up and slow everything down. backend/middleware.py has no logging at all, so nobody sees how often this happens. Count the timeouts, log a warning past a threshold, and look at adding stop checks inside run_backtest. [arch]

## Hardening

- [ ] <a id="f382"></a> **F382** Fix the research seed helper: it combines the base seed with the worker number by XOR, which near powers of two can hand two workers the same seed, so two resamples that should be independent are identical. John kept the shared seed across dose levels on 2026-09-12, so only the helper changes. Record a fresh byte-for-byte baseline after the fix. [easy] [hardening]
- [ ] <a id="f211"></a> **F211** The /api/trading/close-all endpoint tells the broker to flatten everything, with no per-symbol check of long or short. No button calls it, so test it by hand with one long and one short open on one broker. IBKR looks the contract up again here, the same spot that hid an earlier routing bug. Confirm both positions close and that each one logs a trade on the correct side. [medium] [hardening]
- [ ] <a id="f422"></a> **F422** Two checks of the same research idea running at the same time can log the same run twice in that idea's history. The duplicate is cosmetic, because the results file already ignores a repeat. Fix it by allowing only one check per idea at a time, in poll_explore_status. [hardening]
- [ ] <a id="f431"></a> **F431** The Gateway alert loop keeps its cooldown timer in memory only, so a backend restart resets it. After a restart the next poll can send one extra alert for a problem that is already known. Save last_state and last_alert_ts to a small JSON file in the data directory, the way bots.json works. The state lives in AlertState in backend/gateway.py. [next] [easy] [hardening]
- [ ] <a id="f432"></a> **F432** The backend registers the IBKR broker only at startup. If the backend starts while IB Gateway is still logging in, IBKR stays unregistered until someone calls the reconnect endpoint by hand. Make the gateway alert loop in backend/gateway.py call init_ibkr when it sees the Gateway logged in with no API connection. [next] [easy] [hardening]

## Polish

- [ ] <a id="f310"></a> **F310** When the chart switches to a bigger bar size to draw fewer bars, the main pane and the sub pane set their data in two separate steps. The chart library can fire a range event between them and push a bar range onto a sub pane that still holds the old bar count. You see a one frame jump, no error. A fix needs both panes to update in one shared step. [medium] [polish]
- [ ] <a id="f426"></a> **F426** A bot card shows months old Fetch failed lines above today's activity, because the 200 entry activity log is saved to bots.json and survives restarts. After a long gap almost every line is a stale warning. Add a date divider, hide entries older than the bot's current start time, or put a clear log button on the card. [next] [easy] [polish]

## Testing

- [ ] <a id="d24b"></a> **D24b** A regime bot switches between long and short when the market trend flips. Run one in paper trading and check it closes the old side before it opens the new one, retries after a failed close, and shows the regime badge on the bot card. The code exists, but no test covers the retry, so only a live run can prove it. [testing]

## Infra

- [ ] <a id="f434"></a> **F434** Both research workers run real jobs from the VM: a small power audit finished on the home 14900k and on mfcore01, which built its own Python env on first launch. Trap: mfcore01 holds only a 300-file subset of the 6,663-file price cache, so a full-scale run there reads 300 stocks and lies. Left: copy the full cache to mfcore01, then add the four render-farm nodes as workers. [medium] [infra]
- [ ] <a id="f433"></a> **F433** The commit check that used to catch a to-do filed without a category tag no longer fires under the new section layout, so untagged items slip in. Make sync-todo-index.py or the pre-commit hook flag an untagged item under any layout, and add a test in bin/tests/. [easy] [infra]
- [ ] <a id="f97"></a> **F97** The overnight builder container has no backend/venv/, so the backend gate only checks that files parse and import instead of starting the server. Either add a venv with pinned deps to that image and restore the start-up smoke test, or drop the promise from docs/overnight-builder-guide.md. The image is not in this repo. [medium] [infra]
- [ ] <a id="f424"></a> **F424** Run bin/worker-pytest.sh against a real worker for the first time. It went in with a dry run only, because no worker answered in that session. Use bin/worker-probe.sh to find a reachable worker, run the whole backend suite including the slow tests, then record the result and the wall clock time in the worker section of docs/private/orchestrator-playbook.md. [next] [easy] [infra]

## Deferred (gated)

- [ ] <a id="b9"></a> **B9** The backtest cost model uses a flat commission and a fixed short borrow rate. Four real broker charges are still missing: margin interest on days when cash goes negative, IBKR Tiered fees, a live hard-to-borrow rate, and currency conversion. Start it when you trade shorts on margin or move to IBKR Tiered pricing. [features] [gated: shorts on margin or IBKR Tiered pricing in use]
- [ ] <a id="c29"></a> **C29** Walk-forward picks the single best parameter point in each training window, and tags it spike when nearby points do much worse. The better method is to group the grid and pick the centre of the best group. That is real extra work, so it only pays off if spikes are common. Start it when spike tags show up often in real runs. [medium] [features] [gated: spike tags in over a third of windows]
- [ ] <a id="c31"></a> **C31** Walk-forward window sizes are set in bar counts today. Let them also take calendar terms like 3 months or 1 quarter, because that is how people compare strategies across timeframes. Parsing calendar terms and converting them to bars per interval needs care. Start it when bar counts get awkward to set in real use. [easy] [features] [gated: bar counts get awkward in real use]
- [ ] <a id="f2"></a> **F2** backend/broker.py is 743 lines and holds the provider protocol plus the Alpaca and IBKR providers. Move it and the three broker_*.py helpers into a backend/brokers package, one file per provider. Keep broker_health_singleton.py as its own module, because it breaks an import cycle. Nothing has changed in these files since May, so there is no pain yet. [arch] [gated: next change to a provider file]
- [ ] <a id="f3"></a> **F3** Results.tsx is 1277 lines and StrategyBuilder.tsx is 1539 lines, and both have grown a lot since this was filed. Sub-panels are already separate files, so what is left to split is the charts and tables inside Results, and the rule sections inside StrategyBuilder. Neither file has changed since early June. [arch] [gated: the next change to either file]
- [ ] <a id="f8"></a> **F8** Backend and frontend describe the same data twice by hand: Python models in the backend, and about 40 matching TypeScript types in frontend/src/shared/types/. If a field goes missing again the way it did in AddBotRequest, build the TypeScript from the backend API description instead of writing it by hand. No new mismatch has appeared. [arch] [gated: a type mismatch causes a bug again]
- [ ] <a id="f10"></a> **F10** Running StrategyLab for more than one person needs real accounts, per-user data, and SQLite in place of the JSON files in backend/data. Today the site sits behind a Google sign-in that admits only John, and all state is single-user JSON. This is a different product, so do not start it until a second person needs an account. [arch] [gated: a second person needs an account]
- [ ] <a id="f205"></a> **F205** The batch endpoint in backend/routes/backtest_quick.py runs symbols one at a time, so a 500-symbol batch spends its budget in sequence. One at a time is deliberate, it keeps the request rate low for the data provider. Make the worker count a setting, keep the default at 1, hold result order by index, and keep today's deadline behaviour. [hard] [arch] [gated: a timed 500-symbol batch is too slow]
- [ ] <a id="f318"></a> **F318** Add delisted companies to the test universe. The price source holds none, so every study reads a survivor-only universe and the measured edge is understated to unknown. The code for a company that dies during a trade is tested only on fake data. No free source carries delisted prices, and the one paid option was rejected on license. [hard] [features] [gated: a study gives a result you would act on]
