# StrategyLab TODO

\*\*228 / 279 shipped.\*\* Themed roadmap. Items indexed **Section Letter + Number** (e.g. B3) for reference. Checked = done; journal has shipping details.

---

## Critical (P1)

_(none open)_

## Up Next

- [F127](#f127) — [next] [medium] Batch quick-backtest endpoint has no request-level deadline [medium]

## Open Work — 74 items

| Section | Topic | Open | IDs |
|---|---|---|---|
| [A](#a-charts-indicators) | Charts & Indicators | 6 | [A8](#a8), [A14b](#a14b), [A14c](#a14c), [A14d](#a14d), [A15](#a15)–[A16](#a16) |
| [B](#b-strategy-engine-rules) | Strategy Engine & Rules | 8 | [B9](#b9), [B30](#b30)–[B36](#b36) |
| [C](#c-strategy-summary-analytics) | Strategy Summary & Analytics | 7 | [C25b](#c25b), [C25c](#c25c), [C26](#c26)–[C27](#c27), [C29](#c29)–[C31](#c31) |
| [D](#d-bots-live-trading) | Bots (live trading) | 5 | [D24b](#d24b), [D27a](#d27a), [D28](#d28)–[D30](#d30) |
| [E](#e-discovery) | Discovery | 4 | [E1](#e1)–[E4](#e4) |
| [F · Architecture](#f-architecture) | Refactors, abstractions, module shape | 14 | [F2](#f2)–[F3](#f3), [F7](#f7)–[F8](#f8), [F10](#f10), [F25](#f25), [F63](#f63), [F71](#f71), [F96](#f96), [F98](#f98), [F113](#f113), [F117](#f117), [F153](#f153), [F158](#f158) |
| [F · Hardening](#f-hardening) | Security, reliability, validation | 12 | [F49](#f49), [F55](#f55), [F59](#f59), [F62](#f62), [F74](#f74), [F83](#f83), [F105](#f105)–[F106](#f106), [F127](#f127), [F154](#f154), [F159](#f159)–[F160](#f160) |
| [F · Polish](#f-polish) | UI, naming, dead code | 5 | [F34](#f34)–[F35](#f35), [F44](#f44), [F140](#f140), [F157](#f157) |
| [F · Testing and Infra](#f-testing-and-infra) | Test gaps, smoke tests, build pipeline | 13 | [F50](#f50)–[F51](#f51), [F61](#f61), [F79](#f79), [F84](#f84), [F89](#f89), [F97](#f97), [F142](#f142), [F144](#f144), [F146](#f146), [F150](#f150), [F156](#f156), [F161](#f161) |

## A — Charts & Indicators

- [ ] <a id="a8"></a> **A8** Chart performance — large dataset optimizations (100K+ 5-min bars):
  - [x] Equity curve detail mode downsample: root cause was missing `toDisplayTime()` shift on equity timestamps — raw UTC timestamps didn't match the main chart's ET-shifted timestamps, breaking crosshair sync and bucket alignment. Fixed by adding `toDisplayTime` to `shared/utils/time.ts` (mirrors Chart.tsx `toET`) and applying it to equity/baseline/trade-tick timestamps in Results.tsx before downsampling. `downsampleEquity()` itself was always correct.
  - [x] Equity curve detail mode sync: pixel-perfect alignment with main chart via bar-count matching. Passed OHLCV timestamps (`mainTimestamps` prop) from App.tsx, built equity/baseline/tick data with same bar positions (whitespace entries for missing values), logical-range sync. Five sub-fixes: `timeVisible`, baseline bar-matching, tick snapping, deferred width sync, invisible left axis.
  - [ ] Viewport-only rendering — only pass the visible bar range to indicator series and markers instead of all 100K bars. lightweight-charts handles panning via `subscribeVisibleLogicalRangeChange`; feed data on demand. Estimated 500-700 lines across 5-6 files (Chart.tsx, SubPane.tsx, useOHLCV.ts, chartUtils.ts, backend /api/indicators); multi-session task. [hard]
  - [ ] Off-screen downsampling — when zoomed out to show all bars, aggregate to coarser resolution (e.g. 15m/1h) for rendering, switch to full resolution on zoom-in. Reduces object count 10-50x at wide zoom. [next][medium]
- [x] <a id="a14"></a> **A14** Chart loading skeleton — show a lightweight placeholder (pulsing grey bars or spinner) while OHLCV data is fetching, instead of a blank chart area. Improves perceived load time on slow connections or large date ranges. [easy]
- [x] <a id="a14a"></a> **A14a** SubPane loading state — `useInstanceIndicators` exposes `isLoading` (aggregated across all active queries via `allQueries.some(q => q.isLoading)`), threaded through `App.tsx → Chart.tsx → SubPanelEntry → SubPane.tsx`. Overlay div uses `CHART_BG_SCRIM` backdrop + animated `<span>` (matches `chart-skeleton-pulse` pattern from A14 ChartSkeleton) so the chart underneath doesn't flicker. [easy]
- [ ] <a id="a14b"></a> **A14b** Skeleton stale-cache behavior — A14 skeleton is invisible when switching to a previously-visited ticker because React Query's `isLoading` is false when stale cache exists. Either use `isFetching` instead, or document the stale-while-revalidate intent with a comment. [easy] (from A14)
- [ ] <a id="a14c"></a> **A14c** ChartSkeleton file location — `ChartSkeleton` component is defined inline in `App.tsx`. Move to `frontend/src/features/chart/ChartSkeleton.tsx` for better code organization. [easy] (from A14)
- [ ] <a id="a14d"></a> **A14d** SubPane loading flag is per-Chart, not per-pane — `instanceLoading` is a single boolean propagated to every `SubPanelEntry`. If only the RSI HTF group is fetching, every subpane (RSI/MACD/Volume) shows the overlay simultaneously. Acceptable today since the regular query covers all instances at once; revisit if HTF groups split across panes get more independent. Convert to a per-instance/per-pane loading map keyed on indicator id. [medium] (from A14a review)
- [ ] <a id="a15"></a> **A15** Equity curve resize handle broken — `resize: 'vertical'` + `overflow: 'hidden'` on Results.tsx makes native resize non-functional. Change overflow to 'auto' or remove resize property. [easy]
- [ ] <a id="a16"></a> **A16** Signal Trace tab undiscoverable — tab only appears when checkbox is checked in builder, no hint the feature exists. Rename checkbox to "Enable Signal Trace tab" and/or always show tab with empty-state message. [easy]
## B — Strategy Engine & Rules

- [x] <a id="b5"></a> **B5** Borrow cost estimation for live short positions — `borrow_rate_annual` field on `BotConfig` (default 0.5%), `entry_time` on `BotState` set at fill, borrow cost computed at exit using same formula as backtest and stored in journal `borrow_cost` field. `UpdateBotRequest` includes the field for in-place edits.
- [x] <a id="b5a"></a> **B5a** Show `borrow_cost` in TradeJournal UI — add a Borrow column to the journal table (only shown when any row has a non-null `borrow_cost`). Data is already stored in journal JSON from B5. Pure frontend. [easy]
- [x] <a id="b5b"></a> **B5b** Total borrow cost in TradeJournal summary row — added `totalBorrowCost` to `summaryStats` IIFE (null-safe reduce). Summary row Borrow cell shows `$X.XXXX` in red when total > 0, empty otherwise.
- [x] <a id="b5c"></a> **B5c** Bot runner borrow cost on cover — exits.py `_compute_borrow_cost()` was already logging borrow_cost on exit. Gap was `entry_time` never set when bot resumes a pre-existing broker position; fixed by setting `state.entry_time = now` when `entry_price` is None on resume. Without this guard, `_compute_borrow_cost()` returned 0.0 for externally-opened shorts.
- [x] <a id="b8"></a> **B8** Spread-derived slippage (follow-up to B7) — live spread display + spread-derived modeled default. `/api/slippage/{symbol}` returns `live_spread_bps`/`half_spread_bps` when IBKR is active (NBBO). `decide_modeled_bps()` uses half-spread when <20 empirical fills (floor 2 bps, cap 50 bps, market-hours only). Skipped for Alpaca free tier (IEX-only quotes are 10-50x wider than NBBO).
- [x] <a id="b8a"></a> **B8a** "Use live spread" button in Capital & Fees — small button next to the slippage input that pre-fills `slippageBps` from `slipInfo.half_spread_bps` when a live quote is available. Cleaner than auto-apply (user-triggered, reproducible). [easy]
- [x] <a id="b8b"></a> **B8b** Slippage auto-reset overrides "Use live spread" — the `useEffect` in StrategyBuilder.tsx that auto-applies `slipInfo` on ticker change includes `slippageSource` in its dep array, so it re-fires when user clicks "Use live spread". If `slipInfo.source === 'empirical'` (20+ fills), the effect immediately overrides the spread-derived value back to empirical. Fix: guard effect body with `if (slippageSource === 'default' || !slippageSource)` so it only auto-applies when source is still at default. [easy]
- [ ] <a id="b9"></a> **B9** Cost model v2 (deferred from B6):
  - Debit-balance-aware margin interest for shorts (charge margin rate only on days net cash is negative)
  - IBKR Tiered pricing (exchange fees, SEC fee, FINRA TAF, clearing pass-throughs)
  - Hard-to-borrow dynamic rate feed
  - FX conversion cost
- [x] <a id="b20"></a> **B20** ~~Multi-timeframe confirmation~~ — superseded by B28 (regime rules as full rule sets evaluated on HTF). The regime system provides a more powerful version: arbitrary rules on any higher timeframe gate or direct entries, rather than just a single confirm signal.
- [x] <a id="b29"></a> **B29** Regime setup UX overhaul — fixed status line to branch on on_flip + short rules presence (was always "flips to short"), relabeled on_flip dropdown ("Close, wait for signal" / "Close, enter immediately" / "Hold (block new entries)"), removed SINGLE tab when regime enabled with auto-migrate of buyRules→longBuyRules, scoped warning to close_and_reverse only (sit-flat gate no longer warned), added coexistence notice when SINGLE+dual rules overlap. Guards use `some(r => r.indicator)` matching backtest activation condition. [medium]
- [ ] <a id="b30"></a> **B30** RuleRow horizontal overflow — complex rules (e.g. Stochastic with K/D/Smooth) clip without scrollbar on narrow windows. Add `overflowX: 'auto'` to rule panel container. [easy]
- [ ] <a id="b31"></a> **B31** Stochastic rule row: 3 unlabeled number inputs — K/D/Smooth only discoverable via hover tooltip. Add 2-letter prefix labels (K, D, Sk) before each input. [easy]
- [ ] <a id="b32"></a> **B32** No Cmd/Ctrl+Enter keyboard shortcut to run backtest — main interaction loop (tweak → run → read) requires mouse every time. Add keydown listener. [easy]
- [ ] <a id="b33"></a> **B33** Run Backtest button missing spinner/cursor feedback — disables and shows "Running..." text but no spinner or cursor change. Add `cursor: 'not-allowed'` + opacity + CSS spinner. [easy]
- [ ] <a id="b34"></a> **B34** Strategy bar: Delete button adjacent to Rename with no separator — fat-finger risk on irreversible action. Group primary actions, separate Delete with spacer or move to kebab overflow menu. [medium]
- [ ] <a id="b35"></a> **B35** Strategy deletion irreversible with no undo — permanently removes from localStorage. Add 5-second undo toast (keep in memory, persist only after window expires). [medium]
- [ ] <a id="b36"></a> **B36** Results tab bar overflow — wraps unpredictably on narrow panes when 10+ tabs shown. Use `overflowX: 'auto'` + `flexWrap: 'nowrap'` or fixed height. [easy]
## C — Strategy Summary & Analytics

- [x] <a id="c22"></a> **C22** Auto-optimizer — run sensitivity sweeps automatically across multiple parameters, find the combination that maximizes Sharpe / return / win rate. Like C18 but multi-dimensional: sweep param A, pick best, sweep param B, iterate. Show a ranked table of top N parameter combos. Could use grid search for small spaces or Bayesian optimization for larger ones. The "push button, get optimized strategy" workflow. [large]
- [x] <a id="c25"></a> **C25** Optimizer param range NaN handling — if user types a non-numeric value (e.g. 'abc') into the Min/Max fields, `parseFloat` returns NaN which silently produces an all-NaN linspace. Add a `isNaN(minN) || isNaN(maxN)` check to the C23 validation guard. [easy]
- [x] <a id="c25a"></a> **C25a** Optimizer NaN guard improvements — `runOptimizer()` validation split into per-field checks: (a) "Min/Max/Steps is not a valid number" identifies the offending field, (b) `isNaN(stepsN)` guard fires before `< 2`, (c) "system default Min/Max is missing — enter a value manually" when user left the field blank and `opt.defaultMin/Max` is NaN. [easy] (from C25)
- [ ] <a id="c25b"></a> **C25b** OptimizerPanel submission path silently defaults invalid Steps — `runOptimizer()` validation rejects empty/NaN Steps, but the request build at the bottom of the same function uses `Math.max(1, parseInt(p.steps) || 5)`. The two paths diverge: validation says "blank Steps is an error", submission says "blank Steps becomes 5". Validation guards the live UI today, but a refactor that reorders the loops could ship invalid steps to the backend. Replace the submission `|| 5` fallback with the already-parsed `stepsN` so there's a single source of truth. [easy] (from C25a review)
- [ ] <a id="c25c"></a> **C25c** OptimizerPanel ordering invariant has no test — `runOptimizer()` validation now has 7 ordered branches; the `isNaN` checks must fire before `min > max` because `NaN > N` is always false in JS, so a typed-NaN min would silently pass the comparison. Add `OptimizerPanel.test.tsx` using the existing `renderHook`/`userEvent` infra (see `useOHLCV.test.ts`) covering all 7 branches with exact error-message assertions. Each branch maps to a specific user-visible string — refactors that reorder the checks would silently regress without these tests. [easy] (from C25a review)
- [ ] <a id="c26"></a> **C26** Trades table has no sort/filter — chronological only, can't find biggest losers. Add sort-by dropdown (P&L high→low, P&L low→high, hold duration, exit type). [medium]
- [ ] <a id="c27"></a> **C27** Metrics strip shown on Equity tab wastes 60px — same 8 metrics as Summary tab. Show full strip on Summary only; compact 2-metric header on Equity. [easy]
- [x] <a id="c28"></a> **C28** Walk-forward analysis (daily + intraday) — split history into rolling in-sample (IS) / out-of-sample (OOS) windows, optimize parameters on IS, score on adjacent OOS, then roll forward. Intraday added same-branch via `_format_boundary` helper (HH:MM:SS strings on window edges) + loosened YahooProvider date parsing (`pd.Timestamp` instead of strict `strptime`). Direct extension of C22 (auto-optimizer); replaces ad-hoc "tweak params, re-run on the same dates" workflow that overfits silently. Output: per-window IS-best params, OOS metric per window, concatenated/rescaled OOS equity curve (each `run_backtest()` resets capital to `initial_capital` — naive concat produces a sawtooth, must rescale at window boundaries), stitched walk-forward Sharpe vs simple-backtest Sharpe, Walk-Forward Efficiency (WFE = OOS/IS perf ratio; ≥0.5 = marginal, ≥0.7 = healthy per Pardo), parameter-stability CV across windows. Anchored (expanding IS) vs rolling (fixed-width IS) — both available, default rolling, `expand_train: bool` switch (sklearn `TimeSeriesSplit` defaults anchored which trips people up). IS objective v1: Sharpe peak + neighborhood-stability tag (check whether grid neighbors of the IS winner also rank in the top quartile — `"stable plateau"` vs `"spike"` flag). Practitioner consensus: 3:1–4:1 IS:OOS ratio, step = OOS length (non-overlapping OOS for clean stats), ≥6 windows, ≥30 trades per OOS window minimum. Window units: bar count (`is_bars`, `oos_bars`, `gap_bars`). Builds on `backend/routes/backtest_optimizer.py` + `_apply_param` in `backtest_sweep.py`. New endpoint `POST /api/backtest/walk_forward`, new `WalkForwardPanel.tsx` sub-tab in `Results.tsx` alongside `OptimizerPanel`. Gotchas: regime `_compute_regime_series()` HTF lookback extends before window start (may clip intraday limits); indicator warmup (RSI 14, MACD 35) eats short IS windows; no `end > start` validation in `StrategyRequest` — degenerate windows raise 500 not 400. Cost: 10 windows × 50 IS combos = 500 backtests; `_MAX_COMBOS=200` cap is per-optimize-call, not per-WFA. Plan: `docs/plans/2026-05-12-walk-forward-analysis.md`. [hard] (added 2026-05-12)
- [ ] <a id="c29"></a> **C29** Cluster-centroid IS selector — upgrade C28's neighborhood-stability tag to true cluster-based parameter selection (k-means on the IS grid with Silhouette Score to pick k, k=1 fallback to median). Practitioner-preferred over Sharpe-peak per Harbourfrontquant / QuantBeckman / BuildAlpha; centroid of the largest high-performing cluster is more robust than the peak point. Justification gate: ship only once C28's `"spike"` tag rate in practice is high enough to warrant the added complexity (k-means + Silhouette + k=1 handling on a 1000-point max grid). Until then C28's cheap neighborhood tag covers the same anti-overfit signal. [medium] (from C28 deferred design decision) (added 2026-05-12)
- [ ] <a id="c30"></a> **C30** Combinatorial Purged Cross-Validation (CPCV) — alternative to single-path WFA that generates a *distribution* of OOS paths via combinatorial train/test draws + bar-purging to prevent leakage. Output: Probability of Backtest Overfitting (PBO) score + Deflated Sharpe Ratio. López de Prado "Advances in Financial Machine Learning" (2018) + SSRN 3257497. 2024 ScienceDirect study shows CPCV produces lower PBO than standard WFA. Stretch goal — purging matters most for ML-style overlapping labels (triple-barrier), less for rule-based crossover signals; embargo (`gap_bars`) already covers the main rule-engine leakage path. Ship after C28 is in real use and the user wants distributional evidence beyond single WFE. [hard] (from C28 deferred design decision) (added 2026-05-12)
- [ ] <a id="c31"></a> **C31** Calendar-term window inputs for walk-forward — accept skfolio-style pandas-offset strings (`"3MS"` = 3-month start, `"QS"` = quarter start) in addition to bar-count for `is_bars`/`oos_bars` on C28. Practitioners think in calendar terms ("12 months in, 3 months out"), not bar counts, especially when comparing strategies across timeframes. Defer until bar-count proves awkward in practice — `pd.tseries.offsets` parsing + bar-count conversion per interval is non-trivial validation surface. [easy] (from C28 deferred design decision) (added 2026-05-12)
## D — Bots (live trading)

- [ ] <a id="d24b"></a> **D24b** Regime bot visual verification — D24 not visually verified. Need to run a regime bot in paper trading to confirm flip sequence, pending_regime_flip retry, and BotCard regime status display. Manual QA item.
- [x] <a id="d24d"></a> **D24d** Regime HTF cache staleness — `_fetch()` TTL is 1 hour for daily intervals. Regime direction could lag by up to 1 hour after a real flip. Fixed: `_TTL_DAILY_LIVE = 300.0` (5 min) applied when `end >= today` for non-intraday intervals. [medium]
- [x] <a id="d26"></a> **D26** IBC Gateway automation — IBC 3.23.0 installed at `~/ibc/install`, auto-starts IBKR Gateway paper trading via launchd plist. Auto-fills credentials, handles daily restart at 05:00 ET. Only manual step: weekly Sunday 2FA push on IBKR Mobile. [medium]
- [x] <a id="d26"></a> **D26** FundBar invalid input feedback — added `inputError` state to `FundBar`; `handleSet()` validates empty/NaN/negative and sets error instead of silently no-opping; red-bordered input + inline error message shown below input in both the initial setup and edit-mode layouts. [easy]
- [x] <a id="d27"></a> **D27** Bot status tooltip — hovering over a bot's status badge (running/stopped/error) should show a tooltip with last tick time, current P&L, and position size. Currently requires expanding the bot row to see this info. [easy]
- [ ] <a id="d27a"></a> **D27a** Status tooltip upgrade to custom popover — D27 uses the native browser `title` attribute (grey system tooltip, ~1s hover delay, no styling control). A positioned div overlay matching the app's dark theme (following the `TradeTooltip.tsx` pattern) would be more consistent and readable. [medium]
- [ ] <a id="d28"></a> **D28** AddBotBar double-submit — no loading guard on async handleAdd, can create duplicate bots. Add `adding` state to disable button during submission. [easy]
- [ ] <a id="d29"></a> **D29** AddBotBar doesn't copy strategy direction — selecting a short strategy leaves direction at 'long', risking wrong-way trades. Auto-set direction from selected strategy in onStrategyChange. [easy]
- [ ] <a id="d30"></a> **D30** Bot activity log dead-end — "No activity yet" gives no context. Show "Waiting for next tick" (running) or "Bot is stopped" (stopped) instead. [easy]
## E — Discovery

Own multi-session research project. Needs its own design work before implementation.

- [ ] <a id="e1"></a> **E1** Scan for good StrategyLab candidates (criteria TBD)
- [ ] <a id="e2"></a> **E2** Batch backtesting (efficiency-critical)
- [ ] <a id="e3"></a> **E3** AI/ML assisted parameter tweaking
- [ ] <a id="e4"></a> **E4** Pipeline: present candidates -> spawn bot army
## F — Architecture & housekeeping


### F · Architecture
- [ ] <a id="f2"></a> **F2** Group backend broker files into a `backend/brokers/` package **and** split `broker.py` (~680 lines) into `brokers/{yahoo,alpaca,ibkr}.py` behind the existing `TradingProvider` protocol. `broker_aggregate.py`, `broker_health.py`, `broker_health_singleton.py` move into the same package. Preserve `broker_health_singleton.py` as a separate module — it exists to break an import cycle, not as ceremony. Low value, moderate risk — only tackle if friction shows up when editing a single provider. [arch]
- [ ] <a id="f3"></a> **F3** Split `frontend/src/features/strategy/Results.tsx` (~745 lines) and `StrategyBuilder.tsx` (~578 lines) into smaller subcomponents (equity/drawdown/scatter/tables for Results; rule sections for StrategyBuilder). Same tradeoff as F2 — defer until a change actually gets painful. [arch]
- [ ] <a id="f7"></a> **F7** Sub-group `frontend/src/features/trading/` (10 files). Natural split: bot-management (`AddBotBar`, `BotCard`, `BotControlCenter`, `MiniSparkline`) vs account-view (`AccountBar`, `PositionsTable`, `OrderHistory`, `TradeJournal`) vs shared (`BrokerTag`), with `PaperTrading.tsx` staying as the feature entry. Defer until the feature grows further — 10 siblings is borderline, not painful yet. [arch]
- [ ] <a id="f8"></a> **F8** API contract drift watch. `BotConfig`, `StrategyRequest`, etc. are manually mirrored between Pydantic (backend) and `shared/types/index.ts` (frontend). Fine at ~35 types; once drift bites (fields silently dropped via the `AddBotRequest` bug), switch to generating TS types from FastAPI's OpenAPI schema via `openapi-typescript`. Flag only — don't preempt. [arch]
- [ ] <a id="f10"></a> **F10** Multi-user overhaul — proper auth + per-user data namespacing, SQLite instead of JSON for `bots` / `trade_journal`, session handling in the frontend. Don't pursue unless there's real demand for running this as a shared service; it's a different product shape from the current personal tool. [arch]
- [x] <a id="f19"></a> **F19** Migrate bot/journal polling to React Query — 12-14 simultaneous `setInterval` timers with no deduplication. Journal fetched independently by `PositionsTable` (60s) and `TradeJournal` (5s). React Query is already in the project but only used for OHLCV. Migrating would deduplicate, add stale-while-revalidate, and halve the interval count. [medium] [arch]
- [x] <a id="f24"></a> **F24** Configurable poll interval + API rate counter — global `BOT_POLL_MS` setting (default: per-bar-interval fallback) with runtime `PATCH /api/broker/poll-interval` endpoint, persisted to `.env`. `RateCounter` sliding-window deque tracks all trading API calls; `DataRateCounter` tracks data provider calls. AccountBar displays `T:N/200 D:N/min @Nms`. Poll interval input in BotControlCenter. [medium] [spec](docs/specs/2026-05-08-configurable-poll-interval-design.md) [plan](docs/plans/2026-05-08-poll-interval-rate-counter.md) [arch]
- [ ] <a id="f25"></a> **F25** WebSocket bar streaming — replace `_fetch()` polling with Alpaca's real-time bar/quote WebSocket streams. Bots react on each new bar instantly instead of polling. Eliminates the `_fetch()` network latency bottleneck (~200-500ms) that currently caps effective tick rate regardless of poll interval. [hard] [arch]
- [x] <a id="f26"></a> **F26** Shared OHLCV cache per symbol — added `fetch_ohlcv_async()` to `shared.py`: async wrapper around `_fetch()` that deduplicates concurrent bot coroutines at the asyncio Future level. Multiple bots awaiting the same `(symbol, start, end, interval, source)` share one `run_in_executor` Future instead of each dispatching independently. Safe under asyncio cooperative scheduling (no await between dict check and dict store). bot_runner replaced `_run_in_executor(_fetch, ...)` with `fetch_ohlcv_async()`. [medium] [arch]
- [x] <a id="f28e"></a> **F28e** `BotConfig.direction` missing validator — replaced `@field_validator('direction')` pattern with a shared `DirectionField = Annotated[Literal['long', 'short'], BeforeValidator(str.lower)]` type alias in `models.py`. Applied to both `StrategyRequest.direction` and `BotConfig.direction`. Removed the duplicate `@field_validator` from `UpdateBotRequest` in `routes/bots.py`. Closes the F28 validation pass across all models. [easy] (from F28d) [arch]
- [x] <a id="f41"></a> **F41** BotDetail.state type mismatch (F32 follow-up) — resolved by aligning the type to the runtime defensive code: `state: BotState` → `state?: BotState`. All 9 `detail?.state?.` chains in BotCard.tsx already used double optional chain, so no consumer changes needed. The type now under-promises vs the actual API contract (backend always populates `state`); revisit if the morning review prefers reverting the type and dropping the second `?.` in BotCard. (PR #30 build 22) [arch]
- [x] <a id="f58"></a> **F58** Duplicate equity-snapshot append+cap logic in `exits.py` — added `BotState.append_equity_snapshot(value)` helper (mirrors F56's `append_slippage_bps` pattern), routed both exit paths through it. The cap-at-500 and `{time, value}` shape now live in one place. (F122 bundle) [easy] [arch]
- [ ] <a id="f63"></a> **F63** `bot_manager.py` (603 lines) and `backtest.py` (937 lines) exceed the 500-line guideline — F21 (`bot_runner.py` split) was a prior TODO; status of those splits unclear. `backtest.py` is nearly 2x the soft cap and has no split planned. Re-evaluate whether the splits would meaningfully reduce cognitive load or just shuffle deckchairs. [hard] (from architectural audit 2026-05-10) [arch]
- [ ] <a id="f71"></a> **F71** Extract `atomic_write_text(path, content)` helper — the `NamedTemporaryFile + os.replace + cleanup` pattern is now duplicated across four sites: `journal.py:195`, `bot_manager.py:561`, `routes/trading.py` (PR #29), `routes/providers.py` (PR #29). Two divergences already exist: `journal.py` and `bot_manager.py` call `os.unlink(fd.name)` bare in the cleanup path while the new sites wrap in `try/except OSError: pass`; `bot_manager.py` uses bare `except:` while the others use `except Exception:`. Extract one helper in (probably) `backend/fileutil.py`, replace all four sites, harmonize the cleanup. Closes the F57 bare-except finding too. [medium] (from PR #29 maintainability+project-standards review; supersedes parts of F57) [arch]
- [x] <a id="f81"></a> **F81** Shared `normalize_symbol` helper + `SymbolField = Annotated[str, BeforeValidator(normalize_symbol)]` in `models.py`. Strip + upper + reject empty, reject >20 chars, regex `^[A-Z0-9][A-Z0-9.\-]{0,19}$`. Wired into `routes/quote.py` (path-param + per-symbol), `routes/trading.py` (`WatchlistRequest`, `BuyRequest`, `SellRequest`). `ScanRequest`/`StrategyRequest`/`BotConfig`/quick-backtest models still pending under F95. (Build 23) [arch]
- [ ] <a id="f96"></a> **F96** [medium] `get_quotes` calls `get_quote` per symbol, which re-validates `source` via a second `get_available_providers()` lookup → O(N) redundant calls + an inconsistent error shape if a provider is ever deregistered mid-batch (top-level 400 vs per-symbol error row). Refactor: extract a private `_fetch_quote(ticker, source)` helper that skips the HTTP-boundary checks; `get_quote` and `get_quotes` both call it. Removes the dead inner check and gives a single error contract. (from PR #31 build 23 kieran-python + reliability + adversarial — 3 reviewers agreed) [arch]
- [ ] <a id="f98"></a> **F98** [medium] Structured error contract for `SymbolField` rejections — currently `normalize_symbol` in `backend/models.py:31,33` raises `ValueError(f"invalid symbol characters: {s!r}")` and `f"symbol too long (max 20 chars): {s[:20]!r}..."`, echoing the raw user input (including non-ASCII confusables that survive `.strip().upper()`) into Pydantic's 422 `detail.msg`. `routes/quote.py:25` separately collapses to opaque `"Invalid ticker symbol"`, leaving agent clients with no way to distinguish "too long" from "bad characters" without probing. Two reviewers had competing requests (adversarial wants LESS info, agent-native wants MORE structure). Resolve via a structured detail across all SymbolField surfaces: `detail={"error": "invalid_symbol", "reason": "must match ^[A-Z0-9][A-Z0-9.-]{0,19}$"}` (machine-readable, no input echo). Standardize across `quote.py`, `trading.py` Watchlist/Buy/Sell, and the per-row error in `get_quotes`. (from PR #31 morning ce:review adversarial 0.82 + agent-native 0.92) [arch]
- [x] <a id="f99"></a> **F99** `SymbolField` now `Annotated[str, BeforeValidator(normalize_symbol), Field(min_length=1, max_length=20, examples=["AAPL","BRK.B","SPY"], json_schema_extra={"pattern": r"^[A-Z0-9][A-Z0-9.\-]{0,19}$"})]`. JSON schema now exposes pattern + min/max + examples (was just `{"type": "string"}`). Empirically verified Pydantic v2 ordering: `BeforeValidator` runs first, then `Field` constraints — lowercase/whitespace inputs still normalize-then-validate (zero behavior regression). Key gotcha discovered: `Field(pattern=...)` does NOT surface in JSON schema when preceded by `BeforeValidator`; the pattern must be injected via `json_schema_extra` instead. 85/85 pass across `test_models.py + test_trading.py + test_backtest_quick.py`. Closes the F95+F100+F145 SymbolField arc. [easy] [arch]
- [ ] <a id="f113"></a> **F113** [easy] Quick-backtest endpoints have no `source` field — `_run_quick` always calls `_fetch` without specifying source, so the endpoints are implicitly yahoo-only. If multi-provider support is desired, add `source: str = "yahoo"` to `QuickBacktestRequest`/`BatchQuickBacktestRequest` and call `source = require_valid_source(req.source)` at the top of `_run_quick`. If yahoo-only is intentional, add a docstring note. (from build 24 round-2 correctness review) [arch]
- [ ] <a id="f117"></a> **F117** [easy] Status-code split between `require_valid_source` (400) and Pydantic field errors (422) for structurally-equivalent invalid-input conditions. Agent clients with retry-on-422 logic silently won't retry on bad-source errors. Two options: (a) raise `HTTPException(status_code=422, detail=[{"loc": ["body", "source"], "msg": "Invalid source", "type": "value_error"}])` from `require_valid_source` to match Pydantic's envelope — breaks any caller catching 400; (b) add `responses={400: {"description": "Invalid source"}}` to the 5 affected route decorators (`data.py:get_ohlcv`, `indicators.py:post_indicators`, `backtest.py:run_backtest`, `quote.py:get_quote`, `quote.py:get_quotes`) so OpenAPI documents the split. Pick one and apply uniformly. (from PR #32 morning ce:review agent-native, 0.95) [arch]
- [x] <a id="f118"></a> **F118** Post-PR-32 protocol patch bundle — three coordinated edits landed: (1) per-run task cap raised from 3 → 5 with "file-independent, ceiling-not-target" framing in `docs/overnight-builder-prompt-patch.md` §Guard Rails; (2) postmortem template gained a `## Run metrics` section (Shipped / Surfaced / Multiplier / Cumulative open F-items / Reviewer roster) so the new-items-per-build multiplier is visible over time; (3) morning ce:review roster trimmed from 4 personas to 2 (kept agent-native + reliability; dropped adversarial + security) in CLAUDE.md, with PR #32 evidence cited inline. Memory files updated to match. [arch]
- [x] <a id="f136"></a> **F136** Severity-graded review tiers — overnight builder review depth now scales with risk (Tier A = 0 personas for `[easy]` bundles <100 lines; Tier B = 2 personas for medium; Tier C = 4-6 panel for hard/architectural/contract changes). Contract-surface and aggregate-bundle overrides promote tier. Morning calibration stays at 2 personas regardless. Validated against F119+F122+F125 (24 items, 0 personas, 0 regressions, user-confirmed "smoother than ever"). Shipped in commit 23c69f9 — originally tagged F127 in commit message, then renumbered to F136 here after PR #33 (build 25) merged with a colliding F127 for batch deadline. CLAUDE.md and `docs/overnight-builder-prompt-patch.md` updated. (commit 23c69f9; resolved 2026-05-11) [arch]
- [ ] <a id="f153"></a> **F153** Central `requestSignature(req)` helper in `frontend/src/shared/types` (or similar) consumed by both the `BACKTEST_CACHE_KEY` writer (`App.tsx:117-125`) and the F151 restore predicate (`App.tsx:_cachedBacktest`). Today the field-pair comparator list is maintained by hand and silently drifts if a new request field is added without updating the predicate (long_buy_logic etc. would slip through). Defer until the next backtest request field is added — not urgent. [easy] [arch] (added 2026-05-12)
- [ ] <a id="f158"></a> **F158** Type `is_metrics` / `oos_metrics` / `stitched_equity` properly in `backend/routes/walk_forward.py` instead of bare `dict` / `list[dict]`. Define `class BacktestSummary(BaseModel)` with the known summary fields (`num_trades: int`, `sharpe_ratio: float`, `total_return_pct: float`, `win_rate_pct: float`, `max_drawdown_pct: float`, `final_value: float`) and `class WfaEquityPoint(BaseModel): time: str | int; value: float`. Update `WindowResult.is_metrics: BacktestSummary`, `oos_metrics: BacktestSummary`, `WalkForwardResponse.stitched_equity: list[WfaEquityPoint]`. Mirror updates in `WalkForwardPanel.tsx` interface. Closes C28 review findings K2 + K11 + F-WFA-04 (multiple reviewers converged on this — bare `dict` removes the OpenAPI contract and disables static analysis at access sites like `w.oos_metrics.get("num_trades", 0)`). [easy] [arch] (from C28 review; added 2026-05-12)

### F · Hardening
- [x] <a id="f27"></a> **F27** Concurrent `_fetch()` dedup — added `threading.Lock` per `(symbol, interval)` in `shared.py`. Lock wraps the entire check→fetch→populate block; second concurrent caller blocks then hits the freshly-populated cache. Meta-lock protects the lock-dict from concurrent key creation. `threading` was already imported. [hardening]
- [x] <a id="f28"></a> **F28** Backend input validation hardening — `Field` constraints + `@field_validator` across request models: `QuickBacktestRequest`/`BatchQuickBacktestRequest` (gt=0 capital/lookback, ge=0 stop_loss, direction enum, ticker strip+length), `SetFundRequest` (ge=0), `UpdateBotRequest` (ge=0 on spread/drawdown/borrow, direction enum, gt=0 capital), `BuyRequest` (gt=0 qty, ge=0 le=100 stop_loss_pct), `SellRequest` (gt=0 qty), `get_quote` (empty/oversized ticker guard). [easy] [hardening]
- [x] <a id="f28b"></a> **F28b** Validate `buy_logic`/`sell_logic` fields — `eval_rules()` silently falls back to OR semantics for any non-`"AND"` string (e.g. `"all"`, `"BOTH"`). Add `@field_validator` on `QuickBacktestRequest`, `BatchQuickBacktestRequest`, and `BotConfig` restricting to `"AND" | "OR"`. Asymmetric gap: `direction` was hardened in F28 but the adjacent logic fields were not. [easy] [hardening]
- [x] <a id="f28c"></a> **F28c** Fix `cache_info()` 6-tuple unpack crash — `GET /api/cache` unpacks `_fetch_cache` keys as 5-tuples but keys are 6-tuples (includes `extended_hours`). Every call raises `ValueError`. Pre-existing but worsened by F26 making cache diagnostics more important. [easy] [hardening]
- [x] <a id="f28d"></a> **F28d** `StrategyRequest.direction` missing validator — added `@field_validator('direction')` to `StrategyRequest` in `models.py` restricting to `"long" | "short"`. Matches pattern in `QuickBacktestRequest` / `BatchQuickBacktestRequest`. [easy] [hardening]
- [x] <a id="f29"></a> **F29** Watchlist quote endpoint ticker validation — `GET /api/quote/{ticker}` now validates tickers in `get_quote()`, but the batch `POST /api/quotes` iterates `symbols[:20]` without validating individual entries. Add per-symbol validation (strip, uppercase, length guard) to avoid passing garbage to `_fetch()`. [easy] [hardening]
- [x] <a id="f31"></a> **F31** `eval_rules()` defense-in-depth — added `Literal['AND', 'OR']` type annotation to the `logic` parameter in `eval_rules()` in `signal_engine.py`. Added `Literal` to the `from typing` import. [easy] [hardening]
- [x] <a id="f32"></a> **F32** `detail?.state.last_tick` unsafe chain in BotCard.tsx — changed all 8 occurrences of `detail?.state.` to `detail?.state?.` (last_tick ×2, equity_snapshots ×4, activity_log ×2, pause_reason ×2). [easy] [hardening]
- [x] <a id="f37"></a> **F37** `source` param allowlist on quote endpoints — both `GET /api/quote/{ticker}` and `POST /api/quotes` now reject unknown `source` via `if source not in get_available_providers(): raise HTTPException(400, "Invalid source")` before any fetch. Closes the silent-swallow provider-enumeration vector. (Build 23 — partial: backtest / indicators / ohlcv routes tracked under F94.) [hardening]
- [x] <a id="f38"></a> **F38** Log-injection via ticker on quote routes closed — both routes now run input through `normalize_symbol()` which enforces `^[A-Z0-9][A-Z0-9.\-]{0,19}$`, rejecting newlines/control chars before they reach the f-string detail or any log path. (Build 23 — backtest/bot ticker fields still plain `str`, tracked under F95.) [hardening]
- [x] <a id="f39"></a> **F39** Batch quote endpoint silent null — added `error: str | None` to per-symbol response. Validation rejection uses `"invalid symbol"`; HTTPException path uses `e.detail` (avoids leaking `"500: …"` status prefix); generic Exception path uses `str(e).strip() or "no data"`. Frontend `Quote` interface gained optional `error` field, surfaced as `title` tooltip on the loading placeholder. (PR #29) [hardening]
- [x] <a id="f42"></a> **F42** `eval_rules()` Literal not runtime-enforced — added `if logic not in ('AND', 'OR'): raise ValueError(...)` guard at function entry. Invalid logic now surfaces at runtime instead of silently falling back to OR semantics. (F119 bundle) [easy] [hardening]
- [x] <a id="f43"></a> **F43** Log injection via ticker symbols — fully closed by F95. Final audit (F95 security review SEC-02, 0.75): every `_log_trade()` call site now reads a SymbolField-validated source: `BuyRequest.symbol` / `SellRequest.symbol` (F85), `scan_signals` symbol var (F95 via `ScanRequest.symbols: list[SymbolField]`), `bot_runner` `cfg.symbol` (F95 via `BotConfig.symbol`). `QuickBacktestRequest.ticker` + `BatchQuickBacktestRequest.symbols` are SymbolField but never write to the journal — the original F43 entry overcounted those as live holes. [easy] [hardening]
- [x] <a id="f45"></a> **F45** `POST /api/quotes` invalid-entry symbol now truncated to 20 chars and stripped of anything outside `[A-Z0-9.-]` before echoing back, so multi-MB strings + control chars can't ride into the response. (Build 23 side-effect close) [hardening]
- [ ] <a id="f49"></a> **F49** `useInstanceIndicators` swallows HTF query errors — `Object.assign(merged, q.data)` silently skips when `q.data` is `undefined` (error state). User sees a chart missing an indicator with no explanation. Add `isError`/`errorMessage` to the hook's return and surface in `SubPane.tsx` (e.g. red "Failed to load" overlay, mirroring the loading overlay). Pattern is pre-existing but exposed more visibly by A14a — now that loading state is shown, the asymmetry of "loading state visible / error state silent" is more jarring. [medium] (from PR #28 reliability review) [hardening]
- [x] <a id="f52"></a> **F52** `watchlist.json` write is not atomic — `save_watchlist` now uses `tempfile.NamedTemporaryFile` + `os.replace()` with cleanup-on-exception, mirroring the `bot_manager.save()` pattern. (PR #29) [hardening]
- [x] <a id="f53"></a> **F53** `.env` write in providers route is not atomic — `_persist_env` now uses `tempfile.NamedTemporaryFile` + `os.replace()` with cleanup-on-exception. Module-level `os`/`pathlib`/`tempfile` imports replaced the prior function-local imports. (PR #29) [hardening]
- [x] <a id="f54"></a> **F54** `/scan` auto-execute `_log_trade` calls have no `direction` kwarg — both call sites in `routes/trading.py` now pass explicit `direction="long"`. `ScanRequest` has no `direction` field (scan is long-only today); making the default intentional documents the assumption and prevents silent "long" logs when scan eventually grows short support. (F119 bundle) [easy] [hardening]
- [ ] <a id="f55"></a> **F55** `bot_manager.save()` is sync but called from async bot tick context without executor wrapping — `bot_runner.py:508, 531, 536` call `self.manager.save()` directly inside `async def run()`, blocking the event loop on disk I/O. With many bots, this delays the tick loop by disk write latency. Wrap in `await asyncio.get_event_loop().run_in_executor(None, self.manager.save)` in async paths. [easy] (from architectural audit 2026-05-10) [hardening]
- [x] <a id="f56"></a> **F56** `BotState.slippage_bps` list is unbounded — added `BotState.append_slippage_bps(bps)` helper that appends + caps at 1000 entries. Replaced 4 raw `.append()` sites (`bot_manager.py`, `bot_runner.py`, `exits.py`, `regime.py`) with the helper so the cap can't drift across exit paths. (F119 bundle) [easy] [hardening]
- [x] <a id="f57"></a> **F57** `bot_manager.save()` uses bare `except:` — narrowed to `except Exception:` so `KeyboardInterrupt` / `SystemExit` propagate as intended. (F119 bundle) [easy] [hardening]
- [ ] <a id="f59"></a> **F59** `_log_trade` failures in `exits.py` silently downgrade to activity-log only — lines 118 and 395 catch journal write errors and log to activity log but don't surface to bot state, UI, or notifications. A persistent journal failure (disk full, perms) would be invisible. Set `state.error_message` or `asyncio.create_task(notify_error(...))`. [medium] (from architectural audit 2026-05-10) [hardening]
- [x] <a id="f60"></a> **F60** `routes/indicators.py:112` swallows compute errors per-indicator with no logging — added module logger and `logger.exception(...)` calls at both per-instance compute-failed branches (htf path + main path). Server logs now distinguish pandas shape mismatch from missing-data. (F119 bundle) [easy] [hardening]
- [ ] <a id="f62"></a> **F62** Journal read from disk on every PnL call — `journal.py:18-22` `_load_trades()` reads and JSON-parses the entire file on every call. Called from `bot_runner._tick()` indirectly via `_bot_pnl()` → `compute_realized_pnl()`. At ~5s poll × N bots × growing journal, this is N reads/parses per minute. Add `_journal_cache` invalidated on write (already has a write lock). [medium] (from architectural audit 2026-05-10) [hardening]
- [x] <a id="f64"></a> **F64** `BotControlCenter.tsx` reorder failure now surfaces — the `.catch(() => {})` on `reorderBots(newOrder)` now feeds `setError(apiErrorDetail(e, 'Failed to reorder bots'))` and forces `invalidateBots()` so the optimistic cache update gets corrected back to the server's order. The existing error banner in the toolbar renders the message. (build 27) [easy] [hardening]
- [x] <a id="f65"></a> **F65** SignalScanner save-watchlist failure surfacing — closed as subsumed by F135 (build 26). F135's `saveError` state + the red `<span>` under the Save button is exactly the inline-feedback contract F65 specified; the architectural-audit entry pre-dated F135 and was never crossed off. [easy] [hardening]
- [x] <a id="f66"></a> **F66** `_fetch_dedup_locks` key shape inconsistent with `_fetch_cache` key — updated to `(ticker.upper(), interval, source, extended_hours)`. Chose the stable-subset shape (not full cache key with start/end) deliberately to keep the dict bounded by meaningful discriminators while still preventing different-source callers from over-serializing on the same lock. (F122 bundle) [easy] [hardening]
- [x] <a id="f67"></a> **F67** Broker quote fetch errors swallowed silently in `routes/trading.py` — added `logger.exception(...)` at 6 sites: `/buy` `get_latest_price` failure, `_clear_bot_entry_state`, `/sell` pre-cancel stop-loss, scan auto-execute SELL_FAILED branch, scan per-symbol error fallback, and `/performance` backtest fallback. Skipped 3 intentional broad catches (polling break, atomic-write cleanup × 2). (F122 bundle) [easy] [hardening]
- [x] <a id="f69"></a> **F69** `WatchlistRequest.symbols` unbounded (disk-fill / OOM DoS) — `Field(max_length=500)` plus a `field_validator` that explicit-checks `len(v) > 500` first (belt-and-suspenders against any Pydantic version drift), strips whitespace, drops empties, enforces 20-char per-symbol cap, and uppercases. Boundary tests at 500 + 21 + 20 chars added. (PR #30 build 22) [hardening]
- [x] <a id="f70"></a> **F70** `_persist_env` lost-update race closed — module-level `_env_lock = threading.Lock()` wraps the entire read-modify-write block in `routes/providers.py`. Concurrent in-process callers for different keys now serialize cleanly. Cross-process writers (multiple workers, hand-edits) remain unprotected — documented in the function docstring. (Build 23) [hardening]
- [x] <a id="f72"></a> **F72** `POST /api/quotes` lacks Pydantic `response_model` — added `QuoteResult(BaseModel)` with `symbol/price/change_pct/error` (None defaults) and annotated the route with `response_model=list[QuoteResult]`. OpenAPI now documents the shape. Test contract updated: success rows now include `error: None` (the 4-field shape is uniform across success and error paths). (F125 bundle) [easy] [hardening]
- [x] <a id="f73"></a> **F73** Orphan `.tmp` cleanup on startup — new `backend/fileutil.py` with `cleanup_orphan_tmps(directories: list[Path]) -> int`. Wired into the FastAPI lifespan before `init_ibkr()`. Scans `data/` and `backend/` (covers all four atomic-write sites: journal/bot_manager/watchlist/providers env), unlinks `*.tmp` >1h old. Best-effort, OS errors logged at WARNING. (F125 bundle) [easy] [hardening]
- [ ] <a id="f74"></a> **F74** Atomic writes lack `fsync` before `os.replace` — `fd.close()` flushes Python's buffer to the kernel but does not force the kernel page cache to disk. A power failure between `os.replace` and writeback can leave a zero-byte or partial destination file. Add `fd.flush(); os.fsync(fd.fileno())` before `fd.close()` in all four atomic-write sites (best added together with F71). [medium] (from PR #29 adversarial review) [hardening]
- [x] <a id="f75"></a> **F75** Sanitize `/api/quotes` exception leak — `get_quote` 500 detail is now a fixed `"quote fetch failed"` with `logger.exception()`; `get_quotes` batch error rows classify by inner HTTPException status: 404 passes through the safe `"No data for X"` message, anything else (500+) maps to fixed `"fetch failed"`. 2 stale tests (`test_fetch_exception_returns_error_field`, `test_fetch_empty_exception_message_falls_back`) updated to assert the new sanitized contract. (F125 bundle) [easy] [hardening]
- [x] <a id="f76"></a> **F76** `_persist_env` TOCTOU on `.env` existence check closed — replaced `if exists(): read_text()` with `try: read_text() except FileNotFoundError`. Also tracks `existed: bool` so the subsequent `shutil.copymode` only fires when the source genuinely existed at read time (avoids conflating "first-time write" with "external chmod fail"). (Build 23) [hardening]
- [x] <a id="f77"></a> **F77** Defensive newline guard for `_persist_env(key, value)` — added `if "\n" in key/value or "\r" in key/value: raise ValueError(...)` at function entry. Closes the env-injection vector if a future caller forwards user data unsanitized. (F119 bundle) [easy] [hardening]
- [x] <a id="f82"></a> **F82** `ScanRequest.symbols` validation closed — F95 changed `list[str]` to `list[SymbolField]` (per-element normalize + regex via BeforeValidator) and the F95 fixer added F93's dedup pattern (`@field_validator('symbols')` returning `list(dict.fromkeys(v))`). Per-symbol cap is enforced by SymbolField's 20-char regex; list-length cap deferred (not in F82's original ask; ScanRequest's amplification path is dedup-bounded now). [easy] [hardening]
- [ ] <a id="f83"></a> **F83** `tempfile + os.replace` cleanup leaks `.tmp` if `fd.close()` itself raises in the except branch — wrapped at `routes/trading.py:save_watchlist` and `routes/providers.py:_persist_env` in PR #30 (4-line `try: fd.close() except Exception: pass`), but the same pattern in `journal.py:195` and `bot_manager.py:561` still uses unwrapped `fd.close()`. Latent on local disk; observable on NFS / disk-full where the kernel defers I/O errors to `close()`. Should be folded into F71's `atomic_write_text(path, content)` helper rather than fixed in two more places. (from PR #30 adversarial review) **Elevated priority (reliability review on PR #30):** journal.py is the trade ledger; partial/corrupt writes cause incorrect P&L accounting across the entire bot system. Should be accelerated, not deferred behind F71. [hardening]
- [x] <a id="f85"></a> **F85** Symbol allowlist regex closed — `^[A-Z0-9][A-Z0-9.\-]{0,19}$` enforced inside `normalize_symbol` so every site using `SymbolField` (or calling the helper directly) inherits it. Tightened over the original spec to require an alphanumeric leading char so `..`, `.env`, `-A` are also rejected. (Build 23) [hardening]
- [x] <a id="f86"></a> **F86** HTTP body size limit middleware — new `backend/middleware.py` `BodySizeLimitMiddleware` (pure ASGI). 1 MB default cap, override via `STRATEGYLAB_MAX_BODY_BYTES`. Wired into `main.py` after CORS so it's outermost. Adversarial-hardened in-PR: rejects duplicate Content-Length headers and forces slow-path body counting when Transfer-Encoding is present (closes CL=0+chunked and TE+CL coexistence smuggling). Replay-receive returns synthetic empty body on second call so downstream re-reads don't hang. (PR #32 build 24) [hardening]
- [x] <a id="f87"></a> **F87** Empty-list watchlist silently wipes saved state — closed by F104. `_validate_symbols` now raises `ValueError("symbols must contain at least one non-empty entry")` so POST `{"symbols": []}` / `{"symbols": ["", "  "]}` returns 422 instead of silently overwriting the on-disk file. Test `test_watchlist_rejects_all_empty_symbols` pins the contract. (Build 25) [hardening]
- [x] <a id="f88"></a> **F88** GET `/api/trading/watchlist` filter-on-read — disk entries now run through the same `normalize_symbol()` + regex used by writes, but in forgiving mode: invalid/empty/non-string entries are dropped with a single `logger.warning("watchlist read-time filter dropped %d invalid entries", N)`. Dedup via `dict.fromkeys` mirrors F93. Closes the "reads OK, writes 422" trap surfaced by F95's SymbolField rollout. Write-strict / read-tolerant asymmetry preserved (no cap on reads). (F125 bundle) [easy] [hardening]
- [x] <a id="f91"></a> **F91** BatchQuickBacktestRequest.symbols length cap parity — applied the F69 pattern to `BatchQuickBacktestRequest.symbols`: 500-entry list cap (custom message) + per-element `normalize_symbol` validation, all-empty list rejected with 422. `QuickBacktestRequest.ticker` switched to `SymbolField`, removing the duplicate inline `validate_ticker`. Direction validators on both classes also collapsed to the shared `DirectionField` (kieran-python review converged on the same idiom). Closes the OOM vector on POST /api/backtest/quick/batch. (PR #32 build 24) [hardening]
- [x] <a id="f92"></a> **F92** Symlink-safe `_persist_env` — call `env_path = env_path.resolve()` at function entry so `os.replace` lands on the real file, not the symlink. (F119 bundle) [easy] [hardening]
- [x] <a id="f93"></a> **F93** Symbol dedup in WatchlistRequest validator — `_validate_symbols` now returns `list(dict.fromkeys(cleaned))` to drop duplicates while preserving first-occurrence order. Closes the 500x scanner amplification vector. (F119 bundle) [easy] [hardening]
- [x] <a id="f94"></a> **F94** F37-style `source` allowlist on the remaining data routes — extracted shared `require_valid_source(source)` helper in `shared.py` (lowercases + 400s on unknown), refactored `routes/data.py:get_ohlcv`, `routes/indicators.py:post_indicators`, `routes/backtest.py:run_backtest`, and both quote routes (`get_quote` / `get_quotes`) to use it. Closes the case-sensitivity gap on the boundary check (`source=YAHOO` now normalizes to `yahoo` instead of falling through to `_fetch`'s separate 'Unknown data source' branch). 5 sites → 1 helper. (PR #32 build 24) [hardening]
- [x] <a id="f95"></a> **F95** SymbolField applied to remaining ticker/symbol fields — `StrategyRequest.ticker` (`models.py`), `BotConfig.symbol` (`bot_manager.py`), `ScanRequest.symbols: list[SymbolField]` + dedup `@field_validator` (`routes/trading.py`), `PerformanceRequest.symbol` (`routes/trading.py`). Closes F43 + subsumes F82. Tier C full orchestrator: 6 personas → cross-converged P1 (COR-001: `migrate_rule` was outside the try, would have aborted all bots on one bad rule entry) + cross-converged P2 (K1+COR-002: BotConfig ValueError mislabeled as "invalid symbol" — split into normalize-then-construct named-control-flow) + SEC-01 (bot_id %s → %r to prevent log-line injection) + M2 (4 dead `.upper()` calls on SymbolField values removed). [medium] [hardening]
- [x] <a id="f100"></a> **F100** `BotManager.load()` migration for F95 — `normalize_symbol(raw_symbol)` pre-pass + named-control-flow try-split: (a) first try wraps normalize + migrate_rule loop, catches `ValueError` (invalid symbol surface) → WARNING + continue; (b) second try wraps `BotConfig(**cfg_dict)`, catches `pydantic.ValidationError` (any other field rejection) → distinct WARNING + continue; broad `except Exception` retained for unexpected errors with `logger.exception`. All `bot_id` logging via `%r` (newline-safe). 14 new tests in `test_bot_state.py` `TestBotManagerLoadMigration` cover normalize, skip-invalid, mixed entries, missing-symbol-key, None-symbol, and the broad-except fallback. [medium] [hardening]
- [x] <a id="f101"></a> **F101** `.env` permission preservation hardening in `_persist_env` — now tracks `copy_mode_failed` and explicitly `os.chmod(env_path, 0o600)` after `os.replace` whenever (a) the file is new or (b) `copymode` failed. Closes the umask-default-after-failure leak; preserves existing modes on the success path. (F122 bundle) [easy] [hardening]
- [x] <a id="f102"></a> **F102** Cap `buy_rules` / `sell_rules` list length on `QuickBacktestRequest` and `BatchQuickBacktestRequest` — `Field(max_length=100)` applied to both lists on both request models in `backend/routes/backtest_quick.py`. Bounds O(n_rules × n_bars) compute per backtest below the body-cap (F86) ceiling. 6 new tests in `test_backtest_quick.py` covering reject-101 (buy/sell × quick/batch) and accept-exactly-100 (quick + batch boundary). (Build 25) [hardening]
- [x] <a id="f103"></a> **F103** `trailing_stop: Optional[dict]` on `QuickBacktestRequest` / `BatchQuickBacktestRequest` → `Optional[TrailingStopConfig]`. Validation now happens at request-entry instead of being deferred to the downstream `StrategyRequest` re-validation. (F122 bundle) [easy] [hardening]
- [x] <a id="f104"></a> **F104** Harmonize empty-list-after-strip between `WatchlistRequest._validate_symbols` (F69, silently returned `[]`) and `BatchQuickBacktestRequest._validate_symbols` (F91, raises 422). Watchlist validator now raises 422 with the same "symbols must contain at least one non-empty entry" message. Side-effect: closes F87 silent-wipe of watchlist.json. Parametrized test pins 4 empty-list shapes ([], [""], ["   "], ["", "  ", "\t"]). (Build 25) [hardening]
- [ ] <a id="f105"></a> **F105** [easy] Replace free-form `interval: str` with `Literal[...]` on `QuickBacktestRequest`, `BatchQuickBacktestRequest`, `StrategyRequest`, `IndicatorsPostRequest`, and the `get_ohlcv` query param. Currently any string reaches yfinance/Alpaca directly. (from build 24 security review, 0.75) [hardening]
- [ ] <a id="f106"></a> **F106** [medium] Allowlist `Rule.indicator` and `Rule.condition` in `signal_engine.py`. Free-form strings reach the rule engine's dispatch logic; amplified by the now-uncapped rule lists pending under F102. Replace with `Literal[...]` or a `field_validator` that checks against the registered indicator/condition tables. (from build 24 security review, 0.65) [hardening]
- [x] <a id="f111"></a> **F111** `BodySizeLimitMiddleware` rejects `Content-Length` with leading/trailing whitespace — added a pre-check before the duplicate-CL guard: `for v in cl_values: if v.strip() != v: return 400`. Closes the `b' 50'` / `b'50 '` smuggling polish. (F125 bundle) [easy] [hardening]
- [x] <a id="f112"></a> **F112** `Transfer-Encoding: identity` no longer forces slow path — TE detection now compares `value.strip().lower() != b"identity"` before setting `has_transfer_encoding = True`. Identity is the no-op encoding (RFC 7230 §4); the Content-Length fast path stays active. (F125 bundle) [easy] [hardening]
- [x] <a id="f114"></a> **F114** `MonteCarloRequest.pnls` length cap — added `Field(min_length=2, max_length=50000)` on `pnls`. Pins the existing runtime `< 2` guard at the schema level and caps the inner-loop work at 50k × 5000 sim = 250M iterations (vs unbounded). (F122 bundle) [easy] [hardening]
- [x] <a id="f115"></a> **F115** Provider exception strings sanitized across 5 routes — `data.py`, `indicators.py`, `backtest.py`, `quote.py` (covered by F75), and per-symbol rows in `backtest_quick.py`. Each replaces `detail=str(e)` / `error=str(e)` with a fixed message (`"data fetch failed"` / `"indicator compute failed"` / `"backtest failed"` / `"quick-backtest failed"`) and adds `logger.exception(...)` server-side. Loggers added where missing. F126 filed for the remaining leak vector in `routes/trading.py:84` (broker errors). (F125 bundle) [easy] [hardening]
- [x] <a id="f124"></a> **F124** `_fetch_dedup_locks` eviction policy — `_evict_cache` in `shared.py` now also drops dedup-lock entries whose `(ticker, interval, source, extended_hours)` key has no remaining cache entry. The cleanup runs under `_fetch_dedup_locks_meta`, after cache mutations. Bounds growth without forcing a new periodic timer. [easy] [hardening]
- [x] <a id="f126"></a> **F126** `GET /api/trading/account` exception leak closed — replaced `detail=f"Broker API error: {e}"` with fixed `"Broker API unavailable"` and upgraded the existing `logger.error(...)` to `logger.exception("get_account failed")` so the full trace stays server-side. Parity with F115 across the remaining trading route. [easy] [hardening]
- [ ] <a id="f127"></a> **F127** [next] [medium] Batch quick-backtest endpoint has no request-level deadline — `BatchQuickBacktestRequest` admits 500 symbols × 100 rules synchronously through `_run_quick` per symbol; even with F102 in place, the worst-case wall clock at indicator-compute cost × 500 is unbounded by any timeout. **Fix sketch corrected (build 25 morning reliability calibration, 0.82):** `quick_backtest_batch` at `backend/routes/backtest_quick.py:217` is `def`, not `async def` — FastAPI runs it in a threadpool worker that has no event loop. Calling `asyncio.wait_for(...)` from that thread raises `RuntimeError: no running event loop` (the same class of bug documented in CLAUDE.md "Key Bugs Fixed → Sync callbacks need run_coroutine_threadsafe"). Real fix options: (a) `concurrent.futures` + `Future.result(timeout=30)`-style timeout around the sync per-symbol loop; (b) convert the route to `async def` and dispatch `_run_quick` via `run_in_executor`, then wrap with `asyncio.wait_for` — note `_run_quick` calls blocking `_fetch()` so the executor dispatch is required regardless; (c) middleware-level request deadline which avoids the per-route problem entirely (architecturally preferable). Architectural decision: route-level vs middleware. Return partial results with `error` rows on remaining symbols regardless of approach. (from build 25 adversarial P1, 0.88 — deferred from F102 scope; fix sketch corrected by build 25 morning reliability calibration) [hardening]
- [x] <a id="f128"></a> **F128** Rule-list caps applied to 17 sibling fields via shared type alias — introduced `BoundedRuleList = Annotated[list[Rule], Field(max_length=_RULE_LIST_CAP)]` and `OptionalBoundedRuleList = Annotated[Optional[list[Rule]], Field(default=None, max_length=_RULE_LIST_CAP)]` in `models.py` (constant `_RULE_LIST_CAP = 100`). Replaced 17 field declarations: `StrategyRequest` (6: 2 BoundedRuleList + 4 OptionalBoundedRuleList), `BotConfig` (6: same shape), `ScanRequest` (2 BoundedRuleList), `PerformanceRequest` (2 BoundedRuleList). `RegimeConfig.rules` keeps its smaller cap=50 (different value, intentionally not aliased). Required fields stay required — F102's no-default contract preserved (correctness + kieran reviewers caught the early draft's `default_factory=list` regression). 46 new boundary tests across `test_models.py`/`test_bot_state.py`/`test_trading.py` assert `accept-100 → len + first/last element` and `reject-101 → ValidationError(match="too_long")`. (build 26 orchestrator workflow: 3 parallel sonnet implementers + 2 parallel sonnet reviewers + 1 sonnet fixer + Opus orchestrator) [medium] [hardening]
- [x] <a id="f129"></a> **F129** `Rule.value` lookback DoS closed via consumer-site clamp — new `_clamp_lookback(value, default)` helper in `signal_engine.py` returns `max(0, min(int(value), 500))` (caps at 500 AND floors at 0 — review-flagged that negative values would silently OOB into pandas). Applied at the 3 lookback consumer sites (`rising_over`, `falling_over`, `turns_up`/`turns_down` with `max(1, ...)` outer); threshold sites stay unclamped (Rule.value is also used as a price/indicator threshold where high values are legitimate — BRK.A is ~$750k). Option B over Option A per explore: `Field(le=500)` on Rule.value would break threshold rules on high-priced assets. [medium] [hardening]
- [x] <a id="f130"></a> **F130** Per-family indicator-spec cap added to `compute_indicators` — new `_INDICATOR_FAMILY_CAP = 20` constant + `_assert_family_cap(name, specs)` helper raise `ValueError(...)` when distinct-spec count exceeds the cap. Applied to all 7 families: RSI, MA, BB, ATR, volume SMA, stochastic, ADX. Per-family scope (not global): 10 MA + 10 RSI + 10 BB (30 total) still passes; 21 of any single family fails. Both `routes/backtest.py` and `routes/backtest_quick.py` now catch `ValueError` and surface as 400 (was 500 "backtest failed" pre-fix per cross-reviewer P1). 11 new tests in `test_signal_engine.py` cover the helper + 7 families + boundary + negative-input floor. [medium] [hardening]
- [x] <a id="f131"></a> **F131** Single-coroutine-per-bot invariant documented — `# single-coroutine-per-bot: do not insert await between append and slice-cap.` added to `BotState.append_slippage_bps` and `append_equity_snapshot` in `bot_manager.py`. Same comment also applied to the new `append_activity_log` (F132). [easy] [hardening]
- [x] <a id="f132"></a> **F132** `BotState.append_activity_log(entry)` extracted — insert(0)+cap(200) moved from `bot_runner._log` into a new `BotState` helper, with the runner now calling `self.state.append_activity_log(entry)`. Mirrors F121/F123 pattern; closes the last unbounded-growth gap on `BotState`. [easy] [hardening]
- [x] <a id="f133"></a> **F133** `scan_signals` error-path leak closed — per-symbol error row now reports `"error": "scan failed"` instead of `str(e)`; the existing `logger.exception("scan failed for %s", symbol)` preserves the full trace server-side. Final piece of the F115/F126 sanitization sweep across user-facing trading routes. [easy] [hardening]
- [x] <a id="f135"></a> **F135** `SignalScanner.tsx` save-watchlist 422 now surfaced — added `saveError: string | null` state; rewrote `handleSave` to `try { await saveWatchlist(list) } catch (e) { setSaveError(apiErrorDetail(e, 'Failed to save watchlist')) }`; wrapped the Save button in a column-flex `<div>` rendering `<span style={{ color: '#f85149', fontSize: 11 }}>{saveError}</span>` below when set. F104 visibility gap closed. Visual smoke not exercised in-session — see F140. [easy] [hardening]
- [x] <a id="f137"></a> **F137** `shared.py` IBKR `_fetch` exception leak closed — `detail=f"IBKR fetch failed: {e}"` → fixed `"IBKR fetch failed"` + `logger.exception("IBKR fetch failed for %s", ticker)`. Parity with F115/F126/F133 sanitization sweep — `_fetch` was the last remaining `detail=f".*{e}"` site. (build 27) [easy] [hardening]
- [x] <a id="f138"></a> **F138** Dedup-lock high-watermark trigger added — new module-level constant `_DEDUP_LOCKS_HIGH_WATERMARK = 200` in `shared.py`. `_fetch` now fires `_evict_cache()` when EITHER `_fetch_cache` is at cap OR `_fetch_dedup_locks` has grown past the watermark, so low-throughput long-running deployments with rotating tickers don't accumulate locks indefinitely. Picked option (b) over (a) — avoids the per-call eviction scan overhead while still bounding lock growth. New `tests/test_shared_eviction.py` (3 tests) pins orphan-lock pruning + live-lock retention + watermark constant. (build 27) [easy] [hardening]
- [x] <a id="f141"></a> **F141** `UpdateBotRequest` PATCH cap closed — `routes/bots.py:49-58` 6 rule fields swapped from bare `Optional[list]` to F128's `OptionalBoundedRuleList`. Cap now fires at request-validation time (Pydantic field-level, before `model_dump`), not after the BotConfig reconstruct. Also tightens the inner type from `list` to `list[Rule]` for consistency with BotConfig. New `tests/test_routes_bots.py` (14 tests): per-field reject-101 with `match="too_long"`, per-field accept-exactly-100 (asserts len + first/last element survives), None-default preservation, empty-list passthrough. [easy] [hardening]
- [x] <a id="f145"></a> **F145** `SymbolList = list[SymbolField]` extracted in `models.py` and applied to all three sites: `ScanRequest.symbols` (`routes/trading.py`, was `list[SymbolField]`), `WatchlistRequest.symbols` (`routes/trading.py`, was `list[str]` + custom validator), `BatchQuickBacktestRequest.symbols` (`routes/backtest_quick.py`, was `list[str]` + custom validator). Watchlist + batch validators refactored: mode='before' now only drops empty/whitespace entries + holds the custom "too many symbols (max 500, got N)" message; per-element normalize+regex moves to `SymbolField`'s `BeforeValidator(normalize_symbol)`; watchlist dedup hoisted to a `mode='after'` validator since SymbolField runs between mode='before' and mode='after'. `normalize_symbol` import removed from `routes/backtest_quick.py` (no longer used after the refactor). 49/49 tests in `test_trading.py` + `test_backtest_quick.py` pass against the new flow. (build 27) [easy] [hardening]
- [x] <a id="f147"></a> **F147** `routes/bots.py:reorder_bots` wrapped in try/except → `logger.exception("/api/bots/reorder failed")` + `HTTPException(500, detail="reorder failed")`. Closes the F64 backend pair: F64 surfaced reorder failures in the UI (PR #34), F147 now ensures the server side logs the trace and returns a sanitized detail (mirroring the F115/F126/F133/F137 sanitization sweep contract). `import logging` + module-level `logger` added at top of bots.py. 2 new tests in `test_routes_bots.py` cover happy path + sanitized-500 + `caplog` assertion of the `logger.exception` entry. 29/29 pass in `test_routes_bots.py`. [easy] [hardening]
- [x] <a id="f149"></a> **F149** `ScanRequest.symbols = SymbolList = Field(min_length=1, max_length=500)` applied in `routes/trading.py` — closes the residual amplification vector after F82 / F128 by capping `/scan` at 500 symbols (parity with WatchlistRequest + BatchQuickBacktestRequest, both 500). Declarative Field cap mirrors the BatchQuickBacktestRequest pattern; the Watchlist inline-validator pattern is preserved separately for its custom error-message contract. 3 new tests in `test_trading.py` cover the 501-symbol rejection (`too_long`), 500-boundary acceptance, and empty-list rejection (`too_short`). Full `test_trading.py` 36/36 pass; full backend pytest 362/363 (only F139 pre-existing failure remains). [easy] [hardening]
- [x] <a id="f151"></a> **F151** Backtester cache restored stale trades for muted/deleted rules — `frontend/src/App.tsx` `_cachedBacktest` IIFE restored `BACKTEST_CACHE_KEY` (`'strategylab-last-backtest'`) on page mount when only ticker/start/end/interval matched, ignoring rule changes. After muting/deleting a buy rule and refreshing, the chart still showed trades fired by the now-muted rule (user-visible "rules keep running" symptom). Fix: new `loadStrategyCache()` helper reads `'strategylab-strategy'` (the StrategyBuilder snapshot); restore predicate now also compares 12 rule/logic camelCase↔snake_case pairs (`buyRules↔buy_rules`, `sellRules↔sell_rules`, `buyLogic↔buy_logic`, `sellLogic↔sell_logic`, plus long/short variants) and the `regime` block, all via `JSON.stringify` equality. Any mismatch returns `null` so the user sees no result until they click Run Backtest. Backend verified clean separately (`eval_rules` correctly skips `r.muted`; live curl with both sides muted returns `num_trades: 0`). `npm run build` clean. [easy] [hardening]
- [x] <a id="f152"></a> **F152** SignalScanner saved-strategies snapshot stale across tab switches — `frontend/src/features/discovery/SignalScanner.tsx:29` captured `loadStrategies()` once at mount, never refreshed. Tabs use `display: 'none'` (not unmount; `App.tsx:198/369/372`) so the Discovery scanner kept running pre-edit rule snapshots after the user edited a saved strategy in the Chart tab. Same staleness pattern that `AddBotBar.tsx:140` already mitigates via `onFocus={loadStrategies}`. Fix: exposed the `setStrategies` setter and added a `useEffect` that refreshes on both `window` `focus` and `storage` events; cleanup removes both listeners. `npm run build` clean. [easy] [hardening]
- [ ] <a id="f154"></a> **F154** Audit remaining consumers of `'strategylab-saved-strategies'` for the same "snapshot at mount, never refresh" pattern that F152 fixed in SignalScanner. Candidates surfaced by F151/F152 investigation: `BotCard.tsx:357` reads `s.buyRules` off a strategy reference; `StrategyComparison.tsx:5` uses `loadSavedStrategies`. `AddBotBar.tsx:140` is the reference impl (`onFocus={loadStrategies}`). If a consumer renders persistently (no unmount between tab switches) and reads strategies from React state captured at mount, it needs the F152 fix. [easy] [hardening] (added 2026-05-12)
- [x] <a id="f155"></a> **F155** Regime-mode silent-fallback closed — when `regime.enabled` was true the StrategyBuilder hid the single-mode `buyRules` and only showed `▲ LONG` / `▼ SHORT` tabs, but the backend's `b23_mode` required BOTH dual-mode rule lists to be non-empty; if either side was empty the backtester silently fell back to the hidden unified `buy_rules` (typically still holding rules from before regime was turned on). User-visible symptom: trade tooltips showed rules the user thought they removed (reproduced on AMD with `stochastic turns_up_below 5.0` showing on tooltips while the visible LONG rule was `MA 200 EMA rising over N bars`). Fix (option #1 — strictest): `b23_mode = bool(req.regime and req.regime.enabled)` — once regime is on, only direction-specific rules drive entries; empty rule list → no entries on that side. Also fixed two symmetric display bugs: entry trade record + signal_trace use `display_buy_rules = active_buy if b23_mode else buy_rules`, exit trade record + in-position signal_trace use `display_sell_rules = active_sell if b23_mode and position_direction is not None else sell_rules` (added in Tier B review fix pass after correctness P1). Out-of-position signal_trace suppressed in b23_mode to avoid leaking hidden unified `sell_rules` into debug output. Frontend payload drops the `.some(r => r.indicator)` guard at `StrategyBuilder.tsx:371` (always sends dual rules when regime is enabled) and adds small italic hints in the LONG/SHORT entry panels saying "No long/short entry rules — no long/short positions will open. Exits/stops still run on existing positions." when that side is empty. 5 new tests in `test_backtest_short.py` use distinguishable rule conditions ("above" vs "below") so a future revert of the display locals would fail. 12/12 pass. `npm run build` clean. [medium] [hardening] (added 2026-05-12)
- [ ] <a id="f159"></a> **F159** Frontend `WalkForwardPanel.tsx` hard-codes `200` as the combo cap in 4+ inline literals instead of importing/syncing from backend `_MAX_COMBOS`. If the backend cap is ever changed, the UI guard silently diverges (user sees "OK" up to 200 but server rejects at the new value). Either expose `MAX_COMBOS` via a `GET /api/backtest/limits` endpoint at app boot, or accept the duplication and centralize the frontend literal as a top-of-file `const MAX_COMBOS_PER_WINDOW = 200`. Same drift risk applies to `OptimizerPanel.tsx`. [easy] [hardening] (from C28 review F-WFA-02; added 2026-05-12)
- [ ] <a id="f160"></a> **F160** `WalkForwardPanel.tsx` has no AbortController / request-ID guard on `api.post('/api/backtest/walk_forward')`. If the user changes `lastRequest` (e.g. switches ticker on the parent strategy) mid-flight, the stale 30-second response will still call `setResult(data)` and display IS/OOS metrics for the OLD strategy under the NEW context. Out-of-order completions are also possible if the user clicks Run twice rapidly. Add an `AbortController` ref that aborts on (a) new submit and (b) `lastRequest` change. Same risk exists in `OptimizerPanel.tsx` and `SensitivityPanel.tsx` — fix all three together. [easy] [hardening] (from C28 adversarial review ADV-WFA-008; added 2026-05-12)

### F · Polish
- [ ] <a id="f34"></a> **F34** 118+ hardcoded hex colors while CSS variables exist — theme change requires touching dozens of files. Migrate Results.tsx and BotCard.tsx to use --bg-*, --accent-*, --text-* variables. [medium] [polish]
- [ ] <a id="f35"></a> **F35** 3 different active-tab styles across app — background fill vs underline vs colored text. Standardize on one pattern (underline + accent for sub-tabs). [medium] [polish]
- [x] <a id="f36"></a> **F36** Custom date preset icon swapped — `Sidebar.tsx:302` now renders `'…'` instead of the `'⚙'` gear glyph for the `custom` DatePreset button. Matches the 1-character visual rhythm of the sibling buttons ('D', 'W', 'M', 'Q', 'Y'); no longer reads as "settings". [easy] [polish]
- [ ] <a id="f44"></a> **F44** 7 native alert()/confirm() dialogs break dark UI — flash white, block tab, can't be styled. Replace with inline confirmation rows and error text (pattern already used in FundBar). [medium] [polish]
- [x] <a id="f47"></a> **F47** Banner section-header comments removed — `SubPane.tsx` (`// Effect 1:` / `// Effect 2:` / `// Effect 3:`) and `OptimizerPanel.tsx` (`// --- Types ---` / `// --- Constants ---` / `// --- Component ---` / `// --- Styles ---`) all stripped. The code structure is self-evident without the banners. [easy] [polish]
- [x] <a id="f48"></a> **F48** OptimizerPanel local `steps` → `stepsN` rename — two scopes that previously declared `const steps = Math.max(1, parseInt(p.steps) || 5)` (estimatedCombos useMemo + requestParams.map callback) now declare `const stepsN = ...`. Matches the existing validation block's `stepsN` naming. The form-input field `p.steps` on `ParamRow` is preserved. [easy] [polish]
- [x] <a id="f78"></a> **F78** Watchlist error indicator added — `WatchlistPanel.tsx` now branches its price cell: `q?.price` populated → price+change (unchanged); else if `q?.error` set → red bold `!` glyph with the existing tooltip (new `styles.error`: `color: var(--accent-red), fontWeight: 700, fontSize: 11`); else loading `...` placeholder. Permanently-failing symbols now visually distinct from still-loading ones. [easy] [polish]
- [x] <a id="f109"></a> **F109** `BodySizeLimitMiddleware` class docstring trimmed — was 4 lines duplicating the module docstring's "Pure ASGI vs BaseHTTPMiddleware" rationale, now 2 lines stating what the class does + lifespan-interaction note. Threat model stays in the module docstring. (F125 bundle) [easy] [polish]
- [x] <a id="f110"></a> **F110** ASGI type annotations added — imported `ASGIApp, Scope, Receive, Send` from `starlette.types`; annotated `__init__`, `__call__`, and `_reply`. Type-only, runtime identical. (F125 bundle) [easy] [polish]
- [x] <a id="f116"></a> **F116** `BodySizeLimitMiddleware` non-HTTP scope passthrough — added a 3-line comment at the `if scope["type"] != "http":` guard documenting that WebSocket/lifespan scopes intentionally bypass the body cap, and that future WS routes need a separate frame-size limit. (F122 bundle) [easy] [polish]
- [ ] <a id="f140"></a> **F140** [easy] Visual smoke check for F78 + F135 error states — both items add error-conditional rendering that wasn't triggered in-browser when shipped (the F47/F48/F36 portion is visually trivial or invisible, but F78's red `!` indicator requires a permanently-failing quote symbol and F135's save-error span requires posting an all-empty-string watchlist to trigger 422). Boot `npm run dev`, add a bogus symbol like `ZZZZZZ` to the watchlist and confirm the `!` shows up red with the underlying error in the tooltip; in SignalScanner, clear the symbols input, click Save Watchlist, confirm a red error line appears beneath the button. Catch any layout regression introduced by F135's column-flex wrapper around the Save button. (from F36+F47+F48+F78+F135 bundle 2026-05-11 — orchestrator dispatched 4 parallel Sonnet agents but did not boot the dev server) [polish]
- [ ] <a id="f157"></a> **F157** Tighten the comment at `backend/routes/backtest.py:328` about `all_rules` extension. Today says "Combine all rules so indicators are computed for every rule across both sets" — should clarify that the unified `buy_rules` + `sell_rules` were already added to `all_rules` at line 314, and the b23 branch appends direction-specific rules on top. Pure prose polish; flagged as kieran-python KP-1 in F155 review. [easy] [polish] (added 2026-05-12)

### F · Testing and Infra
- [x] <a id="f30"></a> **F30** `fetch_ohlcv_async` coverage — F26 added `fetch_ohlcv_async()` to `shared.py` but the bot_runner test harness (F20) still uses `_run_in_executor(_fetch, ...)` via a mock executor. Updated `test_bot_runner.py` with `TestFetchOhlcvAsyncDedup`: concurrent dedup test (two simultaneous calls share one Future) + sequential independence test. [easy] [testing]
- [x] <a id="f33"></a> **F33** `TestTickStateTransitions` fetch-path audit — existing 7 tests patch `bot_runner._fetch` but `_tick()` now calls `fetch_ohlcv_async()` which calls `shared._fetch`. The `bot_runner._fetch` patch doesn't intercept `shared._fetch`, so tests may not be providing mock data to the actual fetch path. Fixed by patching `shared.fetch_ohlcv_async` in `_base_patches`. All 9 tests pass. [easy] (from F30) [testing]
- [x] <a id="f40"></a> **F40** `TestFetchOhlcvDedup` no longer relies on `time.sleep` — both tests now use a `threading.Event` that the executor thread waits on, and the main coroutine polls `call_count` to confirm the Future is pending before releasing. Deterministic under high system load. [easy] [testing]
- [x] <a id="f46"></a> **F46** `TestFetchOhlcvDedup` test isolation — already addressed: `setUp` calls `shared._async_ohlcv_futures.clear()` AND registers it via `self.addCleanup(...)`, which is per-test and runs even on setUp failure. Equivalent to the requested setUp/tearDown pair, with the more robust unittest idiom. [easy] [testing]
- [ ] <a id="f50"></a> **F50** Frontend console-error smoke test in overnight build — after `npm run build`, boot `npm run preview &` (Vite preview server, default port **4173** — distinct from `npm run dev`'s 5173 because we want to validate the production `dist/` artifact, not the dev server). `curl -fsS http://localhost:4173/` to verify the page loads, then a short headless check (e.g. `node -e` with a minimal fetch + parse) that there are no obvious crash markers in the served HTML. Catches "compiles clean but throws on mount" and broken import paths that `tsc -b` doesn't detect. Append to `docs/overnight-builder-prompt-patch.md` Section 3.5 (smoke test). [easy] (from overnight pipeline gap review) [infra]
- [ ] <a id="f51"></a> **F51** Headless render probe in overnight build — playwright or puppeteer agent that navigates to the main views (chart with default ticker, optimizer, bot panel), screenshots, and asserts: (a) no console errors, (b) key DOM elements exist (e.g. canvas for chart, expected button labels). Catches "blank screen" / "chart mount race" / "panel collapsed wrong" classes that F50's curl-only check misses. Real engineering effort: install playwright in the routine env, write the navigation script, decide on screenshot retention. Closes the bulk of the manual visual-verification gap. [medium] (from overnight pipeline gap review) [infra]
- [ ] <a id="f61"></a> **F61** No test coverage for `exits.py`, `regime.py`, `bot_manager.py` — `exits.py` contains stop-loss, trailing stop, and broker-fill exit logic, the most financially consequential paths. `test_bot_runner.py` has 9 tests now but covers the tick loop, not exit coordination. Start with `test_exits.py` covering stop-loss trigger + trailing-stop activation paths. [medium] (from architectural audit 2026-05-10) [testing]
- [x] <a id="f68"></a> **F68** F52/F53 round-trip + crash-recovery tests — `backend/tests/test_trading.py` (7 tests, includes 422 caps, boundary 500 + 20-char, strip/upper/empty-filter, round-trip, and cleanup-on-failure that pre-creates `{"symbols": ["ORIG"]}` and asserts content unchanged after the failed `os.replace`). `backend/tests/test_routes_providers.py` (4 tests, _persist_env create/update/append/cleanup). `_persist_env` got an `env_path: Optional[pathlib.Path] = None` test-injection parameter. (PR #30 build 22) [testing]
- [ ] <a id="f79"></a> **F79** Archive TODO/JOURNAL bloat — TODO.md is 270 lines and exceeded the 25k-token Read limit on 2026-05-10; JOURNAL.md is 429 lines. AI-collaborator velocity ships at team-pace, so recall cost compounds: every session start re-reads the full files. Plan: time-windowed monthly rollup. Move completed F-series items >30 days old to `docs/todo-archive/YYYY-MM.md`; same for JOURNAL entries to `docs/journal-archive/YYYY-MM.md`. Keep bold ID cross-references intact in archives so `[B5c]`-style backreferences stay greppable. Could be a `bin/archive-tracking-files.sh` script run monthly (or a TODO_archive task the orchestrator runs at month boundaries). Defer the GitHub Issues migration until the archive approach itself stops scaling. [medium] (renumbered F68→F79 2026-05-10 to avoid collision with PR #29's F68–F78; from conversation about tracking-file growth) [infra]
- [x] <a id="f80"></a> **F80** Two-tier review architecture — codified 2026-05-10 (build 22 morning pass). **Decisions made:** (a) `ce:review` is interactive-session only — three skill-name candidates failed across builds 20-22; the routine env doesn't load the plugin registry. Stop the probe ceremony. Manual Task-tool dispatch is canonical for overnight, not "fallback" — verified across 11+ runs. (b) Builder roster trimmed 9 → 4-6 — always-on: correctness, testing, adversarial, security. Conditional: kieran-python/typescript by file type, reliability for async/persistence diffs, project-standards for tracking-file diffs, maintainability for architecture diffs. Build 22 showed 9-persona heavy duplication produced false confidence (5 reviewers piled on a wrong "Pydantic max_length reliability" defense, which morning pass found was dead code). (c) Morning calibration roster fixed at 4 — adversarial, agent-native, security, reliability. Drops correctness/testing/maintainability/kieran-* (heavy overlap with builder roster) and learnings/project-standards (lower per-run impact). (d) Two tiers stay structurally distinct — coverage (builder) vs calibration (morning). Don't consolidate. CLAUDE.md updated; `docs/overnight-builder-prompt-patch.md` skill probe removed. (from 2026-05-10 PR #30 morning ce:review pass) [infra]
- [ ] <a id="f84"></a> **F84** `BotCard.test.tsx` does not exercise `state: undefined` path — F41 (PR #30) made `BotDetail.state` optional; existing tests always mock a populated `state` object. Add a test variant where `fetchBotDetail` resolves with `{config: {...}, state: undefined}` and assert BotCard renders without crashing (falls back to `summary.equity_snapshots` / `summary.activity_log` / `summary.last_tick`). Documents the defensive `?.state?.` chains' actual purpose. [easy] (from PR #30 testing + kieran-typescript — 2 reviewers agreed) [testing]
- [ ] <a id="f89"></a> **F89** TestClient lifespan side-effects in `test_trading.py` — `TestClient(app)` triggers `BotManager.load()`, `HeartbeatMonitor.start()`, and `init_ibkr()` on first use. CI without IBKR creds short-circuits but background tasks may produce noise / interfere with test isolation. Use `with TestClient(app) as client:` context-manager fixture (calls lifespan shutdown) or override the lifespan via `app.dependency_overrides`. Same risk in pre-existing `test_broker_route_health.py`. [medium] (from PR #30 adversarial review) [testing]
- [ ] <a id="f97"></a> **F97** [medium] Provision `backend/venv/` in routine builder container — overnight builds 21/22/23 all hit the same gap: §3.5 backend smoke test originally specified `cd backend && venv/bin/uvicorn …` but the routine container ships without a venv. Spec now codifies AST + import-time check as the substitute. Real fix: the container image includes `backend/venv/` with pinned deps (Pydantic, FastAPI, pytest). Once landed, restore the full uvicorn smoke test path. Container/infra change, not application code. (from build 23 process review) [infra]
- [x] <a id="f107"></a> **F107** `BodySizeLimitMiddleware` HEAD/OPTIONS/DELETE coverage — added `test_non_body_method_bypasses_cap_even_with_huge_content_length` (parametrized over the 3 methods) driving the ASGI middleware directly with `Content-Length: 10000` against `max_bytes=100`. Asserts the downstream handler is called and 413 is NOT emitted. [easy] [testing]
- [x] <a id="f108"></a> **F108** `STRATEGYLAB_MAX_BODY_BYTES` env-var parsing tested — extracted `parse_max_body_env(value)` from `main.py` into `middleware.py` (raises `ValueError` on invalid input so callers can log+fallback uniformly). 4 new tests: returns default on None/empty, parses valid positive integers, raises on non-numeric, raises on non-positive. `main.py` keeps the existing logger.warning + DEFAULT_MAX_BYTES fallback. [easy] [testing]
- [x] <a id="f120"></a> **F120** Stale-test failures cleared — `test_ohlcv_rejects_unknown_source` now asserts `"Invalid source"` (route layer's `require_valid_source` since F94). `test_backtest_costs.py:_req()` defaults pin IBKR Fixed rates (0.0035/0.35) so the 3 commission tests stay independent of the model's flipped defaults (Alpaca commission-free 0.0/0.0). `test_empty_journal_returns_default` expected dict expanded to include `live_spread_bps` / `half_spread_bps` (None when no broker is configured). [easy] [testing]
- [x] <a id="f121"></a> **F121** Unit-test `BotState.append_slippage_bps` cap — new `backend/tests/test_bot_state.py` (3 tests): 1001 appends → length 1000, first survivor=1.0 (not 0.0), last=1000.0; 2dp rounding pinned; below-cap passthrough sanity. (Build 25) [testing]
- [x] <a id="f123"></a> **F123** Unit-test `BotState.append_equity_snapshot` cap and shape — same `test_bot_state.py` (3 tests): 501 appends → length 500, first survivor=1.0, last=500.0; `{time, value}` shape with ISO-8601 UTC `time` (`utcoffset().total_seconds() == 0.0`) and 2dp `value`; below-cap order preservation. (Build 25) [testing]
- [x] <a id="f134"></a> **F134** F104 no-pre-seed variant added — `test_watchlist_422_does_not_create_file_when_absent` (parametrized over the same 4 empty-list shapes as the sibling) asserts `not watchlist_file.exists()` before AND after the POST. Pins the invariant that a 422 must not touch disk, complementing the existing "pre-seeded file unchanged" assertion. [easy] [testing]
- [x] <a id="f139"></a> **F139** Chronic event-loop contamination closed via `backend/tests/conftest.py` autouse fixture. Diagnosis (haiku explore): `asyncio.run()` in prior tests (test_bot_runner, test_body_size_middleware, etc.) closes its internal event loop and leaves the main thread with no default. When `test_ibkr_provider_submit_market_order` later runs, `broker.py:570` lazy-imports `ib_insync` → `eventkit/util.py:24` calls `asyncio.get_event_loop()` at module init → RuntimeError on Python 3.10+ (3.12 here). Fix: `_f139_ensure_event_loop` autouse fixture creates+sets a fresh loop per test, closes + `set_event_loop(None)` on teardown. Morning review (correctness + kieran-python, both P1 ~0.72) converged on dropping the original "leave closed loop visible" teardown — the rationale didn't hold up since pytest-asyncio 1.3.0 manages its loop via `event_loop_policy`, not the thread default. **Full backend pytest now 372/372 — first fully-clean run since the F120/F40/F46/F107/F108/F134 sweep started.** [medium] [testing]
- [ ] <a id="f142"></a> **F142** [easy] Test-suite `_STUB_RULE` divergence — `test_models.py` uses `indicator: 'price'` while `test_bot_state.py` and `test_trading.py` use `indicator: 'rsi'` (and `test_backtest_quick.py` has its own variant). If `Rule.indicator` ever gets an allowlist validator (per F106), the `'price'` stub will silently start failing in `test_models.py` but not the others. Either harmonize to `'rsi'` across all 4 files OR extract a single `_STUB_RULE` (and `_STUB_RULES_100` helper) into `tests/conftest.py` and import everywhere. (from F128 kieran-python review P3 0.75) [testing]
- [x] <a id="f143"></a> **F143** `routes/bots.py` PATCH HTTP-level smoke tests added — `tests/test_routes_bots.py` extended with `client_with_stub_manager` fixture (TestClient + monkeypatched MagicMock `bot_manager`). 13 new tests: per-rule-field reject-101-via-HTTP (parametrized × 6 fields, asserts 422 + `"too_long"` in body), per-rule-field accept-100-via-HTTP (× 6, asserts 200 + `{"ok": True}`), plus the 503 guard test for `bot_manager is None`. Also consolidated the duplicate `_RULE_FIELDS` parametrize list into a single module constant (correctness review P3 cleanup). Other 12 routes/bots.py endpoints still have no integration tests — minimum-scope-PATCH focus per the original TODO entry; broader coverage deferred to F144. [medium] [testing]
- [ ] <a id="f144"></a> **F144** [medium] Integration test coverage gap for the other 12 `routes/bots.py` endpoints — F143 closed the PATCH validator path only. Still untested at the HTTP layer: `POST /api/bots` (AddBotRequest), `GET /api/bots`, `GET /api/bots/{id}`, `DELETE /api/bots/{id}`, `POST /api/bots/start-all`, `POST /api/bots/stop-all`, `PUT /api/bots/reorder` (ReorderBotsRequest), `POST /api/bots/stop-and-close-all`, `POST /api/bots/{id}/start`, `POST /api/bots/{id}/stop`, `POST /api/bots/{id}/buy`, `POST /api/bots/{id}/reset-pnl`, `POST /api/bots/{id}/backtest`, `GET /api/bots/fund`, `PUT /api/bots/fund` (SetFundRequest). Mirrors F143's fixture pattern. Risk-tiered scope: `AddBotRequest` (which lacks the F128 cap entirely, since BotConfig fields are required when constructing a new bot) is the highest priority — 101 rules through POST today still hits the BotConfig reconstruct as 422, but without the F141-style request-level fast-fail. (from F143 follow-up 2026-05-11) [testing]
- [ ] <a id="f146"></a> **F146** [easy] `BotManager.load()` residual test coverage — F100 tests cover normalize + invalid-symbol + missing-symbol-key + None-symbol + the named ValidationError branch. Still untested: (a) empty bots list `{"bots": []}` — should load cleanly; (b) nonexistent `bots.json` (fresh install) — early `if not os.path.exists(DATA_PATH): return`; (c) the broad `except Exception` path for non-ValidationError BotConfig failures (e.g. KeyError from malformed nested dict); (d) `normalize_symbol` 20-char boundary + `BRK.B` dot symbol + digit-leading symbols (`1INCH`) at the F95 entry points. Mostly mechanical, low risk. (from F95 correctness COR-004, testing T3+T4+T5) [testing]
- [x] <a id="f148"></a> **F148** `test_short_backtest_api_endpoint` mocked via `@patch("routes.backtest._fetch")` + 24-bar synthetic price series engineered to drive RSI through `crosses_above 70` (bar 18: 62.07→72.73, short entry) and `crosses_below 50` (bar 22: 51.82→46.71, cover exit). Matches the 6 sibling tests' existing mock pattern. Tier B morning review (correctness + kieran-python personas) converged on a P1/P2 vacuous-pass finding — the trade-type loop assertion was empty-list trivial — fixed in-PR by adding explicit `"short" in trade_types` + `"cover" in trade_types` assertions before the type loop. **Removes the second chronic suite failure: full backend pytest now 371/372 (was 365/2-fail before F148 + F149), with only F139 ib_insync contamination remaining.** [medium] [testing]
- [ ] <a id="f150"></a> **F150** [easy] `_make_ibkr_provider` test-helper leaks an event loop on the `run_side_effect=None` branch — `backend/tests/test_broker.py:166` calls `asyncio.new_event_loop()` and assigns it to the mocked provider's `_loop` without ever closing it. F139's autouse fixture closes the thread-default loop but not this orphaned helper-internal loop, so Python emits `ResourceWarning: unclosed event loop` at GC if warnings are enabled. Pre-existing (not introduced by F139). Fix: wrap the helper as a yielding fixture that closes `_loop` in teardown, or close it inside the test that triggers the `_run_side_effect=None` branch. (from F139 morning review P1-F2, correctness 0.82) [testing]
- [ ] <a id="f156"></a> **F156** Add a `debug=True` signal_trace test for b23_mode that asserts (a) trace entries on bars where the dual-mode rules fire reference the direction-specific rule labels, not the unified `sell_rules` / `buy_rules` labels, and (b) the "MISSED (no position)" out-of-position trace is suppressed when `b23_mode=True` and `position=0`. Closes the correctness C2 residual gap from the F155 review pass. [easy] [testing] (added 2026-05-12)
- [ ] <a id="f161"></a> **F161** Visual smoke verification for C28 walk-forward — manual QA. Run a known overfit strategy (e.g. over-tuned RSI threshold on AAPL daily 2020-2024) and confirm: WFE < 0.5, multiple windows tagged `"spike"`, stitched equity chart renders without sawtooth, per-window table shows IS/OOS Sharpe divergence, `low_windows_warn` callout appears when configured for ≤5 windows. Then run a robust 50/200 MA crossover and confirm WFE > 0.5 with some `"stable_plateau"` tags. C28 shipped with `npm run build` clean + 39 backend + 8 frontend tests but was not visually verified in-session. [easy] [testing] (added 2026-05-12)
