# StrategyLab TODO

\*\*214 / 227 shipped.\*\* Themed roadmap. Items indexed **Section Letter + Number** (e.g. B3) for reference. Checked = done; journal has shipping details.

---

## Critical (P1)

_(none open)_

## Up Next

- [F212](#f212) — [next] F210 inline-confirm browser smoke remains partially open [easy]

## Open Work — 36 items

| Section | Topic | Open | IDs |
|---|---|---|---|
| [A](#a-charts-indicators) | Charts & Indicators | 1 | [A8](#a8) |
| [B](#b-strategy-engine-rules) | Strategy Engine & Rules | 1 | [B9](#b9) |
| [C](#c-strategy-summary-analytics) | Strategy Summary & Analytics | 3 | [C29](#c29)–[C31](#c31) |
| [D](#d-bots-live-trading) | Bots (live trading) | 1 | [D24b](#d24b) |
| [E](#e-discovery) | Discovery | 4 | [E1](#e1)–[E4](#e4) |
| [F · Architecture](#f-architecture) | Refactors, abstractions, module shape | 12 | [F2](#f2)–[F3](#f3), [F7](#f7)–[F8](#f8), [F10](#f10), [F25](#f25), [F63](#f63), [F170](#f170), [F188](#f188), [F199](#f199), [F205](#f205), [F216](#f216) |
| [F · Hardening](#f-hardening) | Security, reliability, validation | 3 | [F187](#f187), [F211](#f211), [F220](#f220) |
| [F · Polish](#f-polish) | UI, naming, dead code | 6 | [F34](#f34)–[F35](#f35), [F140](#f140), [F210](#f210), [F212](#f212), [F217](#f217) |
| [F · Testing and Infra](#f-testing-and-infra) | Test gaps, smoke tests, build pipeline | 5 | [F51](#f51), [F97](#f97), [F144](#f144), [F161](#f161), [F219](#f219) |

## A — Charts & Indicators

- [ ] <a id="a8"></a> **A8** Chart performance — large dataset optimizations (100K+ 5-min bars):
  - [x] Equity curve detail mode downsample: root cause was missing `toDisplayTime()` shift on equity timestamps — raw UTC timestamps didn't match the main chart's ET-shifted timestamps, breaking crosshair sync and bucket alignment. Fixed by adding `toDisplayTime` to `shared/utils/time.ts` (mirrors Chart.tsx `toET`) and applying it to equity/baseline/trade-tick timestamps in Results.tsx before downsampling. `downsampleEquity()` itself was always correct.
  - [x] Equity curve detail mode sync: pixel-perfect alignment with main chart via bar-count matching. Passed OHLCV timestamps (`mainTimestamps` prop) from App.tsx, built equity/baseline/tick data with same bar positions (whitespace entries for missing values), logical-range sync. Five sub-fixes: `timeVisible`, baseline bar-matching, tick snapping, deferred width sync, invisible left axis.
  - [ ] Viewport-only rendering — only pass the visible bar range to indicator series and markers instead of all 100K bars. lightweight-charts handles panning via `subscribeVisibleLogicalRangeChange`; feed data on demand. Estimated 500-700 lines across 5-6 files (Chart.tsx, SubPane.tsx, useOHLCV.ts, chartUtils.ts, backend /api/indicators); multi-session task. [hard]
  - [ ] Off-screen downsampling — when zoomed out to show all bars, aggregate to coarser resolution (e.g. 15m/1h) for rendering, switch to full resolution on zoom-in. Reduces object count 10-50x at wide zoom. [next][medium]
- [x] <a id="a14b"></a> **A14b** Chart skeleton refetch fix — `useOHLCV` exposes `isFetching` separately; App.tsx skeleton guard uses `ohlcvLoading || ohlcvFetching`. Skeleton now shows on stale-cache ticker switches. [easy] (resolved 2026-05-13)
- [x] <a id="a14c"></a> **A14c** ChartSkeleton extracted — moved component + `SKELETON_HEIGHTS` constant from `App.tsx` (was lines 89-104) to new `frontend/src/features/chart/ChartSkeleton.tsx` (default export, matches `Chart.tsx` convention). App.tsx now imports it. All `<ChartSkeleton />` call sites unchanged. [easy] (resolved 2026-05-12)
- [x] <a id="a14d"></a> **A14d** SubPane loading flag is per-Chart, not per-pane — `instanceLoading` is a single boolean propagated to every `SubPanelEntry`. If only the RSI HTF group is fetching, every subpane (RSI/MACD/Volume) shows the overlay simultaneously. Acceptable today since the regular query covers all instances at once; revisit if HTF groups split across panes get more independent. Convert to a per-instance/per-pane loading map keyed on indicator id. [medium] (from A14a review) (resolved 2026-05-15)
- [x] <a id="a15"></a> **A15** Equity curve resize handle — `Results.tsx:625` chart container's `overflow: 'hidden'` flipped to `overflow: 'auto'` so the `resize: 'vertical'` handle is actually rendered+functional. [easy] (resolved 2026-05-12)
- [x] <a id="a16"></a> **A16** Signal Trace checkbox relabeled — `StrategyBuilder.tsx:1165` checkbox label changed from "Signal Trace" → "Enable Signal Trace tab" so users know what the checkbox does. Always-visible empty-state-tab variant skipped (Results.tsx conditional tab list is not load-bearing for other behavior, but the rename alone closes the discoverability gap). [easy] (resolved 2026-05-12)
## B — Strategy Engine & Rules

- [ ] <a id="b9"></a> **B9** Cost model v2 (deferred from B6):
  - Debit-balance-aware margin interest for shorts (charge margin rate only on days net cash is negative)
  - IBKR Tiered pricing (exchange fees, SEC fee, FINRA TAF, clearing pass-throughs)
  - Hard-to-borrow dynamic rate feed
  - FX conversion cost
- [x] <a id="b30"></a> **B30** RuleRow horizontal overflow — `RuleRow.tsx:526` ruleRow style now sets `overflowX: 'auto'` + `flexWrap: 'nowrap'`, so complex rules (Stochastic K/D/Smooth) scroll horizontally instead of clipping. Browser only renders the scrollbar when content overflows, so simple MA/RSI rows are unaffected. [easy] (resolved 2026-05-12)
- [x] <a id="b31"></a> **B31** Stochastic K/D/Smooth field labels — `RuleRow.tsx:344-361` adds tiny prefix spans (K, D, Sk) before the `k_period`, `d_period`, and `smooth_k` inputs. Style matches the existing compact label idiom (`fontSize: 10, color: '#8b949e', marginRight: 2`). Inputs now readable at a glance instead of relying on hover-tooltip discovery. [easy] (resolved 2026-05-12)
- [x] <a id="b32"></a> **B32** Cmd/Ctrl+Enter runs the backtest — `StrategyBuilder.tsx:346-362` adds a window `keydown` listener. Guards: skips when `activeElement` is a `<textarea>` or `contentEditable`. `preventDefault()` for the chord. Uses a `runBacktestRef` updated on every render so the listener (registered once at mount with empty deps) always invokes the LATEST closure — naïve empty-deps would have pinned the mount-time `runBacktest` and read stale state. (orchestrator fixed the stale-closure bug post-implementer.) [easy] (resolved 2026-05-12)
- [x] <a id="b33"></a> **B33** Run button spinner/cursor feedback — `StrategyBuilder.tsx`: a 13px white border-spinner (CSS `@keyframes sb-spin`, no external lib) replaces the Play icon while `loading`; button spreads `opacity: 0.6` + `cursor: 'not-allowed'` over its base style. Inline `<style>` block injected once at the top of the return. [easy] (resolved 2026-05-12)
- [x] <a id="b34"></a> **B34** Strategy bar Delete de-emphasis — small spacer + muted color on Delete button so it reads as secondary. Full visual de-emphasis became unnecessary after B35 made deletion reversible. [medium] (resolved 2026-05-13)
- [x] <a id="b35"></a> **B35** Strategy deletion 5s undo toast — optimistic UI remove + portaled toast with countdown + Undo. Persists to localStorage only after countdown expires. Holistic fixer originally closed over a pre-computed `updated` snapshot, but the stale-closure remained: a concurrent save during the 5s window was still silently overwritten at timer expiry. Browser-verified fix: `savedStrategiesRef` mirrors latest state via effect; timer reads `savedStrategiesRef.current` so concurrent saves survive. Timer typed `setInterval` with unmount cleanup added. [medium] (resolved 2026-05-13)
- [x] <a id="b36"></a> **B36** Results tab bar overflow — `Results.tsx:391` outer tabBar wrapper switched from `flexWrap: 'wrap'` to `flexWrap: 'nowrap', overflowX: 'auto'`. Inner flex row pinned to `flexWrap: 'nowrap'`. Individual tab buttons get `whiteSpace: 'nowrap'` + `flexShrink: 0` so labels never wrap and buttons stay at natural width — narrow panes now scroll horizontally instead of stacking. [easy] (resolved 2026-05-12)
## C — Strategy Summary & Analytics

- [x] <a id="c25b"></a> **C25b** OptimizerPanel validation/submission unified — `OptimizerPanel.tsx:172` request-build expression now uses `p.steps !== '' ? parseInt(p.steps) : NaN` (identical to validation block at line 133) instead of the `Math.max(1, parseInt(p.steps) || 5)` divergence. Blank/NaN Steps produces NaN in BOTH paths now, so validation always fires before submission — single source of truth. [easy] (resolved 2026-05-12)
- [x] <a id="c25c"></a> **C25c** OptimizerPanel ordering invariant has no test — `runOptimizer()` validation now has 7 ordered branches; the `isNaN` checks must fire before `min > max` because `NaN > N` is always false in JS, so a typed-NaN min would silently pass the comparison. Add `OptimizerPanel.test.tsx` using the existing `renderHook`/`userEvent` infra (see `useOHLCV.test.ts`) covering all 7 branches with exact error-message assertions. Each branch maps to a specific user-visible string — refactors that reorder the checks would silently regress without these tests. [easy] (from C25a review) (resolved 2026-05-13)
- [x] <a id="c26"></a> **C26** Trades table has no sort/filter — chronological only, can't find biggest losers. Add sort-by dropdown (P&L high→low, P&L low→high, hold duration, exit type). [medium] (resolved 2026-05-15)
- [x] <a id="c27"></a> **C27** Metrics strip compact on Equity tab — `Results.tsx:484-519` split the single `(summary || equity)` strip into two branches: Summary keeps the full 8-metric strip, Equity renders a compact 2-metric header (Total Return % + Sharpe) using the same `styles.metric` visual style + `padding: '8px 16px'` (vs 12px). Saves ~4-8px vertical space without redesigning the strip language. [easy] (resolved 2026-05-12)
- [x] <a id="c28"></a> **C28** Walk-forward analysis (daily + intraday) — split history into rolling in-sample (IS) / out-of-sample (OOS) windows, optimize parameters on IS, score on adjacent OOS, then roll forward. Intraday added same-branch via `_format_boundary` helper (HH:MM:SS strings on window edges) + loosened YahooProvider date parsing (`pd.Timestamp` instead of strict `strptime`). Direct extension of C22 (auto-optimizer); replaces ad-hoc "tweak params, re-run on the same dates" workflow that overfits silently. Output: per-window IS-best params, OOS metric per window, concatenated/rescaled OOS equity curve (each `run_backtest()` resets capital to `initial_capital` — naive concat produces a sawtooth, must rescale at window boundaries), stitched walk-forward Sharpe vs simple-backtest Sharpe, Walk-Forward Efficiency (WFE = OOS/IS perf ratio; ≥0.5 = marginal, ≥0.7 = healthy per Pardo), parameter-stability CV across windows. Anchored (expanding IS) vs rolling (fixed-width IS) — both available, default rolling, `expand_train: bool` switch (sklearn `TimeSeriesSplit` defaults anchored which trips people up). IS objective v1: Sharpe peak + neighborhood-stability tag (check whether grid neighbors of the IS winner also rank in the top quartile — `"stable plateau"` vs `"spike"` flag). Practitioner consensus: 3:1–4:1 IS:OOS ratio, step = OOS length (non-overlapping OOS for clean stats), ≥6 windows, ≥30 trades per OOS window minimum. Window units: bar count (`is_bars`, `oos_bars`, `gap_bars`). Builds on `backend/routes/backtest_optimizer.py` + `_apply_param` in `backtest_sweep.py`. New endpoint `POST /api/backtest/walk_forward`, new `WalkForwardPanel.tsx` sub-tab in `Results.tsx` alongside `OptimizerPanel`. Gotchas: regime `_compute_regime_series()` HTF lookback extends before window start (may clip intraday limits); indicator warmup (RSI 14, MACD 35) eats short IS windows; no `end > start` validation in `StrategyRequest` — degenerate windows raise 500 not 400. Cost: 10 windows × 50 IS combos = 500 backtests; `_MAX_COMBOS=200` cap is per-optimize-call, not per-WFA. Plan: `docs/plans/2026-05-12-walk-forward-analysis.md`. [hard] (added 2026-05-12)
- [ ] <a id="c29"></a> **C29** Cluster-centroid IS selector — upgrade C28's neighborhood-stability tag to true cluster-based parameter selection (k-means on the IS grid with Silhouette Score to pick k, k=1 fallback to median). Practitioner-preferred over Sharpe-peak per Harbourfrontquant / QuantBeckman / BuildAlpha; centroid of the largest high-performing cluster is more robust than the peak point. Justification gate: ship only once C28's `"spike"` tag rate in practice is high enough to warrant the added complexity (k-means + Silhouette + k=1 handling on a 1000-point max grid). Until then C28's cheap neighborhood tag covers the same anti-overfit signal. [medium] (from C28 deferred design decision) (added 2026-05-12)
- [ ] <a id="c30"></a> **C30** Combinatorial Purged Cross-Validation (CPCV) — alternative to single-path WFA that generates a *distribution* of OOS paths via combinatorial train/test draws + bar-purging to prevent leakage. Output: Probability of Backtest Overfitting (PBO) score + Deflated Sharpe Ratio. López de Prado "Advances in Financial Machine Learning" (2018) + SSRN 3257497. 2024 ScienceDirect study shows CPCV produces lower PBO than standard WFA. Stretch goal — purging matters most for ML-style overlapping labels (triple-barrier), less for rule-based crossover signals; embargo (`gap_bars`) already covers the main rule-engine leakage path. Ship after C28 is in real use and the user wants distributional evidence beyond single WFE. [hard] (from C28 deferred design decision) (added 2026-05-12)
- [ ] <a id="c31"></a> **C31** Calendar-term window inputs for walk-forward — accept skfolio-style pandas-offset strings (`"3MS"` = 3-month start, `"QS"` = quarter start) in addition to bar-count for `is_bars`/`oos_bars` on C28. Practitioners think in calendar terms ("12 months in, 3 months out"), not bar counts, especially when comparing strategies across timeframes. Defer until bar-count proves awkward in practice — `pd.tseries.offsets` parsing + bar-count conversion per interval is non-trivial validation surface. [easy] (from C28 deferred design decision) (added 2026-05-12)
## D — Bots (live trading)

- [ ] <a id="d24b"></a> **D24b** Regime bot visual verification — D24 not visually verified. Need to run a regime bot in paper trading to confirm flip sequence, pending_regime_flip retry, and BotCard regime status display. Manual QA item.
- [x] <a id="d27a"></a> **D27a** Status badge custom popover — new `StatusBadge` component in `BotCard.tsx` with positioned-div popover (uses `getBoundingClientRect()` on hover, same pattern as `TradeTooltip.tsx`). Replaces native `title=` attribute on both status badge sites. [medium] (resolved 2026-05-13)
- [x] <a id="d28"></a> **D28** AddBotBar double-submit guard — `AddBotBar.tsx:43,89,95,135,240-243` adds `const [adding, setAdding] = useState(false)`. `handleAdd` early-returns if already in-flight, sets `adding=true` before the async POST, resets in `finally`. Submit button disabled while adding + label flips to "Adding…". Rapid clicks no longer create duplicate bots. [easy] (resolved 2026-05-12)
- [x] <a id="d29"></a> **D29** AddBotBar copies strategy direction — `AddBotBar.tsx:81` `onStrategyChange` now calls `setDirection((s.direction as 'long' | 'short') ?? 'long')` alongside the existing symbol/interval copies. Older saved strategies without `direction` field default to `'long'`. Selecting a short strategy no longer leaves the new bot at long. [easy] (resolved 2026-05-12)
- [x] <a id="d30"></a> **D30** Bot activity log status-aware empty state — `BotCard.tsx:21-29` ActivityLog component takes an optional `status` prop. Empty state now reads "Waiting for next tick" when `status === 'running'`, "Bot is stopped" when `status === 'stopped'`, "No activity yet." otherwise. Both call sites (lines 302, 601) pass `status={summary.status}`. [easy] (resolved 2026-05-12)
## E — Discovery

Own multi-session research project. Needs its own design work before implementation.

- [ ] <a id="e1"></a> **E1** Scan for good StrategyLab candidates (criteria TBD)
- [ ] <a id="e2"></a> **E2** Batch backtesting (efficiency-critical)
- [ ] <a id="e3"></a> **E3** AI/ML assisted parameter tweaking
- [ ] <a id="e4"></a> **E4** Pipeline: present candidates -> spawn bot army
## F — Architecture & housekeeping


### F · Architecture
- [ ] <a id="f2"></a> **F2** Group backend broker files into a `backend/brokers/` package **and** split `broker.py` (~680 lines) into `brokers/{yahoo,alpaca,ibkr}.py` behind the existing `TradingProvider` protocol. `broker_aggregate.py`, `broker_health.py`, `broker_health_singleton.py` move into the same package. Preserve `broker_health_singleton.py` as a separate module — it exists to break an import cycle, not as ceremony. Low value, moderate risk — only tackle if friction shows up when editing a single provider. [arch]
- [ ] <a id="f3"></a> **F3** Split `frontend/src/features/strategy/Results.tsx` (~745 lines) and `StrategyBuilder.tsx` (~578 lines) into smaller subcomponents (equity/drawdown/scatter/tables for Results; rule sections for StrategyBuilder). Same tradeoff as F2 — defer until a change actually gets painful. [arch]
- [ ] <a id="f7"></a> **F7** Sub-group `frontend/src/features/trading/` (10 files). Natural split: bot-management (`AddBotBar`, `BotCard`, `BotControlCenter`, `MiniSparkline`) vs account-view (`AccountBar`, `PositionsTable`, `OrderHistory`, `TradeJournal`) vs shared (`BrokerTag`), with `PaperTrading.tsx` staying as the feature entry. Defer until the feature grows further — 10 siblings is borderline, not painful yet. [arch]
- [ ] <a id="f8"></a> **F8** API contract drift watch. `BotConfig`, `StrategyRequest`, etc. are manually mirrored between Pydantic (backend) and `shared/types/index.ts` (frontend). Fine at ~35 types; once drift bites (fields silently dropped via the `AddBotRequest` bug), switch to generating TS types from FastAPI's OpenAPI schema via `openapi-typescript`. Flag only — don't preempt. [arch]
- [ ] <a id="f10"></a> **F10** Multi-user overhaul — proper auth + per-user data namespacing, SQLite instead of JSON for `bots` / `trade_journal`, session handling in the frontend. Don't pursue unless there's real demand for running this as a shared service; it's a different product shape from the current personal tool. [arch]
- [ ] <a id="f25"></a> **F25** WebSocket bar streaming — replace `_fetch()` polling with Alpaca's real-time bar/quote WebSocket streams. Bots react on each new bar instantly instead of polling. Eliminates the `_fetch()` network latency bottleneck (~200-500ms) that currently caps effective tick rate regardless of poll interval. [hard] [arch]
- [ ] <a id="f63"></a> **F63** `bot_manager.py` (603 lines) and `backtest.py` (937 lines) exceed the 500-line guideline — F21 (`bot_runner.py` split) was a prior TODO; status of those splits unclear. `backtest.py` is nearly 2x the soft cap and has no split planned. Re-evaluate whether the splits would meaningfully reduce cognitive load or just shuffle deckchairs. [hard] (from architectural audit 2026-05-10) [arch]
- [x] <a id="f71"></a> **F71** `atomic_write_text(path, content)` helper — new `backend/fileutil.py`. Same-directory NamedTemporaryFile, fsync, try/except close, replace, best-effort temp cleanup. 4 sites migrated: journal.py, bot_manager.py, routes/trading.py, routes/providers.py. F71 + F74 + F83 all closed by this one helper. 5 unit tests in `tests/test_fileutil.py`. [medium] [arch] (resolved 2026-05-13)
- [x] <a id="f96"></a> **F96** [medium] `get_quotes` calls `get_quote` per symbol, which re-validates `source` via a second `get_available_providers()` lookup → O(N) redundant calls + an inconsistent error shape if a provider is ever deregistered mid-batch (top-level 400 vs per-symbol error row). Refactor: extract a private `_fetch_quote(ticker, source)` helper that skips the HTTP-boundary checks; `get_quote` and `get_quotes` both call it. Removes the dead inner check and gives a single error contract. (from PR #31 build 23 kieran-python + reliability + adversarial — 3 reviewers agreed) [arch] (resolved 2026-05-13)
- [x] <a id="f98"></a> **F98** [medium] Structured error contract for `SymbolField` rejections — currently `normalize_symbol` in `backend/models.py:31,33` raises `ValueError(f"invalid symbol characters: {s!r}")` and `f"symbol too long (max 20 chars): {s[:20]!r}..."`, echoing the raw user input (including non-ASCII confusables that survive `.strip().upper()`) into Pydantic's 422 `detail.msg`. `routes/quote.py:25` separately collapses to opaque `"Invalid ticker symbol"`, leaving agent clients with no way to distinguish "too long" from "bad characters" without probing. Two reviewers had competing requests (adversarial wants LESS info, agent-native wants MORE structure). Resolve via a structured detail across all SymbolField surfaces: `detail={"error": "invalid_symbol", "reason": "must match ^[A-Z0-9][A-Z0-9.-]{0,19}$"}` (machine-readable, no input echo). Standardize across `quote.py`, `trading.py` Watchlist/Buy/Sell, and the per-row error in `get_quotes`. (from PR #31 morning ce:review adversarial 0.82 + agent-native 0.92) [arch] (resolved 2026-05-13)
- [x] <a id="f113"></a> **F113** Quick-backtest `source` field — `routes/backtest_quick.py`: `QuickBacktestRequest` and `BatchQuickBacktestRequest` both gained `source: str = "yahoo"`. `_run_quick` validates via `require_valid_source(req.source)` at entry, passes through to `_fetch(..., source=source)`. Batch loop's per-symbol `QuickBacktestRequest(...)` constructor inherits `source=req.source` so batch-level provider propagates correctly. Backward-compatible (existing callers without the field default to yahoo). [easy] [arch] (resolved 2026-05-12)
- [x] <a id="f117"></a> **F117** [easy] Status-code split between `require_valid_source` (400) and Pydantic field errors (422) for structurally-equivalent invalid-input conditions. Agent clients with retry-on-422 logic silently won't retry on bad-source errors. Two options: (a) raise `HTTPException(status_code=422, detail=[{"loc": ["body", "source"], "msg": "Invalid source", "type": "value_error"}])` from `require_valid_source` to match Pydantic's envelope — breaks any caller catching 400; (b) add `responses={400: {"description": "Invalid source"}}` to the 5 affected route decorators (`data.py:get_ohlcv`, `indicators.py:post_indicators`, `backtest.py:run_backtest`, `quote.py:get_quote`, `quote.py:get_quotes`) so OpenAPI documents the split. Pick one and apply uniformly. (from PR #32 morning ce:review agent-native, 0.95) [arch] (resolved 2026-05-13)
- [x] <a id="f153"></a> **F153** Central `requestSignature(req)` helper in `frontend/src/shared/types` (or similar) consumed by both the `BACKTEST_CACHE_KEY` writer (`App.tsx:117-125`) and the F151 restore predicate (`App.tsx:_cachedBacktest`). Today the field-pair comparator list is maintained by hand and silently drifts if a new request field is added without updating the predicate (long_buy_logic etc. would slip through). Defer until the next backtest request field is added — not urgent. [easy] [arch] (added 2026-05-12) (resolved 2026-05-13)
- [x] <a id="f158"></a> **F158** Type `is_metrics` / `oos_metrics` / `stitched_equity` properly in `backend/routes/walk_forward.py` instead of bare `dict` / `list[dict]`. Define `class BacktestSummary(BaseModel)` with the known summary fields (`num_trades: int`, `sharpe_ratio: float`, `total_return_pct: float`, `win_rate_pct: float`, `max_drawdown_pct: float`, `final_value: float`) and `class WfaEquityPoint(BaseModel): time: str | int; value: float`. Update `WindowResult.is_metrics: BacktestSummary`, `oos_metrics: BacktestSummary`, `WalkForwardResponse.stitched_equity: list[WfaEquityPoint]`. Mirror updates in `WalkForwardPanel.tsx` interface. Closes C28 review findings K2 + K11 + F-WFA-04 (multiple reviewers converged on this — bare `dict` removes the OpenAPI contract and disables static analysis at access sites like `w.oos_metrics.get("num_trades", 0)`). [easy] [arch] (from C28 review; added 2026-05-12) (resolved 2026-05-13)
- [x] <a id="f162"></a> **F162** Backtest grid hot-path optimization (commit 1 of 2). cProfile of a representative 24-combo grid (SPY daily, 3y, simple RSI rule) showed `_compute_spy_correlation` running every backtest (23% of cumtime), per-bar `pandas __getitem__` reads (34%), `_format_time` called per bar (9%), and `compute_indicators` recomputed across the grid (5%). Four landed fixes: (a) `run_backtest(req, *, include_spy_correlation: bool = True, indicator_cache: dict | None = None)` — new kwargs; route handler split into `backtest_endpoint` wrapper so FastAPI doesn't wrap the body into `{"req": ..., "indicator_cache": ...}` (caught by `test_backtest_short::test_short_backtest_api_endpoint`); (b) vectorized `shared._format_time_index(idx, interval)` using `as_unit('s').view('int64')` — the durable fix for pandas 2.0+ preserving source resolution (yfinance returns `[s]`, Alpaca returns `[us]`); the naive `// 1_000_000_000` divisor would corrupt every intraday timestamp 1000× (P0 caught by both correctness + kieran-python reviewers, independently converged on `as_unit('s')`); (c) `compute_indicators(..., cache=indicator_cache)` with new `_get_or_compute(cache, key, factory)` helper deduping MACD/RSI/ATR/MA/BB across grid combos; (d) numpy array extraction (`close_arr = close.to_numpy(copy=False)`, `date_strs = _format_time_index(df.index, req.interval)`) before the bar loop, with the 4 in-loop sites switching to array indexing — `eval_rule` and position state machine left untouched. New `tests/test_shared_format_time.py` exercises round-trip equivalence across `[s, ms, us, ns]` units (would have caught P0). Wired through `routes/backtest_optimizer.py` + `routes/walk_forward.py` IS-loop only (OOS still wants full result). Result: 24-combo grid 2.59s → 1.53s (**41% faster**, 108ms → 64ms/backtest). 424/424 pytest green. [medium] [arch]
- [x] <a id="f163"></a> **F163** Backtest grid hot-path optimization (commit 2 of 2) — shipped as **serial-only** shared `run_grid` helper after the ProcessPool prototype turned out to be slower than serial-with-cache at our grid sizes. New `backend/routes/grid_runner.py` (single public `run_grid(base, params, timeout_secs) -> (results, timed_out, skipped)`); replaces the copy-pasted `for combo in product(...): run_backtest()` loops in `routes/backtest_optimizer.py:88-122` and `routes/walk_forward.py:309-327` (IS grid only — OOS stays serial). The big win is the indicator-cache lifecycle: one `indicator_cache = {}` lives for the whole grid call, so MACD/RSI/ATR/MA/BB compute once across all combos (same df slice, identical specs). Measured 24-combo SPY-daily grid 1.53s (F162) → **0.25s** (F163, ~6×). ProcessPool path was prototyped + benchmarked: spawn-context startup (~100ms/worker on macOS) + cold yfinance fetch per worker + cache rebuild per worker beat the now-fast serial path on every grid size we tested. See F166 for the design that would actually win (window-level parallelism for WFA + persistent pool with df ship-via-initializer). Upfront `_apply_param` validation surfaces 400s immediately rather than once-per-combo. Test seam: `grid_runner.run_backtest` is module-level so patches retarget there; WFA tests dual-patch (`grid_runner_mod` for IS, `wf_mod` for OOS — same mock function, call-count dispatch preserved). 424/424 pytest green. Tier B review (correctness + kieran-python) caught one P1: original `skipped = total_combos - len(results)` lumped timed-out-not-reached combos with genuine 4xx/exception drops; fixed by promoting `run_grid` to return a 3-tuple with an explicit `skipped` counter — old optimizer-response semantics restored. [medium] [arch]
- [x] <a id="f166"></a> **F166** Window-level parallelism for WFA — shipped as **per-request** ProcessPool (not persistent). New `backend/routes/wfa_pool.py` (~600 lines): `mp.get_context("spawn")` pool sized to `min(cpu_count, n_windows)`, pool initializer ships `pickle.dumps(full_df)` once per worker process so each window worker slices in-process (no re-fetch, no cross-process df pickling per call). Each worker owns one complete window (IS grid via existing `run_grid` + OOS via `run_backtest`). Results collected in `dict[int, WindowOutput]` keyed by window_index so out-of-order completion is fine; main route iterates `windows` in index order and post-processes (scale_factor against `prev_final_equity`, rescale OOS equity, stitch). Serial fallback when `n < _MIN_WINDOWS_FOR_POOL` (=4 — empirical: spawn cost ~100ms × 8 workers ≈ 800ms break-even) OR `_FORCE_SERIAL=True` (test seam — subprocess workers can't see monkeypatches). Tier C review (correctness + kieran-python + reliability) caught a deep set of bugs that all landed mechanically as a holistic fixer pass: **(P0 C-01)** parallel worker didn't drop biased-partial IS windows — fixed via `biased_partial: bool` field on `WindowOutput` + main-route skip; serial path mirrored (use `continue` instead of `break`); **(P0 R-1)** `with ProcessPoolExecutor` block was blocking on `__exit__` for full worker duration despite `shutdown(wait=False, cancel_futures=True)` — switched to explicit `try/finally`; **(P0 R-2/P1 C-03)** `_init_worker` failure was silently dropping windows with `timed_out=False` — `BrokenProcessPool` now caught separately and raised as 500; **(P1 C-02)** workers were each given the full `timeout_secs` (resource leak when wait-deadline fires) — switched to shared `deadline = monotonic() + timeout_secs`, each submission gets `max(0, deadline - monotonic())`; **(P1 C-04/KP-04 converged)** `_wfa_start = monotonic()` was being captured AFTER `_fetch`, so `remaining_budget` always = full `_WFA_TIMEOUT_SECS` — hoisted to top of `run_walk_forward`; **(P1 R-6)** worker 5xx HTTPException was silently swallowed by generic `except Exception` — now caught separately and re-raised; **(P1 KP-02)** dead `_make_dropped_window` function deleted; **(P2 KP-06)** `_MIN_WINDOWS_FOR_POOL=4` anchored with rationale comment; **(P2 C-05)** test comment updated for new mechanism. Measured win on user's NVDA 5m 1y WFA: 57s real-world (post-F169) → ~18s (~3.2× via 8-core parallel; in-process timing 36s → 14.97s = ~2.4×). 425/425 pytest green. Optimizer NOT parallelized — single grid, no window structure, F163's serial-with-cache already wins. [hard] [arch] (deferred from F163; resolved 2026-05-12)
- [x] <a id="f169"></a> **F169** Pre-slice df in `walk_forward.py` instead of re-fetching per IS/OOS window — F162+F163 had brought per-backtest cost to ~10ms on daily but the user's real WFA on NVDA 5m 1y still took 1m55s. Each per-window `run_backtest` re-`_fetch`'d with a different `(start, end)` cache key, missing the in-process TTL cache and round-tripping to Alpaca 78× per run. WFA already pre-fetches the full df ONCE at `walk_forward.py:232`; this commit plumbs the slice through. New `run_backtest(req, *, df: pd.DataFrame | None = None, ...)` and `run_grid(base, params, timeout_secs, *, df=None)` — both kwarg-only — short-circuit `_fetch` when df is provided. WFA computes `is_df = full_df.iloc[is_s:is_e+1]` and `oos_df = full_df.iloc[oos_s:oos_e+1]` per window. Tier B review (correctness + kieran-python) caught the OOS `include_spy_correlation=True` leak (kieran KP-4 + correctness C-02 converged): each window also did a 1d SPY fetch; fixed by passing `include_spy_correlation=False` to the OOS call. Tier B review also surfaced **an unintended pre-existing-bug fix** (correctness C-01): the prior `_fetch(end=is_end_date)` path inherited yfinance/Alpaca exclusive-end semantics, so actual bar counts were `is_bars - 1` and `oos_bars - 1` per window. F169's `df.iloc[is_s:is_e+1]` produces EXACTLY `req.is_bars` and `req.oos_bars` rows — pre-existing WFA results with the same config will shift slightly because each window now has one more bar. Not a regression; a silent fix. New fence test `test_f169_df_slice_size_matches_window_config` captures every df= kwarg passed inside the WFA loop and asserts each length is exactly `is_bars` or `oos_bars`. Optimizer NOT wired (single ticker/date-range, TTL hits cleanly per combo). Measured win on user's exact NVDA 5m 1y WFA (39 windows × 11 backtests): 1m55s → **36.05s** (~3.2×). 425/425 pytest green. **Prerequisite for F166.** [medium] [arch] (resolved 2026-05-12)
- [ ] <a id="f170"></a> **F170** Vectorize `eval_rule` to read from pre-extracted numpy arrays. F162 vectorized the OHLCV reads in `run_backtest`'s outer bar loop but left `eval_rule` (called per-bar per-rule) untouched on purpose — it's also called from `bot_runner`, `signal_trace`, `rule_signals`, and the dual-mode regime path, so the contract is wider than just the backtester. cProfile showed `eval_rule` cumulative time was 20% of a 24-combo daily grid; at 5m × 4000 bars the per-bar pandas-indexing inside it dominates per-backtest cost (~270 ms/backtest on intraday vs ~10 ms after F163's cache win). Approach: precompute a numpy-array view of each indicator series once at the top of `run_backtest`, pass a lightweight `IndicatorArrays` namedtuple to `eval_rule(rule, arrays, i)`, replace internal `series.iloc[i]` / `series[i-1]` reads with `arr[i]` / `arr[i-1]`. Carefully preserve crossover detection (needs `i-1` access; index 0 must short-circuit). Update all four callers (`run_backtest`, `bot_runner._tick`, `signal_trace` builder, dual-mode regime check). Expected per-backtest cost on 5m 4000-bar workload: 270 ms → ~80 ms. Compounds with F166. [hard] [arch] (added 2026-05-12; deferred risk-isolated work — touches the most-called function in the system)
- [x] <a id="f173"></a> **F173** `wfa_pool.py` cleanup shipped — all three parts: (a) precise type annotations on `run_windows_parallel(... -> tuple[dict[int, WindowOutput], bool])` and `_run_windows_serial`; (b) `_WorkerArgs(NamedTuple)` replaces the 11-element positional worker-args tuple — fields accessed by name, pickle-safe primitives + bytes only; (c) `_run_window_core(...)` helper at module scope extracts ~155 lines of IS-winner-selection + stability-tagging + OOS-evaluation; both `_process_window_in_worker` (worker subprocess) and `_run_windows_serial` (in-process) delegate to it, passing their locally-imported `run_backtest` / `run_grid` / `_combo_key` / `_get_neighbor_keys` / `_apply_param` callables as positional arguments to preserve spawn-context purity (no closure capture, no module-level new state). Net wfa_pool.py: 610 → 593 lines, but the duplicated logic is gone — the class of bug that produced F166 C-01 (biased-partial drop logic in only one path) is now structurally impossible. Adversarial + correctness reviews ran; sole P2 finding: the new serial-path's literal `break` on `is_timed_out` differed from the original's `continue` (functionally equivalent given run_grid's clock alignment, but defensive-mirroring preferred). Fixed in the same commit — single `continue`-style code path for all is_timed_out cases (biased_partial, zero combos, full grid at deadline); loop-top deadline check is the canonical termination signal. 51/51 `test_wfa_pool` + `test_walk_forward` pass; 482/482 full backend pytest. [medium] [arch] (from F166 review KP-01 + KP-03 + KP-07; added 2026-05-12) (resolved 2026-05-15)
- [x] <a id="f175"></a> **F175** WFA SSE streaming endpoint + per-window progress bar + cap raise. New `POST /api/backtest/walk_forward/stream` SSE variant streams `started` / `progress` / `result` / `error` events; the frontend's `WalkForwardPanel` swapped its synthetic time-based progress bar (F171) for a real per-window bar driven by completed-count events. Backend wiring: `run_windows_parallel(..., progress_callback)` switched from `wait()`→`as_completed()` so windows report as they finish; shared `_setup_walk_forward(req) -> WalkForwardSetup` (NamedTuple) and `_assemble_walk_forward_response(...)` extracted so the sync route and the stream route share validation/fetch/window-construction/post-processing. Stream route runs setup via `await asyncio.to_thread(_setup_walk_forward, req)` so the FastAPI event loop is never blocked during `_fetch`. Threading: worker thread bridges via `asyncio.Queue` + `loop.call_soon_threadsafe`, each call wrapped in `try/except RuntimeError: pass` so a client-side disconnect (event loop closed) doesn't silently kill the worker thread. Cancel propagation: `threading.Event` set by the async generator's `finally` lets the progress callback short-circuit once the client is gone (work-in-progress windows can't be cancelled, but no further events queue up). All 5 SSE payloads use `json.dumps(allow_nan=False)`; if the WFA result contains NaN, the route emits an error event instead of leaking NaN literals. Cap raise: `_MAX_COMBOS_PER_WINDOW = 1000` (was 200 via shared `_MAX_COMBOS`) and `_WFA_TIMEOUT_SECS = 600` (was 120) — the F162/F163/F166/F169 speedups + the real progress bar make larger grids viable; amber warning at 500+ combos in the UI. Frontend: `SseEvent` discriminated union, `AbortController` aborts the stream on unmount or cancel button, `reader.releaseLock()` in finally, JSON.parse errors `console.warn`'d not thrown, decoder final-flush handled, progress snaps to 100% on result event. Tier C review (correctness + kieran-python + reliability + kieran-typescript) — 2 P0 + 10 P1 + 3 P2 + 4 deferred test gaps; single holistic fixer pass applied all P0/P1/P2 fixes (REL-001 cancel signal, REL-002 call_soon_threadsafe try/except, REL-003/KP-02 allow_nan=False everywhere, KP-01 NamedTuple, KP-03 thread-safety comment, KP-04 return annotation, C6 asyncio.to_thread, KT-1/2/3/4/5, KP-05 collections.abc.Callable, KP-06 test docstring). 428/428 pytest green. `npm run build` clean. [medium] [arch] (resolved 2026-05-12; from C28 follow-up speed work — full WFA chain F162→F163→F166→F169→F171→F175 took user's NVDA 5m 1y workload from 1m55s → ~18s with live per-window progress)
- [ ] <a id="f188"></a> **F188** Bots only support intraday intervals (1m–1h); daily strategies cannot be deployed. `INTERVALS = ['1m', '5m', '15m', '30m', '1h']` is hardcoded in `frontend/src/features/trading/AddBotBar.tsx:8`. Strategies backtested on daily bars (the timeframe where most documented edges — Connors RSI(2), MACD crossovers, etc. — actually work) cannot deploy because the bot polling loop only handles intraday cadence. Reproduced 2026-05-13: KO RSI(2) strategy with Sharpe 0.46 on daily bars collapses to Sharpe 0.05 / 34.5% cost drag on 1h bars (824 round-trips overwhelm the per-trade edge). Two options: (a) extend bot infrastructure to handle daily/EOD scheduling (real architecture work — needs cron-like daily tick instead of polling), or (b) add a clear "Daily strategies — backtest only, not deployable as bot" warning in the strategy save flow so users don't waste cycles tuning a daily strategy they can't ship. (a) is the right long-term answer; (b) closes the gap immediately. [hard] [arch] (added 2026-05-13)
- [x] <a id="f189"></a> **F189** `requestSignature` write/read asymmetry — fixed by extracting a typed `CacheKey` interface + `cacheWriteKey(req): CacheKey` projection, with `requestSignature(req): string` defined as `JSON.stringify(cacheWriteKey(req))` so the 17-field list lives in exactly one place (adding a field to `CacheKey` automatically covers both sides). **Important correction during review**: the initial implementation also changed App.tsx to PERSIST `cacheWriteKey(lastRequest)` instead of the full request — this was a P0 (correctness review C-01) because the persisted request is re-sent by `WalkForwardPanel` and `OptimizerPanel` to the backend, and stripping it dropped `initial_capital`, `position_size`, `source`, costs, stop_loss, trailing_stop, etc., silently substituting Pydantic defaults after every page reload. Reverted that change: the write side stores the FULL request; only the signature comparison uses the 17-field projection. 7 unit tests in `requestSignature.test.ts` pass; `npm run build` clean. (from 15-item bundle kieran-typescript KT-3) [easy] [arch] (added 2026-05-13) (resolved 2026-05-15)
- [x] <a id="f190"></a> **F190** `BacktestSummary` consolidated — moved the 14-field interface to `shared/types/strategy.ts` (canonical source), re-exported from `shared/types/trading.ts`, and retyped `BotSummary.backtest_summary` from `Record<string, number> | null` to `BacktestSummary | null`. F181's `as Record<string, number | null>` cast in `BotCard.tsx:595` removed — the field is now correctly narrowed at the type level. No dynamic indexed access exists anywhere in the codebase, so the tightening doesn't break consumers. Note (F204): backend `BacktestSummary` lives in `routes/walk_forward.py` (not `models.py` as the TODO brief claimed), uses `extra='allow'` plus all-defaulted fields, and the TS interface treats some fields as required-non-null where Pydantic defaults them; mismatch surfaced by kieran-typescript review as P3 — filed as F204 follow-up. (from 15-item bundle kieran-typescript KT-4) [easy] [arch] (added 2026-05-13) (resolved 2026-05-15)
- [ ] <a id="f199"></a> **F199** Middleware-level request deadline — the architecturally preferred option (c) from F127's design discussion. Today only `/api/backtest/quick/batch` enforces a wall-clock budget; `/api/backtest`, `/api/backtest/walk_forward`, `/api/optimize`, `/api/sensitivity`, `/api/scan`, and the bot-management routes have no upper bound on total response latency. A pure-ASGI middleware (mirroring F86's `BodySizeLimitMiddleware`) could set per-route deadlines via a route-tag → seconds dict, fall back to a sane global default, and respond 504 once exceeded. Trade-offs: (a) inherently can't cancel sync route work mid-call — would need cooperative checkpoints in expensive handlers like `run_backtest`; (b) needs a route-tagging convention so different routes can carry different budgets (intraday quote → 5s, batch backtest → 30s, WFA → 300s); (c) makes the partial-results contract (F127's `error="deadline exceeded"` per-symbol row) inapplicable for non-batch routes — they'd just 504. Worth designing before the next per-route timeout request lands. (from F127 build 28 — option (c) deferred) [hard] [arch] (added 2026-05-13)
- [ ] <a id="f205"></a> **F205** F200's per-symbol watchdog uses `ThreadPoolExecutor(max_workers=1)` — the executor is reused for the whole request but only ever runs symbols serially. For large batches (up to 500 symbols), the wall-clock budget could be spent running symbols *concurrently* with each `submit()` carrying its own timeout. Real work: (a) env-configurable `max_workers` (default still 1 to preserve today's rate-limit-friendly behavior), (b) preserve deterministic result ordering by tracking `(idx, future)` pairs and writing into a pre-sized `results[idx]` slot, (c) decide how `deadline_hit` propagates when futures complete out of order (probably: keep current behavior — once monotonic clock crosses the deadline, drain submitted futures with very short individual timeouts, then short-circuit the rest). Defer until 500-symbol batches are observably slow in practice. (from F200 follow-up 2026-05-15) [hard] [arch]
- [x] <a id="f215"></a> **F215** Idle CPU/perf bundle — chrome-devtools-mcp idle trace surfaced 48 setInterval registrations / 10s and 7 idle fetches even with the page doing nothing. Five fixes shipped together: (1) **BotCard dropped `useBroker()` subscription** — N×bot per-card observers were each arming a `setInterval(10000)` for broker polling; `BotControlCenter` now passes `adaptiveInterval` down as a prop so only one observer arms a timer (28 → 6 broker timers, **−79%**). (2) **TradeJournal pairing memoized on `[trades]`** — the O(n) LIFO entry/exit pairing on 542 rows was re-running on every parent re-render in BOTH TradeJournal and PositionsTable; now runs once per data change. (3) **`refetchInterval: () => adaptiveMs(...)` → constant** in `useTradingQueries.ts` — function-form was re-evaluated after every fetch with a clear+rearm cost; constant is cleaner. (4) **`/api/trading/journal` paginated** — added `?limit=N` query param + `Last 100/200/500/1000/All` dropdown in TradeJournal header (default 200, persisted to `strategylab-journal-limit`); CSV export does a one-shot full-history fetch so users still get everything. Payload **177 KB → 67 KB per poll (−62%)**. (5) **Single-owner polling for `journal` + `bots`** — moved `refetchInterval` off the per-observer hooks; `PaperTrading` now owns one `setInterval(5000)` that calls `qc.invalidateQueries(['journal'])` + `(['bots'])` and tanstack dedupes the refetch across observers (journal hits 4/10s → 2/10s, bots same). End-to-end: total `setInterval` registrations **48 → 14 / 10s (−71%)**, journal channel bandwidth **−81%**, zero long tasks throughout. Browser-verified before/after via chrome-devtools-mcp instrumentation hooking `window.setInterval` and `performance.getEntriesByType('resource')`. (added 2026-05-15) (resolved 2026-05-15) [hard] [arch] (added 2026-05-15)
- [ ] <a id="f216"></a> **F216** Extend the single-owner polling pattern (F215) to `positions`/`orders`/`account` queries — they still use per-observer `refetchInterval` in `useTradingQueries.ts`. Today each has only 1 consumer so there's no observable dedup win, but symmetry would let any future second consumer (e.g. a portfolio summary card reading positions) "just work" without re-introducing the multiplier bug F215 fixed. Move their `refetchInterval` into the `PaperTrading` owner effect alongside journal+bots. Trivial change; gating on it is just "do we accept the asymmetry until a second consumer appears." [easy] [arch] (added 2026-05-15)

### F · Hardening
- [x] <a id="f49"></a> **F49** `useInstanceIndicators` swallows HTF query errors — `Object.assign(merged, q.data)` silently skips when `q.data` is `undefined` (error state). User sees a chart missing an indicator with no explanation. Add `isError`/`errorMessage` to the hook's return and surface in `SubPane.tsx` (e.g. red "Failed to load" overlay, mirroring the loading overlay). Pattern is pre-existing but exposed more visibly by A14a — now that loading state is shown, the asymmetry of "loading state visible / error state silent" is more jarring. [medium] (from PR #28 reliability review) [hardening] (resolved 2026-05-13)
- [x] <a id="f55"></a> **F55** `bot_manager.save()` no longer blocks the event loop — `bot_runner.py` lines 198, 335, 506, 529, 534, 545, 552: each `self.manager.save()` inside an `async def` now wraps via `await asyncio.to_thread(self.manager.save)`. Line 468 (`_on_ibkr_error`, sync callback from IBKR's EReader thread) intentionally left as direct `self.manager.save()` — that callback has no running event loop, same pattern as the `run_coroutine_threadsafe` rule in CLAUDE.md Key Bugs Fixed. `asyncio` was already imported. 431/431 pytest. [easy] (resolved 2026-05-12) [hardening]
- [x] <a id="f59"></a> **F59** `_log_trade` errors surface to bot state + notification — `exits.py` both `_log_trade(...)` except blocks now set `state.error_message = f"Journal write failed: {exc}"` and fire `asyncio.create_task(br.notify_error(...))`. Existing `_log("ERROR", ...)` call + recovery semantics unchanged. Both methods are `async def` so `create_task` is safe (no event-loop gotcha). [medium] [hardening] (resolved 2026-05-13)
- [x] <a id="f62"></a> **F62** Journal `_load_trades()` cache — module-level `_journal_cache` + `_journal_cache_key = (st_mtime, st_size)` tuple (size avoids the 1-second mtime-granularity collision flagged by kieran-python). Holds `_journal_lock` for both read + write. `_log_trade()` nulls cache BEFORE the `atomic_write_text` call (raised-write still invalidates). Returns `list(_journal_cache)` defensive copy so callers can't corrupt the cache. 4 cache tests in `test_journal_cache.py`. [medium] [hardening] (resolved 2026-05-13)
- [x] <a id="f64"></a> **F64** `BotControlCenter.tsx` reorder failure now surfaces — the `.catch(() => {})` on `reorderBots(newOrder)` now feeds `setError(apiErrorDetail(e, 'Failed to reorder bots'))` and forces `invalidateBots()` so the optimistic cache update gets corrected back to the server's order. The existing error banner in the toolbar renders the message. (build 27) [easy] [hardening]
- [x] <a id="f65"></a> **F65** SignalScanner save-watchlist failure surfacing — closed as subsumed by F135 (build 26). F135's `saveError` state + the red `<span>` under the Save button is exactly the inline-feedback contract F65 specified; the architectural-audit entry pre-dated F135 and was never crossed off. [easy] [hardening]
- [x] <a id="f74"></a> **F74** `fsync` before `os.replace` — landed inside the F71 `atomic_write_text` helper. `fd.flush()` + `os.fsync(fd.fileno())` before the close attempt, before replace. Durability across power loss between rename and writeback. [medium] [hardening] (resolved 2026-05-13 by F71)
- [x] <a id="f83"></a> **F83** `.tmp` cleanup leak — closed by F71 helper. `fd.close()` wrapped in try/except across all 4 sites uniformly. The journal.py + bot_manager.py sites that had bare close + bare unlink now go through the helper's guarded cleanup. [hardening] (resolved 2026-05-13 by F71)
- [x] <a id="f105"></a> **F105** `interval: Literal[...]` — new `Interval = Literal["1m", ..., "1mo"]` in models.py, applied to `StrategyRequest`, `BotConfig`, `QuickBacktestRequest`, `BatchQuickBacktestRequest`, `IndicatorsPostRequest`, and `get_ohlcv` query param (6 surfaces). Pydantic now rejects invalid intervals with 422 before they reach yfinance/Alpaca. Known divergence (not fixed here, see F184): `"60m"` accepted by Pydantic but rejected by IBKR provider. [easy] [hardening] (resolved 2026-05-13)
- [x] <a id="f106"></a> **F106** [medium] Allowlist `Rule.indicator` and `Rule.condition` in `signal_engine.py`. Free-form strings reach the rule engine's dispatch logic; amplified by the now-uncapped rule lists pending under F102. Replace with `Literal[...]` or a `field_validator` that checks against the registered indicator/condition tables. (from build 24 security review, 0.65) [hardening] (resolved 2026-05-13)
- [x] <a id="f127"></a> **F127** Wall-clock request deadline added to `quick_backtest_batch` in `backend/routes/backtest_quick.py` — option (a) variant: at handler entry, `deadline = time.monotonic() + _get_batch_deadline_secs()`; between symbols, once `time.monotonic() >= deadline`, remaining symbols short-circuit to `QuickBacktestResult(ticker=symbol.upper(), error="deadline exceeded")` without invoking `_run_quick`. Configurable via env `STRATEGYLAB_BATCH_DEADLINE_SECS` (default 30s); `DEFAULT_BATCH_DEADLINE_SECS` constant mirrors F86's `DEFAULT_MAX_BYTES` naming. Helper rejects non-positive AND non-finite values (correctness round-1 P2 0.92: `float('inf')` would silently neuter the deadline since `monotonic() >= inf` is permanently False — fixed with `math.isfinite(v) and v > 0`). 5 new tests in `test_backtest_quick.py`: short-circuit invariant + received-count assertion (no `_run_quick` on tail), default-completion happy-path, uppercase parity with ValueError/Exception branches, malformed-env fallback parametrized over `["not-a-number", "0", "-5", "", "inf", "-inf", "nan"]`, positive-override fractional + integer. Test deadline set to 50ms with 100ms `slow_run` sleep for 2× CI margin (correctness round-1 P2 0.82: 10ms was a race). Architectural option (c) middleware-level deadline deferred → F199 (cross-cutting; would handle ALL routes, not just batch). [medium] [hardening]
- [x] <a id="f147"></a> **F147** `routes/bots.py:reorder_bots` wrapped in try/except → `logger.exception("/api/bots/reorder failed")` + `HTTPException(500, detail="reorder failed")`. Closes the F64 backend pair: F64 surfaced reorder failures in the UI (PR #34), F147 now ensures the server side logs the trace and returns a sanitized detail (mirroring the F115/F126/F133/F137 sanitization sweep contract). `import logging` + module-level `logger` added at top of bots.py. 2 new tests in `test_routes_bots.py` cover happy path + sanitized-500 + `caplog` assertion of the `logger.exception` entry. 29/29 pass in `test_routes_bots.py`. [easy] [hardening]
- [x] <a id="f149"></a> **F149** `ScanRequest.symbols = SymbolList = Field(min_length=1, max_length=500)` applied in `routes/trading.py` — closes the residual amplification vector after F82 / F128 by capping `/scan` at 500 symbols (parity with WatchlistRequest + BatchQuickBacktestRequest, both 500). Declarative Field cap mirrors the BatchQuickBacktestRequest pattern; the Watchlist inline-validator pattern is preserved separately for its custom error-message contract. 3 new tests in `test_trading.py` cover the 501-symbol rejection (`too_long`), 500-boundary acceptance, and empty-list rejection (`too_short`). Full `test_trading.py` 36/36 pass; full backend pytest 362/363 (only F139 pre-existing failure remains). [easy] [hardening]
- [x] <a id="f151"></a> **F151** Backtester cache restored stale trades for muted/deleted rules — `frontend/src/App.tsx` `_cachedBacktest` IIFE restored `BACKTEST_CACHE_KEY` (`'strategylab-last-backtest'`) on page mount when only ticker/start/end/interval matched, ignoring rule changes. After muting/deleting a buy rule and refreshing, the chart still showed trades fired by the now-muted rule (user-visible "rules keep running" symptom). Fix: new `loadStrategyCache()` helper reads `'strategylab-strategy'` (the StrategyBuilder snapshot); restore predicate now also compares 12 rule/logic camelCase↔snake_case pairs (`buyRules↔buy_rules`, `sellRules↔sell_rules`, `buyLogic↔buy_logic`, `sellLogic↔sell_logic`, plus long/short variants) and the `regime` block, all via `JSON.stringify` equality. Any mismatch returns `null` so the user sees no result until they click Run Backtest. Backend verified clean separately (`eval_rules` correctly skips `r.muted`; live curl with both sides muted returns `num_trades: 0`). `npm run build` clean. [easy] [hardening]
- [x] <a id="f152"></a> **F152** SignalScanner saved-strategies snapshot stale across tab switches — `frontend/src/features/discovery/SignalScanner.tsx:29` captured `loadStrategies()` once at mount, never refreshed. Tabs use `display: 'none'` (not unmount; `App.tsx:198/369/372`) so the Discovery scanner kept running pre-edit rule snapshots after the user edited a saved strategy in the Chart tab. Same staleness pattern that `AddBotBar.tsx:140` already mitigates via `onFocus={loadStrategies}`. Fix: exposed the `setStrategies` setter and added a `useEffect` that refreshes on both `window` `focus` and `storage` events; cleanup removes both listeners. `npm run build` clean. [easy] [hardening]
- [x] <a id="f154"></a> **F154** Saved-strategies stale-snapshot audit — enumerated 5 consumers. Only `StrategyComparison.tsx` needed the F152 fix (added `window.addEventListener('focus', refresh)`). BotCard reads localStorage inline at render (fresh by construction); StrategyBuilder is the writer (always current); AddBotBar already correct. Documented in commit. [easy] [hardening] (resolved 2026-05-13)
- [x] <a id="f155"></a> **F155** Regime-mode silent-fallback closed — when `regime.enabled` was true the StrategyBuilder hid the single-mode `buyRules` and only showed `▲ LONG` / `▼ SHORT` tabs, but the backend's `b23_mode` required BOTH dual-mode rule lists to be non-empty; if either side was empty the backtester silently fell back to the hidden unified `buy_rules` (typically still holding rules from before regime was turned on). User-visible symptom: trade tooltips showed rules the user thought they removed (reproduced on AMD with `stochastic turns_up_below 5.0` showing on tooltips while the visible LONG rule was `MA 200 EMA rising over N bars`). Fix (option #1 — strictest): `b23_mode = bool(req.regime and req.regime.enabled)` — once regime is on, only direction-specific rules drive entries; empty rule list → no entries on that side. Also fixed two symmetric display bugs: entry trade record + signal_trace use `display_buy_rules = active_buy if b23_mode else buy_rules`, exit trade record + in-position signal_trace use `display_sell_rules = active_sell if b23_mode and position_direction is not None else sell_rules` (added in Tier B review fix pass after correctness P1). Out-of-position signal_trace suppressed in b23_mode to avoid leaking hidden unified `sell_rules` into debug output. Frontend payload drops the `.some(r => r.indicator)` guard at `StrategyBuilder.tsx:371` (always sends dual rules when regime is enabled) and adds small italic hints in the LONG/SHORT entry panels saying "No long/short entry rules — no long/short positions will open. Exits/stops still run on existing positions." when that side is empty. 5 new tests in `test_backtest_short.py` use distinguishable rule conditions ("above" vs "below") so a future revert of the display locals would fail. 12/12 pass. `npm run build` clean. [medium] [hardening] (added 2026-05-12)
- [x] <a id="f159"></a> **F159** Resolved alongside F175 — `WalkForwardPanel.tsx` centralized the cap as a top-of-file `const _MAX_COMBOS_PER_WINDOW = 1000` (which doubled as the cap-raise), plus `_COMBO_WARN_THRESHOLD = 500`. All 4 hardcoded `200` literals replaced. Drift risk remains for OptimizerPanel.tsx (separate refactor if/when it matters). [easy] [hardening] (resolved 2026-05-12 by F175)
- [x] <a id="f160"></a> **F160** AbortController on Optimizer + Sensitivity panels — mirrored F175's WFA pattern: `abortControllerRef`, abort-on-submit, `signal: controller.signal` passed to `api.post`, ignore `AbortError`/`ERR_CANCELED`, abort on unmount. WalkForwardPanel was already done by F175. [easy] [hardening] (resolved 2026-05-13)
- [x] <a id="f165"></a> **F165** Indicator cache `id(close)` guard — `signal_engine.py`: all 6 `_get_or_compute` call sites in `compute_indicators` now prefix their key tuples with `id(close)`. Keys go from `("rsi", 14, "sma")` → `(id(close), "rsi", 14, "sma")`. Same Series object still hits the cache; a different Series (different ticker/date range) produces a distinct id and can't collide. Closes the F162 reviewers' residual-risk flag without measurable perf cost. [easy] [hardening] (resolved 2026-05-12)
- [x] <a id="f174"></a> **F174** Module-level side-effect audit — 9 files audited (routes/backtest.py, walk_forward.py, wfa_pool.py, grid_runner.py, backtest_sweep.py, shared.py, signal_engine.py, indicators.py, models.py). All module-level statements with side effects annotated with `# F174: WORKER-SAFE: <reason>` comments. No genuinely unsafe side effects found — all statements are either pure constants or lazy-guard-protected. [easy] [hardening] (resolved 2026-05-13)
- [x] <a id="f181"></a> **F181** `BotCard.tsx:595` — `as any` cast on `summary.backtest_summary` is preexisting; replace with typed narrowing (`Record<string, number>` is what the field's already typed as). One-line type fix. (from 20-item bundle kieran-typescript KT-3) [easy] [hardening] (added 2026-05-13) (resolved 2026-05-13)
- [x] <a id="f182"></a> **F182** `exits.py` `_compute_borrow_cost` + `ExitsMixin._detect_external_close` / `_evaluate_exit_reason` / `_execute_exit` — public-surface methods are unannotated (bare `cfg`, `state`, `price`, `df`, `i`, etc.). Add `BotConfig` / `BotState` / `pd.DataFrame` / `int` / `float` annotations to make the mixin's protocol boundary visible. (from 20-item bundle kieran-python KP-3 + KP-4) [easy] [hardening] (added 2026-05-13) (resolved 2026-05-13)
- [x] <a id="f184"></a> **F184** F105 `Interval` Literal accepts `"60m"` but IBKR provider rejects it (only `"1h"` works). Either remove `"60m"` from the Literal (breaks Yahoo/Alpaca callers that use it) or add a Pydantic `BeforeValidator` that normalises `"60m"` → `"1h"` since they're semantically equivalent. Needs design decision. (from 20-item bundle correctness C-05) [medium] [hardening] (added 2026-05-13) (resolved 2026-05-13)
- [x] <a id="f185"></a> **F185** `loadSavedStrategy` crashes on partial/legacy strategy objects — `setTradingHours(s.tradingHours)` (StrategyBuilder.tsx:187) has no fallback, so any saved strategy missing `tradingHours` blanks the page with `Cannot read properties of undefined (reading 'enabled')`. Same shape risk for `dynamicSizing`/`skipAfterStop` when callers seed legacy data (caught while seeding browser-test fixtures for B35). Add `?? { enabled: false, … }` defaults to each `s.X` getter; consider folding into `migrateRule`-style normalisation in `savedStrategies.ts`. [easy] [hardening] (added 2026-05-13) (resolved 2026-05-13)
- [ ] <a id="f187"></a> **F187** Optimizer param-substitution is broken for everything except `slippage_bps`. Sweeping `buy_rule_0_value`, `sell_rule_0_value`, `buy_rule_0_params_period`, `sell_rule_0_params_period` returns identical metrics across all combos — the rule-nested param paths aren't actually overriding `req.buy_rules[i].value` / `req.buy_rules[i].params.period` server-side. Reproduced 2026-05-13 with KO RSI(2) demo: 16-combo sweep of (buy threshold 5–20, sell threshold 50–80) returned identical Sharpe 0.309 / 108 trades / -17.99% DD for every row. User has noticed the same pattern previously, so this is not a one-off. Likely fix: audit the optimizer route's param-path resolver in `backend/routes/optimize.py` (or wherever the sweep applies overrides) to ensure nested keys like `buy_rules.0.value` traverse into the rules array. Until fixed, the Optimizer is effectively unusable for any non-slippage parameter. [hard] [hardening] (added 2026-05-13)
- [x] <a id="f191"></a> **F191** `apiErrorDetail` (frontend/src/shared/utils/errors.ts) doesn't handle dict-typed 422 detail. F98 changed `get_quote` to return structured `{error, reason}`; helper falls through to generic Axios message instead of `reason`. Add: after the Array branch, check `typeof detail === 'object'` and prefer `detail.reason` then `detail.error`. (from 15-item bundle correctness COR-3) [easy] [hardening] (added 2026-05-13) (resolved 2026-05-15)
- [x] <a id="f192"></a> **F192** F49 error overlay sticks indefinitely once React Query exhausts its 3 retries (~7s). Recovery requires window refocus. Either add a visible Retry button in the SubPane error overlay, or set `refetchInterval: 30_000` while in error state. (from 15-item bundle reliability R-002) [medium] [hardening] (added 2026-05-13) (resolved 2026-05-15)
- [x] <a id="f195"></a> **F195** Manual Close button on `PositionsTable` rows hit the wrong broker — `placeSell()` and the `/api/trading/sell` route ignored the position's `broker` and routed through the global `_active_broker` (default `"alpaca"`). Reproduced live 2026-05-13: clicking Close on an IBKR BABA LONG 7 position routed the close to Alpaca, where `close_position` bought-to-cover an unrelated Alpaca BABA SHORT 30. Fix: `placeSell(symbol, qty?, broker?)` sends `broker` in body; `SellRequest.broker: Optional[str]` accepted server-side; `place_sell()` uses `get_trading_provider(req.broker)`. Also keyed `PositionsTable`'s `closing` state by `symbol+broker+side` so the same symbol on two brokers no longer disables both rows simultaneously. `npm run build` clean. F196/F197/F198 follow-ups capture the cover-vs-sell journal mislabel, the wrong-sided stop-cancel cleanup, and the still-mysterious IBKR auto-cancellation of the close orders themselves. [medium] [hardening] (added 2026-05-13) (resolved 2026-05-13)
- [x] <a id="f196"></a> **F196** `_log_trade` in `/api/trading/sell` hardcodes side as `"sell"` — covering a short position via the manual Close button logs as "sell" instead of "cover". Other call sites (`exits.py:120,398`, `regime.py:203`) already do `"cover" if pos_is_short else "sell"`. Mirror that pattern: query `provider.get_positions()` for the symbol's side before close, log `"cover"` for shorts. Surfaced 2026-05-13 when the F195 routing bug caused a wrong-broker close that buy-covered an Alpaca short and journaled it as "SELL 30 manual" — actively misleading. [easy] [hardening] (added 2026-05-13) (resolved 2026-05-15)
- [x] <a id="f197"></a> **F197** `/api/trading/sell` pre-close cleanup hardcodes `o["side"] == "sell"` when cancelling pending orders. For a short position the stop-loss is a BUY order, so the cleanup is wrong-sided. Same direction-aware fix as F196: cancel `"buy"` for shorts, `"sell"` for longs. (added 2026-05-13) [easy] [hardening] (resolved 2026-05-15)
- [x] <a id="f198"></a> **F198** IBKR `close_position` orders got auto-cancelled with no visible reason — root-caused via a new `/api/debug/ibkr-errors` endpoint (50-entry `deque` hooked into `_on_ib_error`) that captured the actual ib_insync chain: code 10311 ("This order will be directly routed to NYSE. Direct routed orders may result in higher trade fees. Restriction is specified in Precautionary Settings of Global Configuration/API.") → code 201 ("Order rejected - reason:Order was discarded."). Root cause: `close_position` placed the order against `p.contract` directly — the position's stored contract has `exchange="NYSE"` because that's where the original BUY filled, and Gateway's Precautionary Settings refuse direct-routed API orders. Fix: rebuild the contract via `self._contract(symbol)` (= `Stock(symbol, "SMART", "USD")`) for both `close_position` and `close_all_positions`. Diagnostic endpoint left in place. Verified mechanically; user closed the actual stuck position via IBKR Mobile while the SMART-route patch was being verified, so the API end-to-end retry needs a backend restart (the broker.py auto-reload de-registered the IBKR provider; lifespan startup re-registers it on a full uvicorn restart). [medium] [hardening] (added 2026-05-13) (resolved 2026-05-13)
- [x] <a id="f200"></a> **F200** [easy] Per-symbol watchdog inside F127 batch deadline — F127 only checks `time.monotonic() >= deadline` BETWEEN symbols. A single hung `_run_quick` (e.g. yfinance request stuck before its httpx timeout fires, or a long `compute_indicators` on a degenerate dataframe) can still exceed the configured wall-clock budget by an unbounded amount. Wrap `_run_quick(single_req)` in a `concurrent.futures.ThreadPoolExecutor` `submit(...).result(timeout=remaining)` with `remaining = max(0, deadline - time.monotonic())` so the inner call is itself capped. On timeout, append `error="quick-backtest timed out"` and continue (or flip `deadline_hit=True` if remaining was zero). Documented as a known limitation in the F127 docstring. (from F127 build 28 — correctness residual_risk) [hardening] (resolved 2026-05-15)
- [x] <a id="f201"></a> **F201** [easy] First-symbol deadline-at-entry contract for F127 — currently if `_get_batch_deadline_secs()` returns a microsecond-scale value (or the call site is on a heavily loaded thread so elapsed time between `deadline = monotonic() + ...` and the first iteration check exceeds the budget), iteration 1 will short-circuit to `error="deadline exceeded"` with zero symbols processed. Either (a) document this in the handler docstring + OpenAPI description so clients know to expect all-deadline-exceeded on absurdly small budgets, or (b) add a `first = True` guard so at least one real attempt always runs. Build 28 correctness P3 0.78 advisory — current behaviour is defensible, just under-specified. (from F127 build 28 — correctness round-1 P3 advisory) [hardening] (added 2026-05-13) (resolved 2026-05-15)
- [x] <a id="f202"></a> **F202** [easy] `QuickBacktestResult.error` is a free-text string ("deadline exceeded", "No data for X", `str(e)`, "quick-backtest failed") — agents must substring-match an unenumerated sentinel to decide retry-the-tail vs give-up. Promote to a `Literal["deadline_exceeded", "no_data", "internal"]` companion field (e.g. `error_code: Optional[Literal[...]] = None`) while keeping the free-text `error` for humans, OR at minimum elevate `"deadline exceeded"` to a module-level `BATCH_DEADLINE_ERROR` constant referenced from the OpenAPI description so the sentinel is discoverable in the generated schema. (from PR #35 morning calibration — agent-native P2 0.85) [hardening] (resolved 2026-05-15)
- [x] <a id="f203"></a> **F203** [easy] `.env.example` missing entries for `STRATEGYLAB_BATCH_DEADLINE_SECS` (F127, 30s default) and `STRATEGYLAB_MAX_BYTES` (F86 pre-existing gap). Operators and agents reading OpenAPI see hard-coded defaults with no env name. Append both as commented examples; doc-only, no code change. (from PR #35 morning calibration — agent-native P2 0.90) [hardening] (resolved 2026-05-15)
- [x] <a id="f204"></a> **F204** TS `BacktestSummary` (`shared/types/strategy.ts`) treats `num_trades`/`total_return_pct`/`win_rate_pct`/`max_drawdown_pct`/`final_value` as required-non-null, but the backend Pydantic model in `routes/walk_forward.py` has `extra='allow'` plus defaults on every field. Some emission sites (e.g. the `no_is_trades` stub at `{'num_trades': 0}` flowing into a bot-summary response) emit partial dicts that the TS consumer would dereference assuming non-null — low likelihood given current call sites but a real shape mismatch. Either (a) make those five TS fields `number | null` to mirror Pydantic optionality, or (b) add a Pydantic `model_validator` enforcing the same required-fields contract on the backend. (from F190 review kieran-typescript K3 P3) [easy] [hardening] (added 2026-05-15) (resolved 2026-05-15)
- [x] <a id="f206"></a> **F206** Audit the rest of `backend/routes/trading.py` for the same direction-bias that F196/F197 fixed — `place_sell` is now direction-aware, but `/api/trading/close-all` (and any other handler that filters open orders by `side == "sell"` or hardcodes `"sell"` in `_log_trade`) likely still has the long-bias bug. Apply the same probe-then-derive pattern (one `provider.get_positions()` call per symbol; derive `cancel_side` and `log_side`). Grep for `"sell"` in `routes/trading.py` and audit each site. (from F196/F197 follow-up 2026-05-15) [easy] [hardening] (resolved 2026-05-15)
- [ ] <a id="f211"></a> **F211** F206 audit found `close_all_positions` (line ~258) delegates to `provider.close_all_positions()` (a broker primitive) with no per-symbol direction probe. Verify the IBKR and Alpaca primitives actually buy-to-cover shorts correctly when called from this endpoint — particularly IBKR's `close_all` if it exists, since the SMART-routing trap previously hid this kind of bug. May require a paper-trading test (open one long + one short on the same broker, then hit the Close-All button, check both close cleanly + journal correctly). (from F206 audit follow-up 2026-05-15) [medium] [hardening]
- [x] <a id="f214"></a> **F214** Manual close attribution — `/api/trading/sell` was logging journal rows with `broker=None`, no `bot_id`, and the frontend P&L pairing skipped any non-`bot` source so manual closes of a bot's position showed `—` for broker, P&L, and Gain%, AND the bot card / portfolio card never credited the realized P&L. Fixed end-to-end: (a) `routes/trading.py` `/sell` now resolves `broker = req.broker or get_active_broker()`, looks up the owning bot via new `_find_owning_bot(symbol, direction)` helper (regime/bidirectional bots match either side), and passes `broker=` + `bot_id=` + `direction=` to `_log_trade`; `/buy` also tags broker. (b) `journal.py compute_realized_pnl` now accepts trades when `bot_id` matches the requested bot regardless of `source`, so manual exits credit the bot's P&L (flows to bot card via next `bot_runner._tick`). (c) `frontend/.../TradeJournal.tsx` pairing loop dropped the `t.source !== 'bot'` gate, so manual exits paired with bot entries display green/red P&L + Gain% and the win/loss row tint applies. (d) `bin/backfill-manual-trades.py` one-shot script backfills existing rows: walks the journal chronologically, pairs each manual SELL/COVER with the most-recent unconsumed entry on the same `(symbol, direction)`, copies that entry's `bot_id` onto the exit, and sets `broker="alpaca"` (the only historical broker) on any manual row missing it. Backed up `trade_journal.json.bak` before write; on the live journal: 8 manual rows broker-backfilled, 4 of those bot_id-tagged (the other 4 had no preceding bot entry to pair with). (added 2026-05-15) (resolved 2026-05-15) [medium] [hardening] (added 2026-05-15)
- [x] <a id="f218"></a> **F218** Chart perpetual repaint loop — user spotted naked-eye flicker in main chart + ghosted/duplicated axis labels and B/S markers on MACD/RSI panes in a phone-camera long-exposure photo (invisible to crisp screenshots). Browser-instrumented via chrome-devtools-mcp: `requestAnimationFrame` was scheduled **120/sec sustained while idle**, `getContext` called **~1320/sec**, canvas attributes mutating **120/sec** between two fixed sizes (1298×94.375 ↔ 1304×100.625) every frame. Three parallel ce:review agents (performance + julik-frontend-races + correctness) all confirmed at conf ≥0.90: external `ResizeObserver` in `Chart.tsx:371` and `SubPane.tsx:172` calling `chart.applyOptions({width, height})` was a leftover v4 pattern that **fights lightweight-charts v5's internal RO-based auto-sizing** — the two would never agree on the canonical canvas size (DPR fractional pixels), producing a stable two-state oscillation at frame rate. First fix attempt (rAF-batch the RO + skip-if-same) made it WORSE (240→480 rAF/s, 5040 mutations/2s) because rAF deferral retimes the oscillator into a two-frame cycle rather than breaking it. Real fix (~10 lines, two files): `autoSize: true` in `chartOptions`, drop explicit `height` from both `createChart()` calls, delete both external `new ResizeObserver(...)` blocks. **After:** rAF/getContext/mutations all = **0/sec** while idle. CPU usage drop noticed in Activity Monitor. Three-pane sync, crosshair, syncWidths, regime bg, markers all intact. (added 2026-05-15) (resolved 2026-05-15) [medium] [hardening]
- [ ] <a id="f220"></a> **F220** Sweep remaining lightweight-charts callsites for v4-pattern `ResizeObserver + applyOptions({width, height})` — F218 fixed the main `Chart.tsx`/`SubPane.tsx` pair, but bot card sparklines and any other chart instances (Discovery? equity curve overlay?) may carry the same dead pattern. Grep for `new ResizeObserver` and `applyOptions.*width.*height` across `frontend/src/` — for each hit, either migrate to `autoSize: true` (preferred, matches F218) or document why the explicit RO is intentional. Each instance left unfixed is a potential ~60Hz CPU sink that compounds with bot count. [easy] [hardening] (added 2026-05-15)

### F · Polish
- [ ] <a id="f34"></a> **F34** 118+ hardcoded hex colors while CSS variables exist — theme change requires touching dozens of files. Migrate Results.tsx and BotCard.tsx to use --bg-*, --accent-*, --text-* variables. [medium] [polish]
- [ ] <a id="f35"></a> **F35** 3 different active-tab styles across app — background fill vs underline vs colored text. Standardize on one pattern (underline + accent for sub-tabs). [medium] [polish]
- [x] <a id="f44"></a> **F44** 7 native alert()/confirm() dialogs break dark UI — flash white, block tab, can't be styled. Replace with inline confirmation rows and error text (pattern already used in FundBar). [medium] [polish] (resolved 2026-05-15)
- [ ] <a id="f140"></a> **F140** [easy] Visual smoke check for F78 + F135 error states — both items add error-conditional rendering that wasn't triggered in-browser when shipped (the F47/F48/F36 portion is visually trivial or invisible, but F78's red `!` indicator requires a permanently-failing quote symbol and F135's save-error span requires posting an all-empty-string watchlist to trigger 422). Boot `npm run dev`, add a bogus symbol like `ZZZZZZ` to the watchlist and confirm the `!` shows up red with the underlying error in the tooltip; in SignalScanner, clear the symbols input, click Save Watchlist, confirm a red error line appears beneath the button. Catch any layout regression introduced by F135's column-flex wrapper around the Save button. (from F36+F47+F48+F78+F135 bundle 2026-05-11 — orchestrator dispatched 4 parallel Sonnet agents but did not boot the dev server) [polish]
- [x] <a id="f157"></a> **F157** `routes/backtest.py:358` comment rewritten — now states explicitly that `all_rules` already holds the unified buy+sell pool from an earlier line, that the b23 branch appends direction-specific rules so their indicators are pre-computed, and that duplicates are harmless because `compute_indicators` caches by `(indicator, params)`. Pure prose; no code change. [easy] [polish] (resolved 2026-05-12)
- [x] <a id="f171"></a> **F171** WFA UI run-progress feedback — shared `useRequestTimer(active)` hook (renamed from `useElapsedSeconds` mid-session) in `frontend/src/shared/hooks/useRequestTimer.ts` returns `{elapsed, final}`: `elapsed` ticks every ~250ms while `active=true`, `final` captures the run duration on the falling edge and stays stable until the next run starts. Used in WalkForwardPanel, OptimizerPanel, SensitivityPanel. Three UX additions: (1) button text `Running 12s…` while in flight; (2) post-run `Completed in 18s` indicator (with `35% under estimate` / `12% over estimate` calibration callout in WFA when the pre-flight estimate is non-zero); (3) thin time-based progress bar in WFA underneath the live elapsed line, width = `min(99%, elapsed / estimatedSeconds * 100%)` (capped at 99% so the user knows the run isn't done until result lands). Progress bar is synthetic (no streaming from backend, no granular events) — it divides clock time, not real work. Required exposing `estimatedSeconds: tSecs` from `preflightEstimate` (was previously only `timeStr`). Hook uses `performance.now()` so wall-clock stays honest under browser tab throttling. `npm run build` clean. [easy] [polish] (resolved 2026-05-12)
- [x] <a id="f183"></a> **F183** Trim F174 `_data_rate_counter` comment in `shared.py:32` — currently over-explains by referencing subprocess isolation as the safety property. Should be one sentence: `# F174: WORKER-SAFE — pure in-process deque allocation; no I/O.` (from 20-item bundle kieran-python KP-6) [easy] [polish] (added 2026-05-13) (resolved 2026-05-13)
- [x] <a id="f186"></a> **F186** F160 abort-on-resubmit path is dead in OptimizerPanel + SensitivityPanel — both buttons are `disabled={loading}` (OptimizerPanel.tsx:308, SensitivityPanel.tsx:223), so users can never double-fire and the `abortControllerRef.current?.abort()` at submit time (lines 172/117) never has anything to abort. Unmount-cleanup is also rarely exercised because Results.tsx sub-panels stay mounted via `display: 'none'`. Either delete the resubmit-abort branch + keep only unmount cleanup, or wire a real Cancel button (like F175 WFA) if user-triggered abort has value for long Optimizer runs. F175 retains its full abort surface (Cancel button is the user trigger). (browser-verified during F160 smoke test 2026-05-13) [easy] [polish] (added 2026-05-13) (resolved 2026-05-13)
- [x] <a id="f208"></a> **F208** F192 retry button — removed `pointerEvents: 'none'` from the SubPane error overlay so the Retry button is clickable. Verify keyboard accessibility: Tab should focus the Retry button when the error overlay is open, Enter should fire `onRetry`. Also browser-verify hover/focus styling matches the rest of the SubPane button vocabulary. (from F192 follow-up 2026-05-15) [easy] [polish] (resolved 2026-05-15)
- [x] <a id="f209"></a> **F209** C26 trades-table sort dropdown should persist the last-selected sort mode across reload — currently `useState<SortMode>('chronological')` resets on every mount. Either localStorage-key it by strategy id, or scope it per-result with `useEffect` reset on `trades` reference change (so a fresh backtest restarts at chronological but a tab-switch within the same run preserves mode). (from C26 follow-up 2026-05-15) [easy] [polish] (resolved 2026-05-15)
- [ ] <a id="f210"></a> **F210** F44 inline-confirm rewrites of `alert()` / `confirm()` shipped without browser verification — per the Live-Browser UI Verification rule, this should be exercised in `chrome-devtools-mcp` against the 6 sites: BotControlCenter Stop+Close, BotCard menu Reset P&L, BotCard expanded-card Reset P&L, StrategyBuilder commission migration notice, StrategyBuilder name-conflict on rename, StrategyBuilder import-rules Replace. Verify visual layout, focus management on Cancel, and that the second click commits. (from F44 follow-up 2026-05-15) [easy] [polish] [testing]
- [ ] <a id="f212"></a> **F212** [next] F210 inline-confirm browser smoke remains partially open — F208 (Retry button a11y) browser-verified 2026-05-15 via chrome-devtools-mcp (XHR-patched indicator failure → Retry button surfaces, `tabIndex=0`, `pointerEvents=auto`, focus+Enter fires 4 indicator XHRs across MACD+RSI subpanes; screenshot at `docs/screenshots/f208-retry-button.png`). F210's 6-site walk-through was not completed in-session — the bot-related sites (BotControlCenter Stop+Close, BotCard menu Reset P&L, BotCard expanded Reset P&L) need a running bot with open positions and aren't reachable from a cold dev server. The 3 StrategyBuilder sites (commission-migration notice, rename-conflict, import-replace) are reachable via localStorage seeding but were deferred. Tackle either via paper-trading session or via headless saved-strategy seeding for the 3 StrategyBuilder sites. [easy] [polish] [testing]
- [x] <a id="f213"></a> **F213** Trades-table sorting promoted from a dropdown (C26) to click-to-sort column headers. 12 sortable columns (`#`, Entry date, Entry $, Exit date, Exit $, Shares, P&L, Return, Slip, Comm, Borrow, Exit type). Three-state cycle: click → asc ↑, click again → desc ↓, third click → clear (back to chronological). Cross-column switching resets to asc on the new column. Active column rendered in `--text-primary` colour with the directional arrow; inactive columns stay muted. Comparators handle date strings + unix-int intraday timestamps + pair-sum costs (slip/comm aggregate buy+sell legs via the sell→buy index map). Persistence key migrated from `strategylab-trades-sort-mode` (old SortMode enum) to `strategylab-trades-sort` (new `{col, dir}` JSON). Tooltip on the header row documents the cycle. Browser-verified end-to-end: asc/desc/clear transitions, cross-column switch, localStorage round-trip; screenshot at `docs/screenshots/trades-sort-click-headers.png`. (added 2026-05-15) (resolved 2026-05-15) [easy] [polish]
- [ ] <a id="f217"></a> **F217** TradeJournal "Last N" dropdown should surface the unloaded count — F215 added the limit selector but the user can't tell at a glance whether their selection is truncating the history (e.g. with 542 trades and `Last 200`, 342 trades aren't visible). Two options: (a) the existing `count` chip after "Trade Journal" already shows `trades.length` from the limited fetch — change the label to `200 of 542` when limited, plain `542` when "All", which means the endpoint also needs to return the unbounded total (cheap: add `total: int` to the journal response alongside `trades: list`). (b) cheaper fallback if backend change is unwanted: just add a small "(showing last 200)" hint next to the dropdown when limited. Pick (a) — it's the right shape long-term and a 1-field response addition. [easy] [polish] (added 2026-05-15)

### F · Testing and Infra
- [x] <a id="f50"></a> **F50** Frontend console-error smoke test in overnight build — after `npm run build`, boot `npm run preview &` (Vite preview server, default port **4173** — distinct from `npm run dev`'s 5173 because we want to validate the production `dist/` artifact, not the dev server). `curl -fsS http://localhost:4173/` to verify the page loads, then a short headless check (e.g. `node -e` with a minimal fetch + parse) that there are no obvious crash markers in the served HTML. Catches "compiles clean but throws on mount" and broken import paths that `tsc -b` doesn't detect. Append to `docs/overnight-builder-prompt-patch.md` Section 3.5 (smoke test). [easy] (from overnight pipeline gap review) [infra] (resolved 2026-05-15)
- [ ] <a id="f51"></a> **F51** Headless render probe in overnight build — playwright or puppeteer agent that navigates to the main views (chart with default ticker, optimizer, bot panel), screenshots, and asserts: (a) no console errors, (b) key DOM elements exist (e.g. canvas for chart, expected button labels). Catches "blank screen" / "chart mount race" / "panel collapsed wrong" classes that F50's curl-only check misses. Real engineering effort: install playwright in the routine env, write the navigation script, decide on screenshot retention. Closes the bulk of the manual visual-verification gap. [medium] (from overnight pipeline gap review) [infra]
- [x] <a id="f61"></a> **F61** `tests/test_exits.py` — 6 tests via `_ExitHarness` concrete class wrapping `ExitsMixin._evaluate_exit_reason`. Covers: stop-loss long trigger, stop-loss short trigger (inverted threshold), stop-loss disabled (None), trailing-stop long activation threshold, trailing-stop long peak tracking + stop-out, trailing-stop short trough tracking + stop-out. [medium] [testing] (resolved 2026-05-13)
- [x] <a id="f79"></a> **F79** TODO + JOURNAL archived. TODO went 270 → 159 lines (-41%) via `bin/sync-todo-index.py --archive-before 2026-05-12` moving 82 completed items to `docs/todo-archive/shipped-pre-2026-05-12.md`. JOURNAL went 933 → 519 lines (-44%) via one-off `split_journal.py` extracting Apr entries to `docs/journal-archive/2026-04.md` (150 lines) and May 1-9 entries to `2026-05-pre10.md` (274 lines). Active JOURNAL holds 2026-05-10..12 only (last 3 days of post-shipment context). Bold ID cross-references like **[F175]** still grep across active + archived files. (resolved 2026-05-12) [medium] [infra]
- [x] <a id="f84"></a> **F84** BotCard `state: undefined` defensive-path test — `BotCard.test.tsx` adds one test using `vi.mocked(fetchBotDetail).mockResolvedValueOnce({config: minimalConfig, state: undefined})` to verify the `?.state?.` fallback chains. Holistic fixer replaced initial `as never` casts with a proper `minimalConfig: BotConfig` fixture so future type drift surfaces. [easy] [testing] (resolved 2026-05-13)
- [x] <a id="f89"></a> **F89** TestClient lifespan fixtures — `test_trading.py` and `test_broker_route_health.py` converted to `with TestClient(app) as c: yield c` context-manager form. Module-scope (`scope="module"`) — function-scope made the full suite 32x slower (80s vs ~20s). Module-scope mirrors the original pre-F89 module-level constant's semantics + adds lifespan shutdown. [medium] [testing] (resolved 2026-05-13)
- [ ] <a id="f97"></a> **F97** [medium] Provision `backend/venv/` in routine builder container — overnight builds 21/22/23 all hit the same gap: §3.5 backend smoke test originally specified `cd backend && venv/bin/uvicorn …` but the routine container ships without a venv. Spec now codifies AST + import-time check as the substitute. Real fix: the container image includes `backend/venv/` with pinned deps (Pydantic, FastAPI, pytest). Once landed, restore the full uvicorn smoke test path. Container/infra change, not application code. (from build 23 process review) [infra]
- [ ] <a id="f144"></a> **F144** [medium] Integration test coverage gap for the other 12 `routes/bots.py` endpoints — F143 closed the PATCH validator path only. Still untested at the HTTP layer: `POST /api/bots` (AddBotRequest), `GET /api/bots`, `GET /api/bots/{id}`, `DELETE /api/bots/{id}`, `POST /api/bots/start-all`, `POST /api/bots/stop-all`, `PUT /api/bots/reorder` (ReorderBotsRequest), `POST /api/bots/stop-and-close-all`, `POST /api/bots/{id}/start`, `POST /api/bots/{id}/stop`, `POST /api/bots/{id}/buy`, `POST /api/bots/{id}/reset-pnl`, `POST /api/bots/{id}/backtest`, `GET /api/bots/fund`, `PUT /api/bots/fund` (SetFundRequest). Mirrors F143's fixture pattern. Risk-tiered scope: `AddBotRequest` (which lacks the F128 cap entirely, since BotConfig fields are required when constructing a new bot) is the highest priority — 101 rules through POST today still hits the BotConfig reconstruct as 422, but without the F141-style request-level fast-fail. (from F143 follow-up 2026-05-11) [testing]
- [x] <a id="f146"></a> **F146** `BotManager.load()` residual coverage — `test_bot_state.py::TestBotManagerLoadResidual` (6 tests): empty bots list, missing file early-return, KeyError on malformed dict (outer except), normalize_symbol dot/digit-leading/20-char-boundary handling. [easy] [testing] (resolved 2026-05-13)
- [x] <a id="f148"></a> **F148** `test_short_backtest_api_endpoint` mocked via `@patch("routes.backtest._fetch")` + 24-bar synthetic price series engineered to drive RSI through `crosses_above 70` (bar 18: 62.07→72.73, short entry) and `crosses_below 50` (bar 22: 51.82→46.71, cover exit). Matches the 6 sibling tests' existing mock pattern. Tier B morning review (correctness + kieran-python personas) converged on a P1/P2 vacuous-pass finding — the trade-type loop assertion was empty-list trivial — fixed in-PR by adding explicit `"short" in trade_types` + `"cover" in trade_types` assertions before the type loop. **Removes the second chronic suite failure: full backend pytest now 371/372 (was 365/2-fail before F148 + F149), with only F139 ib_insync contamination remaining.** [medium] [testing]
- [x] <a id="f150"></a> **F150** `_make_ibkr_provider` event loop leak closed — `tests/test_broker.py` rewrote the helper to close BOTH loops it allocates: the `run_side_effect` branch closes the constructor loop immediately (it was unused for dispatch), and the default branch wraps each `run_until_complete` in a `try/finally` that closes the one-shot loop after each call. No `ResourceWarning: unclosed event loop` from this site. Pre-existing, pre-F139. [easy] [testing] (resolved 2026-05-12)
- [x] <a id="f156"></a> **F156** b23_mode signal_trace tests shipped — `tests/test_backtest_short.py` adds `test_b23_signal_trace_uses_direction_specific_labels` (assertion a: trace labels reference direction-specific rule lists) and `test_b23_signal_trace_suppresses_missed_no_position` (assertion b: MISSED trace suppressed when `position == 0`). **Test surfaced a real production bug** in `routes/backtest.py:465` — `_trace_rules` used `resolve_series(r, indicators) or indicators.get("close")` which triggers `ValueError: The truth value of a Series is ambiguous` on any non-None Series return. Fixed with explicit `if ind_series is None` guard. Bug affected ANY `POST /api/backtest` call with `debug: True` and indicator rules (price, rsi, macd, bb, atr, etc.) — would have 400'd on the user. Test originally landed as `@pytest.mark.xfail(strict=True)`; orchestrator applied the prod fix and flipped the mark to passing. 433/433 pytest. [easy] [testing] (resolved 2026-05-12)
- [ ] <a id="f161"></a> **F161** Visual smoke verification for C28 walk-forward — manual QA. Run a known overfit strategy (e.g. over-tuned RSI threshold on AAPL daily 2020-2024) and confirm: WFE < 0.5, multiple windows tagged `"spike"`, stitched equity chart renders without sawtooth, per-window table shows IS/OOS Sharpe divergence, `low_windows_warn` callout appears when configured for ≤5 windows. Then run a robust 50/200 MA crossover and confirm WFE > 0.5 with some `"stable_plateau"` tags. C28 shipped with `npm run build` clean + 39 backend + 8 frontend tests but was not visually verified in-session. [easy] [testing] (added 2026-05-12)
- [x] <a id="f164"></a> **F164** `mock_run_backtest` explicit signatures — ~26 sites across `test_walk_forward.py` (18) + `test_backtest_optimizer.py` (8) tightened from `(req, **kwargs)` to `(req, *, include_spy_correlation=True, indicator_cache=None, df=None)`. Future kwargs to production `run_backtest` will now surface as TypeError instead of being silently swallowed. [easy] [testing] (resolved 2026-05-13)
- [x] <a id="f167"></a> **F167** `_patch_run_backtest` helper — extracted to top of `test_walk_forward.py`. Replaces 18 dual-patch sites (correctness review caught 5 orphan sites the initial implementer missed, including one that patched only `wf_mod.run_backtest` and would have silently hit the real backtest if reached via `grid_runner_mod`'s binding). Holistic fixer migrated all 23 sites. [easy] [testing] (resolved 2026-05-13)
- [x] <a id="f168"></a> **F168** `tests/test_grid_runner.py` — 7 direct unit tests for `run_grid()`: 3-tuple shape, 4xx skip, 5xx re-raise, ValueError skip, `_apply_param` 400 before loop, timeout partial results + `timed_out=True`, single-combo case. [easy] [testing] (resolved 2026-05-13)
- [x] <a id="f172"></a> **F172** Direct unit tests for `routes/wfa_pool.run_windows_parallel` parallel path. The autouse `_force_serial_wfa` fixture in `tests/test_walk_forward.py` sets `_FORCE_SERIAL=True` for every test (subprocess workers can't see monkeypatches), so the actual ProcessPool dispatch, df pickling, `_init_worker`, worker error propagation, and timeout-driven shutdown have ZERO test coverage. Add `tests/test_wfa_pool.py` with at least: (a) happy-path 4-window parallel run on a synthetic 200-bar df (no live fetch); (b) `_init_worker` raising → `BrokenProcessPool` surfaces as HTTPException(500), not silent empty result; (c) per-worker timeout produces partial results + `timed_out=True`; (d) biased-partial drop equivalence between parallel and serial paths on identical inputs. Use a tiny synthetic df + a real (un-mocked) `run_backtest` call against deterministic data so the test exercises the pool plumbing. All 3 reviewers (correctness C-06, kieran TG-01, reliability R-5) converged on this gap. [easy] [testing] (from F166 review; added 2026-05-12) (resolved 2026-05-13)
- [x] <a id="f176"></a> **F176** Parallel-path progress test — shipped as `backend/tests/test_wfa_pool.py::TestRunWindowsParallel::test_progress_callback_fires_per_window`. Real ProcessPool dispatch (no `_FORCE_SERIAL` fixture; `n_windows == _MIN_WINDOWS_FOR_POOL == 4` guarantees the parallel path). Synthetic 170-bar daily df + trivial RSI rules + `min_trades_is=0` so workers complete in ~1.2s without network I/O. Asserts: progress callback fires exactly `n_windows` times, `completed` values cover `{1..n_windows}`, max call is `(n_windows, n_windows)`, results dict has all windows, `timed_out=False`. Tier B review added `assert not _FORCE_SERIAL` guard at test entry so a leaked fixture from another module fails loudly instead of silently running the serial path. `@pytest.mark.skipif(SKIP_SLOW_TESTS=="1")` for CI escape hatch (F180 to convert to a registered `slow` marker). 431/431 backend green. [easy] [testing] (resolved 2026-05-12)
- [x] <a id="f177"></a> **F177** Cancel-signal integration test — shipped as `test_walk_forward.py::TestStreamEndpoint::test_cancel_signal_sets_event_on_early_close`. Subclasses `threading.Event` into `_RecordingEvent` that captures every constructed instance + tracks `set()` calls; monkeypatches `wf_mod.threading.Event` to the subclass. Test opens `client.stream()`, breaks after the first `progress` event, asserts on with-block exit that `instances[-1].is_set() == True` — the LAST-constructed Event (the cancel_event inside `event_stream()`) was specifically set, not just any Event in the process. Tier B correctness review caught the broad-assertion P0 (`len(set_calls) >= 1` would pass if any unrelated Event fired); fix tightened to instance-identity check. Mock signature also tightened: `_stub_run_backtest(req, *, include_spy_correlation=True, indicator_cache=None, df=None)` rejects unknown kwargs so new production kwargs surface as TypeErrors. [easy] [testing] (resolved 2026-05-12)
- [x] <a id="f178"></a> **F178** NaN-in-metrics serialisation test — shipped as `test_walk_forward.py::TestStreamEndpoint::test_nan_in_metrics_emits_error_event`. Monkeypatches `wf_mod._assemble_walk_forward_response` to inject `wfe=float('nan')` into the otherwise-valid response, then asserts the stream emits `started` → `error` (status=500, detail mentions "serialis") with no subsequent `result` event. Verifies the `json.dumps(allow_nan=False)` ValueError → error-event path added by F175. Mock signature tightened alongside F177. [easy] [testing] (resolved 2026-05-12)
- [x] <a id="f179"></a> **F179** Frontend SSE malformed-event handling test — shipped as `frontend/src/features/strategy/sseParser.test.ts` (7 tests, co-located with source per project convention). Required extracting the inline `JSON.parse + try/catch` from `WalkForwardPanel.tsx` into `sseParser.ts` (24 lines: `parseSseFrame(dataLines, label='SSE event'): unknown | null`). Behavior preserved exactly at both call sites (main read loop + after-loop flush); flush site passes `label='SSE event in flush'` to keep the diagnostic distinction from the pre-F179 inline code. Tests cover: valid event, multi-line event, empty input (silent null), malformed JSON (warn + null), truncated JSON (warn + null), JSON `null` literal (silent null — not malformed), whitespace-only line (warn + null). Tier B kieran-typescript review caught the unsafe `as SseEvent | null` cast at both call sites — added runtime `typeof evt.type !== 'string'` discriminant guard so non-conforming JSON objects log+skip instead of silently falling through every type branch. `npm run build` clean. [easy] [testing] (resolved 2026-05-12)
- [x] <a id="f180"></a> **F180** Registered `slow` marker in `backend/pytest.ini` with a one-line description (deselect with `-m 'not slow'`). Replaced `test_wfa_pool.py`'s `@pytest.mark.skipif(os.environ.get("SKIP_SLOW_TESTS") == "1", ...)` with `@pytest.mark.slow`. Dropped now-unused `import os`. Default `pytest` still runs 431/431 (slow tests included by default — single tier, no need to opt out unless something is wrong); `pytest -m 'not slow'` now correctly deselects the one ProcessPool test (430 passed, 1 deselected). Convention now visible in `pytest.ini` instead of buried in an env-var check. [easy] [testing] (resolved 2026-05-12)
- [x] <a id="f193"></a> **F193** F172(d) test exercises serial-vs-serial (subprocess monkeypatching infeasible). Add explicit docstring `# KNOWN COVERAGE GAP: parallel subprocess isolation prevents biased_partial injection` and consider an end-to-end synthetic test that uses an extremely short timeout to force the parallel path's biased_partial behavior naturally. (from 15-item bundle correctness COR-2) [easy] [testing] (added 2026-05-13) (resolved 2026-05-15)
- [x] <a id="f194"></a> **F194** Frontend test for `useInstanceIndicators` `isError`/`errorMessage` surfacing (F49 hook contract). Mock `useQueries` to return `[{ isError: true, error: ... }]`, assert hook returns `isError: true` + non-empty `errorMessage`. Plus a SubPane component test that the error overlay renders the message text. (from 15-item bundle kieran-typescript TG-02 + reliability gap) [easy] [testing] (added 2026-05-13) (resolved 2026-05-15)
- [x] <a id="f207"></a> **F207** Frontend test infra: 11 pre-existing failures in `WalkForwardPanel.test.tsx` (10) + `BotCard.test.tsx` (1) predate the 2026-05-15 bundles — `WalkForwardPanel` failures look like baseURL/MSW infra issues (`waitForWrapper` timing out on `findByText` after `fireEvent.click`); BotCard's single failure is uninvestigated. The journal repeatedly references these as "pre-existing" but they've persisted long enough to mask future regressions. Triage: stash all open changes, run `npm run test -- --run src/features/strategy/WalkForwardPanel.test.tsx -t "renders result table"` against a clean main, capture the exact failure mode, fix the test infra (likely a missing MSW setup or fake-baseURL assertion). [easy] [testing] (resolved 2026-05-15)
- [ ] <a id="f219"></a> **F219** Idle-rAF canary smoke test — F218 surfaced a class of bug (perpetual repaint loop while idle) that is invisible to unit tests, invisible to crisp screenshots, and only manifests as elevated CPU + naked-eye flicker. A trivial chrome-devtools-mcp probe — hook `requestAnimationFrame` for 500ms after chart mount with no user input, assert `< 5` calls — would have caught F218 the moment it landed. Real work: add to the overnight headless render probe (F51) once that exists, or as a standalone post-build check. Also worth: a MutationObserver canary that counts canvas-attribute mutations over 1s (zero expected). Both run in <2s. Catches future regressions to autoSize handling and any new v4→v5 lightweight-charts pattern drift. [easy] [testing] (added 2026-05-15)
