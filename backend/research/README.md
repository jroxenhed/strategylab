Standalone research instruments — one-shot analytics scripts decoupled from the FastAPI application.
No FastAPI imports; no runtime server dependency. Run each script directly with python3.
Outputs go to backend/data/turnaround/ alongside the validation data they analyse.
