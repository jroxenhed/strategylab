# Archived TODO items — shipped before 2026-05-12

These items were checked off in TODO.md before the cutoff and moved here to keep the active file lean. Bold ID cross-references in JOURNAL.md still work — items remain greppable across both files.

## B — Strategy Engine & Rules
- [x] <a id="b8b"></a> **B8b** Slippage auto-reset overrides "Use live spread" — the `useEffect` in StrategyBuilder.tsx that auto-applies `slipInfo` on ticker change includes `slippageSource` in its dep array, so it re-fires when user clicks "Use live spread". If `slipInfo.source === 'empirical'` (20+ fills), the effect immediately overrides the spread-derived value back to empirical. Fix: guard effect body with `if (slippageSource === 'default' || !slippageSource)` so it only auto-applies when source is still at default. [easy]
- [x] <a id="b20"></a> **B20** ~~Multi-timeframe confirmation~~ — superseded by B28 (regime rules as full rule sets evaluated on HTF). The regime system provides a more powerful version: arbitrary rules on any higher timeframe gate or direct entries, rather than just a single confirm signal.

## F — Architecture & housekeeping
- [x] <a id="f28e"></a> **F28e** `BotConfig.direction` missing validator — replaced `@field_validator('direction')` pattern with a shared `DirectionField = Annotated[Literal['long', 'short'], BeforeValidator(str.lower)]` type alias in `models.py`. Applied to both `StrategyRequest.direction` and `BotConfig.direction`. Removed the duplicate `@field_validator` from `UpdateBotRequest` in `routes/bots.py`. Closes the F28 validation pass across all models. [easy] (from F28d) [arch]
- [x] <a id="f41"></a> **F41** BotDetail.state type mismatch (F32 follow-up) — resolved by aligning the type to the runtime defensive code: `state: BotState` → `state?: BotState`. All 9 `detail?.state?.` chains in BotCard.tsx already used double optional chain, so no consumer changes needed. The type now under-promises vs the actual API contract (backend always populates `state`); revisit if the morning review prefers reverting the type and dropping the second `?.` in BotCard. (PR #30 build 22) [arch]
- [x] <a id="f58"></a> **F58** Duplicate equity-snapshot append+cap logic in `exits.py` — added `BotState.append_equity_snapshot(value)` helper (mirrors F56's `append_slippage_bps` pattern), routed both exit paths through it. The cap-at-500 and `{time, value}` shape now live in one place. (F122 bundle) [easy] [arch]
- [x] <a id="f81"></a> **F81** Shared `normalize_symbol` helper + `SymbolField = Annotated[str, BeforeValidator(normalize_symbol)]` in `models.py`. Strip + upper + reject empty, reject >20 chars, regex `^[A-Z0-9][A-Z0-9.\-]{0,19}$`. Wired into `routes/quote.py` (path-param + per-symbol), `routes/trading.py` (`WatchlistRequest`, `BuyRequest`, `SellRequest`). `ScanRequest`/`StrategyRequest`/`BotConfig`/quick-backtest models still pending under F95. (Build 23) [arch]
- [x] <a id="f99"></a> **F99** `SymbolField` now `Annotated[str, BeforeValidator(normalize_symbol), Field(min_length=1, max_length=20, examples=["AAPL","BRK.B","SPY"], json_schema_extra={"pattern": r"^[A-Z0-9][A-Z0-9.\-]{0,19}$"})]`. JSON schema now exposes pattern + min/max + examples (was just `{"type": "string"}`). Empirically verified Pydantic v2 ordering: `BeforeValidator` runs first, then `Field` constraints — lowercase/whitespace inputs still normalize-then-validate (zero behavior regression). Key gotcha discovered: `Field(pattern=...)` does NOT surface in JSON schema when preceded by `BeforeValidator`; the pattern must be injected via `json_schema_extra` instead. 85/85 pass across `test_models.py + test_trading.py + test_backtest_quick.py`. Closes the F95+F100+F145 SymbolField arc. [easy] [arch]
- [x] <a id="f118"></a> **F118** Post-PR-32 protocol patch bundle — three coordinated edits landed: (1) per-run task cap raised from 3 → 5 with "file-independent, ceiling-not-target" framing in `docs/overnight-builder-prompt-patch.md` §Guard Rails; (2) postmortem template gained a `## Run metrics` section (Shipped / Surfaced / Multiplier / Cumulative open F-items / Reviewer roster) so the new-items-per-build multiplier is visible over time; (3) morning ce:review roster trimmed from 4 personas to 2 (kept agent-native + reliability; dropped adversarial + security) in CLAUDE.md, with PR #32 evidence cited inline. Memory files updated to match. [arch]
- [x] <a id="f136"></a> **F136** Severity-graded review tiers — overnight builder review depth now scales with risk (Tier A = 0 personas for `[easy]` bundles <100 lines; Tier B = 2 personas for medium; Tier C = 4-6 panel for hard/architectural/contract changes). Contract-surface and aggregate-bundle overrides promote tier. Morning calibration stays at 2 personas regardless. Validated against F119+F122+F125 (24 items, 0 personas, 0 regressions, user-confirmed "smoother than ever"). Shipped in commit 23c69f9 — originally tagged F127 in commit message, then renumbered to F136 here after PR #33 (build 25) merged with a colliding F127 for batch deadline. CLAUDE.md and `docs/overnight-builder-prompt-patch.md` updated. (commit 23c69f9; resolved 2026-05-11) [arch]
- [x] <a id="f37"></a> **F37** `source` param allowlist on quote endpoints — both `GET /api/quote/{ticker}` and `POST /api/quotes` now reject unknown `source` via `if source not in get_available_providers(): raise HTTPException(400, "Invalid source")` before any fetch. Closes the silent-swallow provider-enumeration vector. (Build 23 — partial: backtest / indicators / ohlcv routes tracked under F94.) [hardening]
- [x] <a id="f38"></a> **F38** Log-injection via ticker on quote routes closed — both routes now run input through `normalize_symbol()` which enforces `^[A-Z0-9][A-Z0-9.\-]{0,19}$`, rejecting newlines/control chars before they reach the f-string detail or any log path. (Build 23 — backtest/bot ticker fields still plain `str`, tracked under F95.) [hardening]
- [x] <a id="f39"></a> **F39** Batch quote endpoint silent null — added `error: str | None` to per-symbol response. Validation rejection uses `"invalid symbol"`; HTTPException path uses `e.detail` (avoids leaking `"500: …"` status prefix); generic Exception path uses `str(e).strip() or "no data"`. Frontend `Quote` interface gained optional `error` field, surfaced as `title` tooltip on the loading placeholder. (PR #29) [hardening]
- [x] <a id="f42"></a> **F42** `eval_rules()` Literal not runtime-enforced — added `if logic not in ('AND', 'OR'): raise ValueError(...)` guard at function entry. Invalid logic now surfaces at runtime instead of silently falling back to OR semantics. (F119 bundle) [easy] [hardening]
- [x] <a id="f43"></a> **F43** Log injection via ticker symbols — fully closed by F95. Final audit (F95 security review SEC-02, 0.75): every `_log_trade()` call site now reads a SymbolField-validated source: `BuyRequest.symbol` / `SellRequest.symbol` (F85), `scan_signals` symbol var (F95 via `ScanRequest.symbols: list[SymbolField]`), `bot_runner` `cfg.symbol` (F95 via `BotConfig.symbol`). `QuickBacktestRequest.ticker` + `BatchQuickBacktestRequest.symbols` are SymbolField but never write to the journal — the original F43 entry overcounted those as live holes. [easy] [hardening]
- [x] <a id="f45"></a> **F45** `POST /api/quotes` invalid-entry symbol now truncated to 20 chars and stripped of anything outside `[A-Z0-9.-]` before echoing back, so multi-MB strings + control chars can't ride into the response. (Build 23 side-effect close) [hardening]
- [x] <a id="f52"></a> **F52** `watchlist.json` write is not atomic — `save_watchlist` now uses `tempfile.NamedTemporaryFile` + `os.replace()` with cleanup-on-exception, mirroring the `bot_manager.save()` pattern. (PR #29) [hardening]
- [x] <a id="f53"></a> **F53** `.env` write in providers route is not atomic — `_persist_env` now uses `tempfile.NamedTemporaryFile` + `os.replace()` with cleanup-on-exception. Module-level `os`/`pathlib`/`tempfile` imports replaced the prior function-local imports. (PR #29) [hardening]
- [x] <a id="f54"></a> **F54** `/scan` auto-execute `_log_trade` calls have no `direction` kwarg — both call sites in `routes/trading.py` now pass explicit `direction="long"`. `ScanRequest` has no `direction` field (scan is long-only today); making the default intentional documents the assumption and prevents silent "long" logs when scan eventually grows short support. (F119 bundle) [easy] [hardening]
- [x] <a id="f56"></a> **F56** `BotState.slippage_bps` list is unbounded — added `BotState.append_slippage_bps(bps)` helper that appends + caps at 1000 entries. Replaced 4 raw `.append()` sites (`bot_manager.py`, `bot_runner.py`, `exits.py`, `regime.py`) with the helper so the cap can't drift across exit paths. (F119 bundle) [easy] [hardening]
- [x] <a id="f57"></a> **F57** `bot_manager.save()` uses bare `except:` — narrowed to `except Exception:` so `KeyboardInterrupt` / `SystemExit` propagate as intended. (F119 bundle) [easy] [hardening]
- [x] <a id="f60"></a> **F60** `routes/indicators.py:112` swallows compute errors per-indicator with no logging — added module logger and `logger.exception(...)` calls at both per-instance compute-failed branches (htf path + main path). Server logs now distinguish pandas shape mismatch from missing-data. (F119 bundle) [easy] [hardening]
- [x] <a id="f66"></a> **F66** `_fetch_dedup_locks` key shape inconsistent with `_fetch_cache` key — updated to `(ticker.upper(), interval, source, extended_hours)`. Chose the stable-subset shape (not full cache key with start/end) deliberately to keep the dict bounded by meaningful discriminators while still preventing different-source callers from over-serializing on the same lock. (F122 bundle) [easy] [hardening]
- [x] <a id="f67"></a> **F67** Broker quote fetch errors swallowed silently in `routes/trading.py` — added `logger.exception(...)` at 6 sites: `/buy` `get_latest_price` failure, `_clear_bot_entry_state`, `/sell` pre-cancel stop-loss, scan auto-execute SELL_FAILED branch, scan per-symbol error fallback, and `/performance` backtest fallback. Skipped 3 intentional broad catches (polling break, atomic-write cleanup × 2). (F122 bundle) [easy] [hardening]
- [x] <a id="f69"></a> **F69** `WatchlistRequest.symbols` unbounded (disk-fill / OOM DoS) — `Field(max_length=500)` plus a `field_validator` that explicit-checks `len(v) > 500` first (belt-and-suspenders against any Pydantic version drift), strips whitespace, drops empties, enforces 20-char per-symbol cap, and uppercases. Boundary tests at 500 + 21 + 20 chars added. (PR #30 build 22) [hardening]
- [x] <a id="f70"></a> **F70** `_persist_env` lost-update race closed — module-level `_env_lock = threading.Lock()` wraps the entire read-modify-write block in `routes/providers.py`. Concurrent in-process callers for different keys now serialize cleanly. Cross-process writers (multiple workers, hand-edits) remain unprotected — documented in the function docstring. (Build 23) [hardening]
- [x] <a id="f72"></a> **F72** `POST /api/quotes` lacks Pydantic `response_model` — added `QuoteResult(BaseModel)` with `symbol/price/change_pct/error` (None defaults) and annotated the route with `response_model=list[QuoteResult]`. OpenAPI now documents the shape. Test contract updated: success rows now include `error: None` (the 4-field shape is uniform across success and error paths). (F125 bundle) [easy] [hardening]
- [x] <a id="f73"></a> **F73** Orphan `.tmp` cleanup on startup — new `backend/fileutil.py` with `cleanup_orphan_tmps(directories: list[Path]) -> int`. Wired into the FastAPI lifespan before `init_ibkr()`. Scans `data/` and `backend/` (covers all four atomic-write sites: journal/bot_manager/watchlist/providers env), unlinks `*.tmp` >1h old. Best-effort, OS errors logged at WARNING. (F125 bundle) [easy] [hardening]
- [x] <a id="f75"></a> **F75** Sanitize `/api/quotes` exception leak — `get_quote` 500 detail is now a fixed `"quote fetch failed"` with `logger.exception()`; `get_quotes` batch error rows classify by inner HTTPException status: 404 passes through the safe `"No data for X"` message, anything else (500+) maps to fixed `"fetch failed"`. 2 stale tests (`test_fetch_exception_returns_error_field`, `test_fetch_empty_exception_message_falls_back`) updated to assert the new sanitized contract. (F125 bundle) [easy] [hardening]
- [x] <a id="f76"></a> **F76** `_persist_env` TOCTOU on `.env` existence check closed — replaced `if exists(): read_text()` with `try: read_text() except FileNotFoundError`. Also tracks `existed: bool` so the subsequent `shutil.copymode` only fires when the source genuinely existed at read time (avoids conflating "first-time write" with "external chmod fail"). (Build 23) [hardening]
- [x] <a id="f77"></a> **F77** Defensive newline guard for `_persist_env(key, value)` — added `if "\n" in key/value or "\r" in key/value: raise ValueError(...)` at function entry. Closes the env-injection vector if a future caller forwards user data unsanitized. (F119 bundle) [easy] [hardening]
- [x] <a id="f82"></a> **F82** `ScanRequest.symbols` validation closed — F95 changed `list[str]` to `list[SymbolField]` (per-element normalize + regex via BeforeValidator) and the F95 fixer added F93's dedup pattern (`@field_validator('symbols')` returning `list(dict.fromkeys(v))`). Per-symbol cap is enforced by SymbolField's 20-char regex; list-length cap deferred (not in F82's original ask; ScanRequest's amplification path is dedup-bounded now). [easy] [hardening]
- [x] <a id="f85"></a> **F85** Symbol allowlist regex closed — `^[A-Z0-9][A-Z0-9.\-]{0,19}$` enforced inside `normalize_symbol` so every site using `SymbolField` (or calling the helper directly) inherits it. Tightened over the original spec to require an alphanumeric leading char so `..`, `.env`, `-A` are also rejected. (Build 23) [hardening]
- [x] <a id="f86"></a> **F86** HTTP body size limit middleware — new `backend/middleware.py` `BodySizeLimitMiddleware` (pure ASGI). 1 MB default cap, override via `STRATEGYLAB_MAX_BODY_BYTES`. Wired into `main.py` after CORS so it's outermost. Adversarial-hardened in-PR: rejects duplicate Content-Length headers and forces slow-path body counting when Transfer-Encoding is present (closes CL=0+chunked and TE+CL coexistence smuggling). Replay-receive returns synthetic empty body on second call so downstream re-reads don't hang. (PR #32 build 24) [hardening]
- [x] <a id="f87"></a> **F87** Empty-list watchlist silently wipes saved state — closed by F104. `_validate_symbols` now raises `ValueError("symbols must contain at least one non-empty entry")` so POST `{"symbols": []}` / `{"symbols": ["", "  "]}` returns 422 instead of silently overwriting the on-disk file. Test `test_watchlist_rejects_all_empty_symbols` pins the contract. (Build 25) [hardening]
- [x] <a id="f88"></a> **F88** GET `/api/trading/watchlist` filter-on-read — disk entries now run through the same `normalize_symbol()` + regex used by writes, but in forgiving mode: invalid/empty/non-string entries are dropped with a single `logger.warning("watchlist read-time filter dropped %d invalid entries", N)`. Dedup via `dict.fromkeys` mirrors F93. Closes the "reads OK, writes 422" trap surfaced by F95's SymbolField rollout. Write-strict / read-tolerant asymmetry preserved (no cap on reads). (F125 bundle) [easy] [hardening]
- [x] <a id="f91"></a> **F91** BatchQuickBacktestRequest.symbols length cap parity — applied the F69 pattern to `BatchQuickBacktestRequest.symbols`: 500-entry list cap (custom message) + per-element `normalize_symbol` validation, all-empty list rejected with 422. `QuickBacktestRequest.ticker` switched to `SymbolField`, removing the duplicate inline `validate_ticker`. Direction validators on both classes also collapsed to the shared `DirectionField` (kieran-python review converged on the same idiom). Closes the OOM vector on POST /api/backtest/quick/batch. (PR #32 build 24) [hardening]
- [x] <a id="f92"></a> **F92** Symlink-safe `_persist_env` — call `env_path = env_path.resolve()` at function entry so `os.replace` lands on the real file, not the symlink. (F119 bundle) [easy] [hardening]
- [x] <a id="f93"></a> **F93** Symbol dedup in WatchlistRequest validator — `_validate_symbols` now returns `list(dict.fromkeys(cleaned))` to drop duplicates while preserving first-occurrence order. Closes the 500x scanner amplification vector. (F119 bundle) [easy] [hardening]
- [x] <a id="f94"></a> **F94** F37-style `source` allowlist on the remaining data routes — extracted shared `require_valid_source(source)` helper in `shared.py` (lowercases + 400s on unknown), refactored `routes/data.py:get_ohlcv`, `routes/indicators.py:post_indicators`, `routes/backtest.py:run_backtest`, and both quote routes (`get_quote` / `get_quotes`) to use it. Closes the case-sensitivity gap on the boundary check (`source=YAHOO` now normalizes to `yahoo` instead of falling through to `_fetch`'s separate 'Unknown data source' branch). 5 sites → 1 helper. (PR #32 build 24) [hardening]
- [x] <a id="f95"></a> **F95** SymbolField applied to remaining ticker/symbol fields — `StrategyRequest.ticker` (`models.py`), `BotConfig.symbol` (`bot_manager.py`), `ScanRequest.symbols: list[SymbolField]` + dedup `@field_validator` (`routes/trading.py`), `PerformanceRequest.symbol` (`routes/trading.py`). Closes F43 + subsumes F82. Tier C full orchestrator: 6 personas → cross-converged P1 (COR-001: `migrate_rule` was outside the try, would have aborted all bots on one bad rule entry) + cross-converged P2 (K1+COR-002: BotConfig ValueError mislabeled as "invalid symbol" — split into normalize-then-construct named-control-flow) + SEC-01 (bot_id %s → %r to prevent log-line injection) + M2 (4 dead `.upper()` calls on SymbolField values removed). [medium] [hardening]
- [x] <a id="f100"></a> **F100** `BotManager.load()` migration for F95 — `normalize_symbol(raw_symbol)` pre-pass + named-control-flow try-split: (a) first try wraps normalize + migrate_rule loop, catches `ValueError` (invalid symbol surface) → WARNING + continue; (b) second try wraps `BotConfig(**cfg_dict)`, catches `pydantic.ValidationError` (any other field rejection) → distinct WARNING + continue; broad `except Exception` retained for unexpected errors with `logger.exception`. All `bot_id` logging via `%r` (newline-safe). 14 new tests in `test_bot_state.py` `TestBotManagerLoadMigration` cover normalize, skip-invalid, mixed entries, missing-symbol-key, None-symbol, and the broad-except fallback. [medium] [hardening]
- [x] <a id="f101"></a> **F101** `.env` permission preservation hardening in `_persist_env` — now tracks `copy_mode_failed` and explicitly `os.chmod(env_path, 0o600)` after `os.replace` whenever (a) the file is new or (b) `copymode` failed. Closes the umask-default-after-failure leak; preserves existing modes on the success path. (F122 bundle) [easy] [hardening]
- [x] <a id="f102"></a> **F102** Cap `buy_rules` / `sell_rules` list length on `QuickBacktestRequest` and `BatchQuickBacktestRequest` — `Field(max_length=100)` applied to both lists on both request models in `backend/routes/backtest_quick.py`. Bounds O(n_rules × n_bars) compute per backtest below the body-cap (F86) ceiling. 6 new tests in `test_backtest_quick.py` covering reject-101 (buy/sell × quick/batch) and accept-exactly-100 (quick + batch boundary). (Build 25) [hardening]
- [x] <a id="f103"></a> **F103** `trailing_stop: Optional[dict]` on `QuickBacktestRequest` / `BatchQuickBacktestRequest` → `Optional[TrailingStopConfig]`. Validation now happens at request-entry instead of being deferred to the downstream `StrategyRequest` re-validation. (F122 bundle) [easy] [hardening]
- [x] <a id="f104"></a> **F104** Harmonize empty-list-after-strip between `WatchlistRequest._validate_symbols` (F69, silently returned `[]`) and `BatchQuickBacktestRequest._validate_symbols` (F91, raises 422). Watchlist validator now raises 422 with the same "symbols must contain at least one non-empty entry" message. Side-effect: closes F87 silent-wipe of watchlist.json. Parametrized test pins 4 empty-list shapes ([], [""], ["   "], ["", "  ", "\t"]). (Build 25) [hardening]
- [x] <a id="f111"></a> **F111** `BodySizeLimitMiddleware` rejects `Content-Length` with leading/trailing whitespace — added a pre-check before the duplicate-CL guard: `for v in cl_values: if v.strip() != v: return 400`. Closes the `b' 50'` / `b'50 '` smuggling polish. (F125 bundle) [easy] [hardening]
- [x] <a id="f112"></a> **F112** `Transfer-Encoding: identity` no longer forces slow path — TE detection now compares `value.strip().lower() != b"identity"` before setting `has_transfer_encoding = True`. Identity is the no-op encoding (RFC 7230 §4); the Content-Length fast path stays active. (F125 bundle) [easy] [hardening]
- [x] <a id="f114"></a> **F114** `MonteCarloRequest.pnls` length cap — added `Field(min_length=2, max_length=50000)` on `pnls`. Pins the existing runtime `< 2` guard at the schema level and caps the inner-loop work at 50k × 5000 sim = 250M iterations (vs unbounded). (F122 bundle) [easy] [hardening]
- [x] <a id="f115"></a> **F115** Provider exception strings sanitized across 5 routes — `data.py`, `indicators.py`, `backtest.py`, `quote.py` (covered by F75), and per-symbol rows in `backtest_quick.py`. Each replaces `detail=str(e)` / `error=str(e)` with a fixed message (`"data fetch failed"` / `"indicator compute failed"` / `"backtest failed"` / `"quick-backtest failed"`) and adds `logger.exception(...)` server-side. Loggers added where missing. F126 filed for the remaining leak vector in `routes/trading.py:84` (broker errors). (F125 bundle) [easy] [hardening]
- [x] <a id="f124"></a> **F124** `_fetch_dedup_locks` eviction policy — `_evict_cache` in `shared.py` now also drops dedup-lock entries whose `(ticker, interval, source, extended_hours)` key has no remaining cache entry. The cleanup runs under `_fetch_dedup_locks_meta`, after cache mutations. Bounds growth without forcing a new periodic timer. [easy] [hardening]
- [x] <a id="f126"></a> **F126** `GET /api/trading/account` exception leak closed — replaced `detail=f"Broker API error: {e}"` with fixed `"Broker API unavailable"` and upgraded the existing `logger.error(...)` to `logger.exception("get_account failed")` so the full trace stays server-side. Parity with F115 across the remaining trading route. [easy] [hardening]
- [x] <a id="f128"></a> **F128** Rule-list caps applied to 17 sibling fields via shared type alias — introduced `BoundedRuleList = Annotated[list[Rule], Field(max_length=_RULE_LIST_CAP)]` and `OptionalBoundedRuleList = Annotated[Optional[list[Rule]], Field(default=None, max_length=_RULE_LIST_CAP)]` in `models.py` (constant `_RULE_LIST_CAP = 100`). Replaced 17 field declarations: `StrategyRequest` (6: 2 BoundedRuleList + 4 OptionalBoundedRuleList), `BotConfig` (6: same shape), `ScanRequest` (2 BoundedRuleList), `PerformanceRequest` (2 BoundedRuleList). `RegimeConfig.rules` keeps its smaller cap=50 (different value, intentionally not aliased). Required fields stay required — F102's no-default contract preserved (correctness + kieran reviewers caught the early draft's `default_factory=list` regression). 46 new boundary tests across `test_models.py`/`test_bot_state.py`/`test_trading.py` assert `accept-100 → len + first/last element` and `reject-101 → ValidationError(match="too_long")`. (build 26 orchestrator workflow: 3 parallel sonnet implementers + 2 parallel sonnet reviewers + 1 sonnet fixer + Opus orchestrator) [medium] [hardening]
- [x] <a id="f129"></a> **F129** `Rule.value` lookback DoS closed via consumer-site clamp — new `_clamp_lookback(value, default)` helper in `signal_engine.py` returns `max(0, min(int(value), 500))` (caps at 500 AND floors at 0 — review-flagged that negative values would silently OOB into pandas). Applied at the 3 lookback consumer sites (`rising_over`, `falling_over`, `turns_up`/`turns_down` with `max(1, ...)` outer); threshold sites stay unclamped (Rule.value is also used as a price/indicator threshold where high values are legitimate — BRK.A is ~$750k). Option B over Option A per explore: `Field(le=500)` on Rule.value would break threshold rules on high-priced assets. [medium] [hardening]
- [x] <a id="f130"></a> **F130** Per-family indicator-spec cap added to `compute_indicators` — new `_INDICATOR_FAMILY_CAP = 20` constant + `_assert_family_cap(name, specs)` helper raise `ValueError(...)` when distinct-spec count exceeds the cap. Applied to all 7 families: RSI, MA, BB, ATR, volume SMA, stochastic, ADX. Per-family scope (not global): 10 MA + 10 RSI + 10 BB (30 total) still passes; 21 of any single family fails. Both `routes/backtest.py` and `routes/backtest_quick.py` now catch `ValueError` and surface as 400 (was 500 "backtest failed" pre-fix per cross-reviewer P1). 11 new tests in `test_signal_engine.py` cover the helper + 7 families + boundary + negative-input floor. [medium] [hardening]
- [x] <a id="f131"></a> **F131** Single-coroutine-per-bot invariant documented — `# single-coroutine-per-bot: do not insert await between append and slice-cap.` added to `BotState.append_slippage_bps` and `append_equity_snapshot` in `bot_manager.py`. Same comment also applied to the new `append_activity_log` (F132). [easy] [hardening]
- [x] <a id="f132"></a> **F132** `BotState.append_activity_log(entry)` extracted — insert(0)+cap(200) moved from `bot_runner._log` into a new `BotState` helper, with the runner now calling `self.state.append_activity_log(entry)`. Mirrors F121/F123 pattern; closes the last unbounded-growth gap on `BotState`. [easy] [hardening]
- [x] <a id="f133"></a> **F133** `scan_signals` error-path leak closed — per-symbol error row now reports `"error": "scan failed"` instead of `str(e)`; the existing `logger.exception("scan failed for %s", symbol)` preserves the full trace server-side. Final piece of the F115/F126 sanitization sweep across user-facing trading routes. [easy] [hardening]
- [x] <a id="f135"></a> **F135** `SignalScanner.tsx` save-watchlist 422 now surfaced — added `saveError: string | null` state; rewrote `handleSave` to `try { await saveWatchlist(list) } catch (e) { setSaveError(apiErrorDetail(e, 'Failed to save watchlist')) }`; wrapped the Save button in a column-flex `<div>` rendering `<span style={{ color: '#f85149', fontSize: 11 }}>{saveError}</span>` below when set. F104 visibility gap closed. Visual smoke not exercised in-session — see F140. [easy] [hardening]
- [x] <a id="f137"></a> **F137** `shared.py` IBKR `_fetch` exception leak closed — `detail=f"IBKR fetch failed: {e}"` → fixed `"IBKR fetch failed"` + `logger.exception("IBKR fetch failed for %s", ticker)`. Parity with F115/F126/F133 sanitization sweep — `_fetch` was the last remaining `detail=f".*{e}"` site. (build 27) [easy] [hardening]
- [x] <a id="f138"></a> **F138** Dedup-lock high-watermark trigger added — new module-level constant `_DEDUP_LOCKS_HIGH_WATERMARK = 200` in `shared.py`. `_fetch` now fires `_evict_cache()` when EITHER `_fetch_cache` is at cap OR `_fetch_dedup_locks` has grown past the watermark, so low-throughput long-running deployments with rotating tickers don't accumulate locks indefinitely. Picked option (b) over (a) — avoids the per-call eviction scan overhead while still bounding lock growth. New `tests/test_shared_eviction.py` (3 tests) pins orphan-lock pruning + live-lock retention + watermark constant. (build 27) [easy] [hardening]
- [x] <a id="f141"></a> **F141** `UpdateBotRequest` PATCH cap closed — `routes/bots.py:49-58` 6 rule fields swapped from bare `Optional[list]` to F128's `OptionalBoundedRuleList`. Cap now fires at request-validation time (Pydantic field-level, before `model_dump`), not after the BotConfig reconstruct. Also tightens the inner type from `list` to `list[Rule]` for consistency with BotConfig. New `tests/test_routes_bots.py` (14 tests): per-field reject-101 with `match="too_long"`, per-field accept-exactly-100 (asserts len + first/last element survives), None-default preservation, empty-list passthrough. [easy] [hardening]
- [x] <a id="f145"></a> **F145** `SymbolList = list[SymbolField]` extracted in `models.py` and applied to all three sites: `ScanRequest.symbols` (`routes/trading.py`, was `list[SymbolField]`), `WatchlistRequest.symbols` (`routes/trading.py`, was `list[str]` + custom validator), `BatchQuickBacktestRequest.symbols` (`routes/backtest_quick.py`, was `list[str]` + custom validator). Watchlist + batch validators refactored: mode='before' now only drops empty/whitespace entries + holds the custom "too many symbols (max 500, got N)" message; per-element normalize+regex moves to `SymbolField`'s `BeforeValidator(normalize_symbol)`; watchlist dedup hoisted to a `mode='after'` validator since SymbolField runs between mode='before' and mode='after'. `normalize_symbol` import removed from `routes/backtest_quick.py` (no longer used after the refactor). 49/49 tests in `test_trading.py` + `test_backtest_quick.py` pass against the new flow. (build 27) [easy] [hardening]
- [x] <a id="f36"></a> **F36** Custom date preset icon swapped — `Sidebar.tsx:302` now renders `'…'` instead of the `'⚙'` gear glyph for the `custom` DatePreset button. Matches the 1-character visual rhythm of the sibling buttons ('D', 'W', 'M', 'Q', 'Y'); no longer reads as "settings". [easy] [polish]
- [x] <a id="f47"></a> **F47** Banner section-header comments removed — `SubPane.tsx` (`// Effect 1:` / `// Effect 2:` / `// Effect 3:`) and `OptimizerPanel.tsx` (`// --- Types ---` / `// --- Constants ---` / `// --- Component ---` / `// --- Styles ---`) all stripped. The code structure is self-evident without the banners. [easy] [polish]
- [x] <a id="f48"></a> **F48** OptimizerPanel local `steps` → `stepsN` rename — two scopes that previously declared `const steps = Math.max(1, parseInt(p.steps) || 5)` (estimatedCombos useMemo + requestParams.map callback) now declare `const stepsN = ...`. Matches the existing validation block's `stepsN` naming. The form-input field `p.steps` on `ParamRow` is preserved. [easy] [polish]
- [x] <a id="f78"></a> **F78** Watchlist error indicator added — `WatchlistPanel.tsx` now branches its price cell: `q?.price` populated → price+change (unchanged); else if `q?.error` set → red bold `!` glyph with the existing tooltip (new `styles.error`: `color: var(--accent-red), fontWeight: 700, fontSize: 11`); else loading `...` placeholder. Permanently-failing symbols now visually distinct from still-loading ones. [easy] [polish]
- [x] <a id="f109"></a> **F109** `BodySizeLimitMiddleware` class docstring trimmed — was 4 lines duplicating the module docstring's "Pure ASGI vs BaseHTTPMiddleware" rationale, now 2 lines stating what the class does + lifespan-interaction note. Threat model stays in the module docstring. (F125 bundle) [easy] [polish]
- [x] <a id="f110"></a> **F110** ASGI type annotations added — imported `ASGIApp, Scope, Receive, Send` from `starlette.types`; annotated `__init__`, `__call__`, and `_reply`. Type-only, runtime identical. (F125 bundle) [easy] [polish]
- [x] <a id="f116"></a> **F116** `BodySizeLimitMiddleware` non-HTTP scope passthrough — added a 3-line comment at the `if scope["type"] != "http":` guard documenting that WebSocket/lifespan scopes intentionally bypass the body cap, and that future WS routes need a separate frame-size limit. (F122 bundle) [easy] [polish]
- [x] <a id="f40"></a> **F40** `TestFetchOhlcvDedup` no longer relies on `time.sleep` — both tests now use a `threading.Event` that the executor thread waits on, and the main coroutine polls `call_count` to confirm the Future is pending before releasing. Deterministic under high system load. [easy] [testing]
- [x] <a id="f46"></a> **F46** `TestFetchOhlcvDedup` test isolation — already addressed: `setUp` calls `shared._async_ohlcv_futures.clear()` AND registers it via `self.addCleanup(...)`, which is per-test and runs even on setUp failure. Equivalent to the requested setUp/tearDown pair, with the more robust unittest idiom. [easy] [testing]
- [x] <a id="f68"></a> **F68** F52/F53 round-trip + crash-recovery tests — `backend/tests/test_trading.py` (7 tests, includes 422 caps, boundary 500 + 20-char, strip/upper/empty-filter, round-trip, and cleanup-on-failure that pre-creates `{"symbols": ["ORIG"]}` and asserts content unchanged after the failed `os.replace`). `backend/tests/test_routes_providers.py` (4 tests, _persist_env create/update/append/cleanup). `_persist_env` got an `env_path: Optional[pathlib.Path] = None` test-injection parameter. (PR #30 build 22) [testing]
- [x] <a id="f80"></a> **F80** Two-tier review architecture — codified 2026-05-10 (build 22 morning pass). **Decisions made:** (a) `ce:review` is interactive-session only — three skill-name candidates failed across builds 20-22; the routine env doesn't load the plugin registry. Stop the probe ceremony. Manual Task-tool dispatch is canonical for overnight, not "fallback" — verified across 11+ runs. (b) Builder roster trimmed 9 → 4-6 — always-on: correctness, testing, adversarial, security. Conditional: kieran-python/typescript by file type, reliability for async/persistence diffs, project-standards for tracking-file diffs, maintainability for architecture diffs. Build 22 showed 9-persona heavy duplication produced false confidence (5 reviewers piled on a wrong "Pydantic max_length reliability" defense, which morning pass found was dead code). (c) Morning calibration roster fixed at 4 — adversarial, agent-native, security, reliability. Drops correctness/testing/maintainability/kieran-* (heavy overlap with builder roster) and learnings/project-standards (lower per-run impact). (d) Two tiers stay structurally distinct — coverage (builder) vs calibration (morning). Don't consolidate. CLAUDE.md updated; `docs/overnight-builder-prompt-patch.md` skill probe removed. (from 2026-05-10 PR #30 morning ce:review pass) [infra]
- [x] <a id="f107"></a> **F107** `BodySizeLimitMiddleware` HEAD/OPTIONS/DELETE coverage — added `test_non_body_method_bypasses_cap_even_with_huge_content_length` (parametrized over the 3 methods) driving the ASGI middleware directly with `Content-Length: 10000` against `max_bytes=100`. Asserts the downstream handler is called and 413 is NOT emitted. [easy] [testing]
- [x] <a id="f108"></a> **F108** `STRATEGYLAB_MAX_BODY_BYTES` env-var parsing tested — extracted `parse_max_body_env(value)` from `main.py` into `middleware.py` (raises `ValueError` on invalid input so callers can log+fallback uniformly). 4 new tests: returns default on None/empty, parses valid positive integers, raises on non-numeric, raises on non-positive. `main.py` keeps the existing logger.warning + DEFAULT_MAX_BYTES fallback. [easy] [testing]
- [x] <a id="f120"></a> **F120** Stale-test failures cleared — `test_ohlcv_rejects_unknown_source` now asserts `"Invalid source"` (route layer's `require_valid_source` since F94). `test_backtest_costs.py:_req()` defaults pin IBKR Fixed rates (0.0035/0.35) so the 3 commission tests stay independent of the model's flipped defaults (Alpaca commission-free 0.0/0.0). `test_empty_journal_returns_default` expected dict expanded to include `live_spread_bps` / `half_spread_bps` (None when no broker is configured). [easy] [testing]
- [x] <a id="f121"></a> **F121** Unit-test `BotState.append_slippage_bps` cap — new `backend/tests/test_bot_state.py` (3 tests): 1001 appends → length 1000, first survivor=1.0 (not 0.0), last=1000.0; 2dp rounding pinned; below-cap passthrough sanity. (Build 25) [testing]
- [x] <a id="f123"></a> **F123** Unit-test `BotState.append_equity_snapshot` cap and shape — same `test_bot_state.py` (3 tests): 501 appends → length 500, first survivor=1.0, last=500.0; `{time, value}` shape with ISO-8601 UTC `time` (`utcoffset().total_seconds() == 0.0`) and 2dp `value`; below-cap order preservation. (Build 25) [testing]
- [x] <a id="f134"></a> **F134** F104 no-pre-seed variant added — `test_watchlist_422_does_not_create_file_when_absent` (parametrized over the same 4 empty-list shapes as the sibling) asserts `not watchlist_file.exists()` before AND after the POST. Pins the invariant that a 422 must not touch disk, complementing the existing "pre-seeded file unchanged" assertion. [easy] [testing]
- [x] <a id="f139"></a> **F139** Chronic event-loop contamination closed via `backend/tests/conftest.py` autouse fixture. Diagnosis (haiku explore): `asyncio.run()` in prior tests (test_bot_runner, test_body_size_middleware, etc.) closes its internal event loop and leaves the main thread with no default. When `test_ibkr_provider_submit_market_order` later runs, `broker.py:570` lazy-imports `ib_insync` → `eventkit/util.py:24` calls `asyncio.get_event_loop()` at module init → RuntimeError on Python 3.10+ (3.12 here). Fix: `_f139_ensure_event_loop` autouse fixture creates+sets a fresh loop per test, closes + `set_event_loop(None)` on teardown. Morning review (correctness + kieran-python, both P1 ~0.72) converged on dropping the original "leave closed loop visible" teardown — the rationale didn't hold up since pytest-asyncio 1.3.0 manages its loop via `event_loop_policy`, not the thread default. **Full backend pytest now 372/372 — first fully-clean run since the F120/F40/F46/F107/F108/F134 sweep started.** [medium] [testing]
- [x] <a id="f142"></a> **F142** `_STUB_RULE` extracted to conftest.py — `tests/conftest.py` now exports canonical `_STUB_RULE = {"indicator": "rsi", "condition": "above", "value": 50}` plus `_STUB_RULES_100 = [_STUB_RULE for _ in range(100)]`. `test_models.py`, `test_bot_state.py`, `test_trading.py`, `test_backtest_quick.py` all replaced their local stubs with `from tests.conftest import _STUB_RULE`. `test_models.py` had three inline assertions tied to the divergent `'price'`/`value=1` shape; updated to match canonical `'rsi'`/`value=50`. F106's future allowlist validator will now affect all four files uniformly. [easy] [testing] (resolved 2026-05-12)
- [x] <a id="f143"></a> **F143** `routes/bots.py` PATCH HTTP-level smoke tests added — `tests/test_routes_bots.py` extended with `client_with_stub_manager` fixture (TestClient + monkeypatched MagicMock `bot_manager`). 13 new tests: per-rule-field reject-101-via-HTTP (parametrized × 6 fields, asserts 422 + `"too_long"` in body), per-rule-field accept-100-via-HTTP (× 6, asserts 200 + `{"ok": True}`), plus the 503 guard test for `bot_manager is None`. Also consolidated the duplicate `_RULE_FIELDS` parametrize list into a single module constant (correctness review P3 cleanup). Other 12 routes/bots.py endpoints still have no integration tests — minimum-scope-PATCH focus per the original TODO entry; broader coverage deferred to F144. [medium] [testing]

