# Research Handoff — where we are, what's next

*Living doc: overwrite at each session close. Last updated 2026-06-08 (the F-BATCH-0608B testing-universe expansion session). Read alongside [PROGRAM.md](PROGRAM.md) (axioms, methodology rules, program state).*

## Where we are, in one paragraph

**The research runway is now wide and fast — the next premise is still John's call.** Two sessions back we killed the PEAD-on-filing premise (F370, clean NULL). This session was pure infrastructure: John asked to clear every TODO item limiting how far/fast/reliably we can expand the testing universe, in one batch, fixing new items rather than deferring. Done — 13 items (F-BATCH-0608B). The headline: **core utilization (F380)** now spreads the three slow serial steps (per-dose bootstrap, the ~16k-read gap-lens loop, the power-grid Monte Carlo) across cores via a shared `parallel_map.py`, with `--workers` flowing to every phase — **measured 6.7× on the power grid / ~5.1× end-to-end on the Linux worker, byte-identical to the single-core path**, panel mmap'd so IPC dropped 806 MB → 5 MB (ready for the 192-core EPYC farm). **Data coverage (F322)** fixed dual-class / rapid-dilution share counts — PTON's 2021 count was wrong (38.5M, now 288.7M); the valuation pillar is now trustworthy across rapid-dilution names. **Worker reliability (F376)** — the dispatcher auto-syncs backend-root deps now, so the stale-dependency crash that bit F348/F370 last session can't recur (the `WORKER_SYNC` workaround is retired). Plus cache pruning (F314), the F348 surprise-layer hardening (F377/F378/F379/F381), and tooling (F323). **No research verdict changed; no confirm alpha touched.** The next-premise decision below is exactly where it was — the runway is just clearer now.

## Done (2026-06-08, this session — F-BATCH-0608B)

13 items, overnight orchestrator cycle (4 implementers → review-wave 15 agents → 2 fixers → re-gate; 1,517 backend tests green, determinism byte-identical). Per-item detail in JOURNAL.md. Telemetry in `.run/F-BATCH-0608B/`.
- **Core/compute:** F380 (parallel_map + --workers everywhere, 6.7×/5.1× worker-measured, mmap panel), F381 (power_audit/F370 polish + 18 tests).
- **Data coverage:** F322 (dual-class share summation + quarterly WA fallback + source tag; PTON 2021 38.5M→288.7M), F314 (EDGAR cache prune, moved after scan, min-keep guard), F320 (confirmed already-shipped), F329 (real Form-4 P-code fixture).
- **Worker/research infra:** F376 (auto-sync backend-root deps), F375 (per-symbol NaN coverage), F373 (census polish).
- **Instrument hardening:** F377/F378/F379 (F348 surprise layer), F323 (archive-todo pre-syncs anchors).

## Next actions

1. **Pick the next premise — STILL OPEN, John's call** (unchanged by tonight's infra work). PEAD-on-filing is dead. Live options: (a) **8-K 2.02 announcement entry** — the drift, if any, is at the announcement not the filing; F348 deltas aren't available at 2.02 time (XBRL arrives with the 10-Q), so this needs a new 2.02-press-release-financials parser (real build, deferred in the F370 spec). (b) **F347 information-speed heterogeneity** ("for whom do filings lead price?") — gated on John's charter approval. (c) A premise family outside the lineage. The census report (`docs/research/2026-06-08-premise-power-census.md`) is still the map of where power exists — and the harness is now much faster to run it on.
2. **F382 — cross-dose bootstrap seed correlation (John's methodology call).** All 3 PEAD doses share one `_SEED`, so block-bootstrap resamples are correlated across doses (pre-existing; F380 preserved it for byte-identical determinism). Independent per-dose seeds would change all bootstrap CIs and need a fresh determinism anchor. Decide before the next dose-response confirm run. [gated on John]
3. **F383 — consume the new WA-approximate share flag in valuation.** F322 now tags shares by source (instantaneous/dei/wa/wa_fy); `compute_valuation` should treat WA-sourced P/S as lower-confidence/approximate.
4. **F372 — R-2 execution gate (John's call).** Census predicts UNTESTABLE (447 D2 events, MDE 4.63pp). Bigger net or shelve.
5. **R-1c question (still open, unhurried):** R-1b's edge is strong-but-uncallable; same options (longer Form-4 history / relaxed floors), survivorship caveat weighs.
6. Carried hardening: F362 (review-wave per-persona telemetry — tonight recorded the wave as one synthetic row), F314/F322/F323/F364/F373/F375 now closed; remaining F360/F377-era polish all shipped. Open infra: F97, F302, F306, F309.

## Operational notes for the next session

- **Worker is the default for ALL compute** (`bin/worker-dispatch.sh <outdir> <logname> <script-and-args…>`; `bin/worker-pull.sh` to retrieve). **F376 is FIXED** — the dispatcher now auto-syncs the backend-root allowlist (edgar/fileutil/shared/turnaround/turnaround_validation), so the manual `WORKER_SYNC` workaround is no longer needed for those. Pass `WORKER_SYNC` only for deps outside that allowlist.
- **`--workers N` now works on `power_audit.py` and `run_f370_explore.py` standalone** (sys.path bootstrap added). Default `workers=1` = exact serial byte-identical; pass a higher count on the worker for the 6.7×/5.1× speedup. Determinism test lives at `.run/F-BATCH-0608B/test_determinism.py`.
- **Share counts changed for rapid-dilution / dual-class names (F322).** Any re-run that touches the valuation pillar (P/S filtering) will now see corrected share counts — this is an improvement, but it means valuation-dependent universe membership can differ vs pre-2026-06-08 runs. The WA-sourced counts are tagged approximate (F383 will wire that into confidence).
- **Reviews go through the `review-wave` WORKFLOW** (structured JSON `args`, adversarial-verify). Ran clean this session (caught the F322 summation gap across 4 personas + a power_audit CLI crash the gate missed).
- **8-K linkage lesson (F370, still current):** 8-K item-2.02 `reportDate` is the earnings-announcement date, NOT the fiscal period end. Link 8-K↔10-Q by TIME (most recent 2.02 before the filing, within ~90d). Helpers in `run_f370_explore.py`.

## Working agreements worth remembering

Plain language always (define every term inline). **All compute on the worker by default** (F376 now auto-syncs backend-root deps). **`review-wave` workflow is the default review mechanism.** Blind/outcome-blind authoring for charter text; John approves anything touching a signed charter, and any change that alters research results or determinism (e.g. F382 seeding) is John's call, never a silent overnight change. F338: real-data probe with pre-stated anchors before believing any instrument; skipped checks report NOT-RUN, never PASS (this session's F322 study-era re-probe caught the 38.5M→288.7M correction the synthetic test couldn't). Explore looks are cheap + (when ledgered) FDR-accounted; feasibility screens are non-ledgered; confirm alpha is the scarce resource. Dose-response over cliff-edges; one frozen hashed score per confirm; the explore-as-power-gate prevents wasted confirm. Promote durable findings immediately (PROGRAM.md). Long-running tasks always followable (log to a file in the artifact dir). Decisiveness within agreements; flag deviations before acting. Population statistics must state the population measured (F364). Claim an F-ID in TODO before commit. Parallelize by default.
