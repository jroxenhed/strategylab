# Research Handoff — where we are, what's next

*Living doc: overwrite at each session close. Last updated 2026-06-08 (the census + fix-wave session). Read alongside [PROGRAM.md](PROGRAM.md) (axioms, methodology rules, program state).*

## Where we are, in one paragraph

**The next-run blockers are cleared and the deck is set for a new premise.** After R-1b came back UNTESTABLE-underpowered (strong but uncallable: MDE 3.40pp vs the 1.0pp floor), we stopped guessing premises and ran a **power census** (F369): for each candidate family, count clean events + measure return dispersion → predict the smallest detectable edge. Verdict: **the power ceiling is rare-event-specific.** High-frequency public filings (PEAD 10-Q/10-K, 8-K item 2.02 earnings, 5.02 officer-change, 8.01) have ~3× the events at the same ~24pp dispersion → binary-test MDE 0.56–0.90pp, comfortably under the 1.0pp floor. **That's where the engine can actually resolve an edge — the next charter (F370) comes from there.** R-2 distress will be UNTESTABLE (447 events, MDE 4.63pp — census saved the blind run). R-1b's "calmer universe" escape is dead (edge lives in small-caps, vanishes where it's calm). A follow-up fix wave (F-BATCH-0608) then hardened everything the next *real* run depends on and made worker-compute the default. Full record in PROGRAM.md under "F369 PREMISE POWER CENSUS" and "F371".

## Done (2026-06-08, this session)

- **F369 census shipped + run on the worker** — `backend/research/premise_power_census.py`, spec `docs/plans/2026-06-08-premise-power-census-spec.md`, report `docs/research/2026-06-08-premise-power-census.md`. F338-anchored (reproduces R-1b: n=4,245, std 23.39pp, one-sample MDE 1.006pp, dose-gap 3.37pp). Two-MDE framing (one-sample vs dose-gap, ~3× harder) promoted to PROGRAM.md.
- **F371 — filing artifact quantified** (placebo control): ~0.9–1.1pp of the uniform filing mean-excess is size/survivorship baseline; event-specific net 0.4–1.05pp; the `no_target` 8-K bucket nets −0.02pp as a negative control that validates the method. The directional net is a screening read, not a verdict — promoted premises re-test on the real harness (whose dose-response Q5−Q1 cancels the baseline automatically).
- **F-BATCH-0608 fix wave (7 items)** — **F374** worker-default dispatcher (`bin/worker-dispatch.sh` + `worker-pull.sh`), **F352** ledger file-lock + None→SKIP, **F358** `universe_loader.py` (one universe truth), **F363** matrix NaN guard, **F315** schema_version, **F330** payload guardrail; **F337** found already-shipped (stale-closed). 5-persona review → 4 fixers → `review-wave` adversarial-verify **workflow** (21 agents) which confirmed the fixes and caught 2 more (unguarded pre-flight SSH timeout; placebo blackout not propagated to PEAD/8K/R2) — both fixed. 1,419 backend tests green.

## Next actions

1. **F370 — PEAD / 8-K earnings-surprise charter (the fun; John's go).** The census says this family has the power. Brainstorm it into a pre-registered, blind-authored charter: a frozen dose score (estimate-free surprise — needs **F348** payload layer on F320's derived fundamentals cache), dose-response Q5−Q1 headline, the standard lens stack. 8-K 8.01 (+1.05 net) and 5.02 officer (+0.96) had the largest event-specific nets; 2.02 earnings has the most events. Start here.
2. **F372 — R-2 execution gate (John's call).** Census predicts UNTESTABLE (447 events, MDE 4.63pp). Decide before spending the approved R-2 run: bigger net (longer period / relaxed floors w/ caveats) or shelve.
3. **The R-1c question (still open, unhurried):** R-1b's edge is strong-but-uncallable. Same options as before (longer Form-4 history → era heterogeneity; relaxed floors); the survivorship caveat (universe has zero dead companies) weighs on any R-1c and on how hard to read the +3.3pp.
4. **F348** (fundamental-surprise payloads on F320's derived cache) — the dose-score enabler for F370. Build before/with the F370 charter.
5. Carried hardening: **F362** (review-wave ↔ run-state token telemetry + make the skill emit structured args not prose — playbook default + args-contract now documented), **F375** (NaN per-symbol traceability, deferred from this wave), F314/F322/F323/F364/F373.

## Operational notes for the next session

- **Worker is the default for ALL compute now (John's standing rule).** Use `bin/worker-dispatch.sh <outdir> <logname> <script-and-args…>` — it probes reachability, syncs `backend/research/*.py`, runs a `WORKER_REQUIRE` pre-flight data manifest (F368), launches via `worker-run.sh`, and falls back LOUDLY to local only if the worker is unreachable. `bin/worker-pull.sh <remote-rel> <local>` retrieves artifacts (atomic). Proven live this session on the census re-run. Don't hand-roll ssh/tar — the dispatcher handles the cmd→WSL→bash quoting trap and SSH timeouts.
- **Reviews go through the `review-wave` WORKFLOW, not hand-rolled persona dispatch** — it adds adversarial verification (it caught 2 issues the manual wave missed this session). `args` MUST be a structured JSON object `{taskId, files, intent, personas:[{key,prompt}]}` — prose crashes its `JSON.parse`; the skill's "Invoke:" hint misleads (echoes prose). Exact contract is now in `docs/private/orchestrator-playbook.md` "Invoking the review-wave workflow".
- **Worker state**: code synced per dispatch; matrix/submissions/r1b-events/regime_states/fdr_ledger present (verified via pre-flight this session). Gitignored data artifacts do NOT travel with git pull — the pre-flight manifest is the guard.
- **Ledger**: now file-locked (fcntl) over the full read-modify-write; `ledger_path=None` SKIPs with a warning (anti-pollution); corrupt JSON is backed up before reset. Still single-writer in practice; F352 lock makes concurrent runs safe.
- Census run telemetry in `.run/F369/`; fix-wave telemetry + all review/fix changelogs + decisions.md in `.run/F-BATCH-0608/`.

## Working agreements worth remembering

Plain language always (define every term inline). **All compute runs on the worker by default** — local is fallback only; it's infrastructure-building, not just a number (John, 2026-06-08). **`review-wave` workflow is the default review mechanism** (adversarial verify; structured args). Blind authors for all charter text. F338: pre-stated anchors before believing any instrument; skipped checks report NOT-RUN/skipped, never PASS. §10/charter pre-outcome fixes are legal but John approves anything touching a charter he signed. Ledger writes are explicit-path-only. Promote durable findings immediately. Long-running tasks always followable (log to a file in the artifact dir). Decisiveness applies within agreements, never to them — flag deviations before acting. Probe before estimating. Parallelize by default. Population statistics in findings must state the population measured (F364). Claim an F-ID in TODO before commit to avoid collisions (hit one this session — F371 reused; reconciled).
